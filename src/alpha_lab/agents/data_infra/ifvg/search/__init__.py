"""Robust FSM configuration search & prop realization lane (additive).

Contract name: ``ifvg_prop_robust_config_search_v1``. This package is a new,
separately-contracted research lane beside the frozen M0–M3 context-experiment
lane. It never modifies existing artifacts, catalogs, or the fixed tier
registry, and it contains no Strategy-Core changes.
"""
