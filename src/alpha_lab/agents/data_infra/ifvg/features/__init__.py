"""Feature-block / bundle / MBP-1 source contracts (additive §7A layer).

The frozen M0–M3 tier registry is never modified; blocks partition it exactly
and the frozen tier bundles reproduce it order-exact.
"""
