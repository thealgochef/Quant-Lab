"""Bounded §7B ML lane for `ifvg_prop_robust_config_search_v1` (R5).

V1 active scope (`ML_REGIME_CONTRACT_PLAN.md` §0, D-044): the
training-prevalence reference, regularized logistic regression, the fixed
CatBoost challenger, and — at R6 — `kmeans_v1`. Everything else is a
registered planned entry with fail-closed refusals. No deep learning, no
reinforcement learning, no automated model/feature/threshold/cluster-count
selection outside a frozen charter.
"""
