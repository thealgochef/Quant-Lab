# Dashboard repair checklist — maintained by the implementation agent

Initial status: task package only. No application changes, tests, or browser checks have been performed by creating this file. Preserve entries if this task is resumed; do not reset progress.

## Current work

- Current step (Sept 23, ~11:45 PM Chicago): DONE — repairs, review fixes, full suite, integrity check and the verified package were complete at 10:20 PM. At the owner's request the remaining constraints are recorded in [OPEN_DECISIONS.md](OPEN_DECISIONS.md), the relevant documentation is updated, and the work is committed and pushed to `main` in two commits (the earlier funded work as it stood before this task, then this repair). No redesign begun.
- Next step: none in this task; the open owner decisions are in [OPEN_DECISIONS.md](OPEN_DECISIONS.md). The handoff package under `reports/` (ignored by git) and the internal engineering store stay local and are never committed.
- Owner decisions required: R5 replacement threshold for draft `9aa2072d…`; R7 scope for the other study types (worker baseline-child semantics) and prepared S0 context data for the Trade review default; R8 which stored data block to use, the two market closures, and authorizing preparation of pre-2026 days with a separate cache location; whether the stored pre-2026 days count as already authorized (the window was opened on that assumption). Also inform the owner of the read-only search incident below. Each decision, the known limitations, the pre-existing test failures, the incident and the evidence locations are listed in full in [OPEN_DECISIONS.md](OPEN_DECISIONS.md).
- Task source baseline: Quant-Lab `main` HEAD `5ece4d650767d60654a7b9104497850a5ddac96b` plus 110 pre-existing uncommitted paths (the funded payout/comparison work, all preserved). Pre-task copies of every uncommitted path: `baseline/pre_task_source.tar` (SHA-256 `a210688e…`) and `baseline/tracked_diff_vs_head.patch` (`cd0bc124…`) in the internal evidence folder.
- Imported engines: main app (`streamlit run scripts/dashboard.py`) imports the installed `strategy_core` package (pinned `7c7111e398c0…`). Dedicated app (`python scripts/run_ifsm_research_ui.py`) imports the pinned checkout `Claude-Quant-Lab-Research-Artifacts/ifsm-research-core/7c7111e…` (source identity `9247f16e…`) by default, or, only with `--research-core`, the scale-out research branch `strategy-core-scale-out-exit` (base `7c7111e`, patch identity `962ad56a…`). Both `--check` receipts: `baseline/launcher_checks.txt`. Neither pin nor branch is modified by this task.
- Stores: main app — `data/ifvg_datasets/search/v1` + `search_test/v1`, drafts `data/ifvg_study_drafts`, jobs `data/ifvg_search_jobs`. Dedicated app — `data/ifsm_ui_replication/{search/v1,search_test/v1,drafts,jobs,funded_comparison_jobs,funded_jobs}`.
- Active jobs and shared-file ownership: no Python/Streamlit processes at 6:30 PM Chicago; no repository file modified in the preceding three hours except this task package; funded TASKS.md records no active job. Codex computer-use Node runtimes (started Sept 21) are running but have not written to the repository.
- Internal engineering evidence folder: `C:/Users/gonza/Documents/Claude-Quant-Lab-Research-Artifacts/ifvg-dashboard-repairs-20260923/` (baseline, scripts, investigation, isolated copies, receipts, screenshots).
- Pre-task artifact digests: `baseline/pre_task_hashes.json` — 22,581 files (all drafts of both apps, both apps' stores, job states, funded plans/approvals/results, published funded review folders, `research/core`, `pyproject.toml`).
- Final repair-review folder: `reports/ifvg_dashboard_repairs/ifvg_dashboard_repairs_20260923_v1/` + `.zip` (verified).

## Acceptance ledger

| ID | Outcome | Status | Evidence / command / result |
|---|---|---|---|
| P0 | Actual roots, both app contexts, pins, uncommitted work, active jobs, and safe test stores recorded | Done | See "Current work" above; `baseline/` in the internal evidence folder. |
| R1 | Saved settings/counts/approvals preserved; incompatible runtime cannot launch a reduced plan; worker payload verified | Fixed and verified | Reproduced on an isolated copy (`scripts/r1_reproduce.py`, `isolated/r1/pre_fix_pinned.json`): opening the saved draft in the pinned app rewrote it (SHA-256 `d812016d…` → `7ab20fb1…`) and deleted the half-exit selection; screen showed 48 configurations with an approval area. Research engine: 64, approved, completed, unchanged (`pre_fix_research.json`). After the fix: pinned = read-only "This study contains 64 configurations and requires … partial exits … Your saved settings have not been changed", bytes unchanged, no approval/run controls (`post_fix_pinned.json`); research unchanged (`post_fix_research.json`). Tests: `test_receipts/r1/`. **Final (8:35–8:50 PM):** browser-checked in both apps and both engines (screenshots 01, 11–13); final-code reproduction on isolated copies: pinned read-only, bytes `d812016d…` unchanged, nothing spawned; research engine 64 × 2 = 128, rebuilt plan = `78406b15…` (`isolated/r1/final_*.json`, `plan_identity_final_*.json`). Review fixes A2, A4, A6 (stale page reset now keyed on the saved file's digest). Tests: `test_receipts/review_fixes/funded_saved_drafts_both_engines.txt` (pinned 14 passed 1 skipped; research 24 passed 1 skipped). |
| R2 | Firm/configuration context stays aligned across ranking, detail, charts, account/trade links and export scope | Fixed and verified | Cause: ranking used `st.tabs` (the chosen tab never reaches Python) while the detail had its own firm radio defaulting to the first firm; detail widget keys were global, not tied to the result. Tests: `test_receipts/r2/` (pre-repair tree 3 failed; post-fix 15 passed). **Final:** browser (screenshots 03, 04, 10); headline + ranking 7/7 to the cent (`isolated/r2/r2_real_results.json`); every completed pair of both results (128 + 64) matches stored trades/accounts/payouts/months to the cent (`isolated/r2/r2_detail_tables.json`); instant-ordering test fails on the pre-task presenter (`test_receipts/r4/pre_fix_tree_instant_order.txt`). Account now remembered per result and pair (review A5). Tests: `final_test_funded_comparison_screen.txt` 17 passed. |
| R3 | Existing trade review reaches actual funded fills and preserves existing sources and notes | Fixed and verified | Reproduced on the pre-repair screen (`test_receipts/r3/pre_fix_behavior_repro.txt`: 'No saved searches are available for review.' with a completed funded comparison present). Fix: funded comparisons of the app's registered store join the existing Study selector; new `scripts/ifvg_funded_trade_review.py` + pure `presentation/funded_trade_review.py` show the recorded path (entry, quantity, initial stop, half fill with exact nanosecond time, remaining quantity, moved stop, final exit, liquidation check and replacement) on the strategy's verified original bars from the plan's bound study package; gap zones only where the verified study saved the exact trade; honest messages otherwise; reviews use funded-only ledger keys; results → review action and back-to-result restore. Real-data AppTest over 10 representative trades (both results): `isolated/r3/r3_probe.json`. Tests: `test_receipts/r3/post_fix.txt` (11 passed). **Final:** browser (screenshots 05–10, 26 — account carried into Trade review and restored by Back); final-code probe of 10 real trades: 10/10 charts, no errors (`isolated/r3/r3_probe_final.json`). Review fixes A1 (Incomplete/review-only results offered with a label; no silent fallback), A3 (honest gap-zone captions), A5 (account carried). Tests: `final_test_funded_trade_review.txt` 13 passed. |
| R4 | Human-facing Chicago AM/PM conversion is consistent; machine instants and trading rules unchanged | Fixed and verified | New shared helper `presentation/chicago_time.py` (converts instants; 12-hour AM/PM; CST/CDT named; naive input refused unless its UTC convention is named). Applied in the review screen (selector, axis, markers, gap zones, lifecycle), the funded trade review, the verifier charts/captions/hover/review times, research review tables, research candidate chart and cutoff columns, results timeline axis. Session logic, stored values, ledgers, exports unchanged. Pre-repair output reproduces the screenshot ('1. 2026-01-12 23:31 ET · stop · 25883.75', `test_receipts/r4/pre_fix_tree_review.txt`). Tests: helper 13; review 2; helper-owned sites 11 new + 115 guards → 126 passed (11 new fail on originals). The 'no overnight holding' wording is already corrected (not reproduced). **Final:** browser (screenshots 07, 20, 21). Review fixes B8 (last Eastern caption), B11 (NumPy integers) and C1 (chart positions to the microsecond — a nanosecond fill no longer triggers a Plotly warning). Tests: 40 passed across the time helper, display sites, review and funded review files. |
| R5 | 2,050-day observation explained; correct input/format validation without guessing a replacement | Fixed and verified (validation); owner value decision open | 2050 is the saved value (draft `9aa2072d…` field `steps.benchmarks.strategy_gates.min_independent_days` = 2050.0, inherited from frozen draft `caca5ab4…`; approval `8369bbf0…` and charter `79485ce6…` record it; that search completed with 46 configurations first failing on independent days). Not a display/parse/autosave defect; how it was typed is not recorded. New `search/charter_day_threshold.py` refuses a threshold above the evaluated-date count (warmup excluded; verification policy exempt) at satisfiability/review, approval request (before saving), charter validation (freeze, save, approval, worker entry, child boundary); whole-number gates refuse fractions/negatives. Never clamps; historic charter still loads. Tests: 23 new (21 fail on originals) + 6 fixtures corrected from impossible to reachable thresholds. Owner decision: the replacement value for draft `9aa2072d…` only. **Final:** browser (screenshot 19: 2,050 vs 107 evaluated days refused on Review; approval blocked; saved value unchanged). No review finding. |
| R6 | Reported loading stall measured; bounded fix verified or limitation documented | Fixed and measured (partial: first load still ~4 minutes) | Route: main app → New study → More study types → Feature and model study (`render_research_configuration` → `list_source_subjects`). Before: 524 s cold (first call), 560 s second call, AppTest 559 s first render / 556 s rerun (`investigation/R6/`). Cause: each of 356 children re-verified every membership and neutrality envelope (262,728 verified loads per listing) and the listing re-ran on every rerun. Fix: one verified pass per listing call (`research_subject._verified_family`, default callers unchanged) + display-only cache keyed by the resolved store and every entry's manifest stat, with a spinner. Scratch-copy measurement: 254 s first load, 0.09 s rerun, rows identical. Remaining: first load still ~4 minutes (full verification of every child) — deferred; all 356 children list as unavailable under the current pinned engine (separate compatibility limitation, reported). Tests: 3 new. **Final:** browser first visit at most 4 min 51 s, second under 3 s (screenshots 23–25; `performance_checks.csv`). Review fix B10 (cache signature also tracks `envelope.json`). |
| R7 | New studies default to a named S0_D80_W1_P1 baseline with the mandatory close; legacy baseline unchanged and warned | Partly fixed — Evaluate default, warnings and review statement verified; other study types and Trade review default blocked on owner decisions | `named_baselines.py` builds S0_D80_W1_P1 from the verified package (10 saved values; re-resolves to `86261cc9…`; refused if it differs); not a registered profile (that would rename its identity). New Evaluate studies start on it in both apps (at creation only). Legacy warning in both wizards' baseline step and in Trade review / context research configuration; review steps state a non-daily-close holding rule before approval. Opening a saved draft no longer rewrites it (baseline hash from the earlier engine kept until the owner clicks Update; no keys injected; fixed settings not injected). Blocked: Compare/Search/Prop/Strategy across firms/Full workflow would need the worker to treat a non-registry configuration as the baseline child (research-semantics change); Trade review/context default needs prepared S0 context data (a run not authorized). Tests: 10 new + 3 updated. **Correction (review B2):** the claim above that fixed settings were not injected was incomplete — two real legacy Evaluate drafts (`f3099b0b…`, `708c133a…`) still had the inherited gap rule injected on opening (pre-existing behavior). Fixed: the rule is recorded only with an owner edit; opening returns the saved settings unchanged (tests in both apps). Other review fixes: B1 (holding count when a Search varies it), B3 (tests without the local archive), B7 (warning/statement no longer fail open), B9 (developer Start new draft). Browser: screenshots 14, 15, 18, 22. |
| R8 | Permitted research period extended back to the earliest authorized local data; June 11, 2026 onward still refused at every layer; date range replaces one-per-line entry | Fixed and verified; owner decisions open | Earliest stored day: 2021-12-02 (blocks 2021-12-02–2022-03-10 and 2025-06-02–2026-06-10); earliest evidence day with ten stored warmup days: 2021-12-14. `research_period.py` is the single source; start/end pickers (June 11+ cannot be picked) resolve with the existing calendar, list left-out days with reasons (missing files, two evidenced Christmas closures), show count/first/last and the exact list; saved lists kept byte-identical. Extended at the browser, saved-plan `DatePolicy`, validation step and the worker's replay policy; June 11 protection unchanged at every layer. Unchanged on purpose: the preparation date list and its audit hash, registered calendar closures, the 2026-only prepared-input gate (preparing older days would overwrite 2026 inputs under the current cache layout), MBP-1 evidence scope. Approval of a pre-2026 plan is blocked with a plain message until preparation is authorized. Tests: 7 new + 3 updated; 307 access/calendar/approval tests pass. **Final:** browser (screenshots 16, 17, 18). Review fixes B4 (saved lists outside the pickers open unchanged), B5 (not-prepared note follows the first replayed day), B6 (a changed unusable range saves no dates). The date-step scan test was updated for the pickers (the old test counted one date text area) and now also scans `ifvg_research_dates.py`. |
| V1 | Relevant new and regression tests completed with truthful counts | Done | Final full suite on the final code (9:37–10:14 PM, `test_receipts/full_suite/suite.log`): **3,540 passed, 5 failed, 2 skipped**; all 5 failures pre-existing (fail identically on the recorded pre-task tree: catboost frozen bundle, htf_cap_experiment, three capture-scheme tests). Run 1 (8:50–9:35 PM): 3,539 passed, 6 failed — the sixth (missing help text on the new R7 button) fixed, then run 2. Targeted batch after review fixes: 1,722 passed, 2 failed (htf_cap pre-existing; a scan test that counted the R8-replaced date text area, updated). Fail-before evidence per repair in `validation_summary.json`. |
| V2 | Actual rendered-browser checks completed in both applicable apps/runtime contexts | Done | Chrome on isolated copies: main app (8611), dedicated app pinned (8612) and research engine (8613); 29 captures internally, 16 exported with `SCREEN_EVIDENCE.md`; final-code account carry-over checked after the review fixes (capture 26). |
| V3 | Completed financial records, saved manifests, approvals, and source pins preserved | Done | 22,581 snapshotted files identical before and after (0 changed/added/removed); saved variation draft, plans 78406b15…/9edbbdcd…, results 5fa65149…/92f5a08d…, approvals, R5 draft/charter/approval, legacy drafts f3099b0b…/708c133a…, review ledger and exports unchanged; launcher identity checks and the `pyproject.toml` pin unchanged (`study_integrity_checks.csv`: 33 Pass + 1 isolated reproduction). |
| V4 | Fresh read-only review completed; demonstrated in-scope issues fixed and retested | Done | Two fresh read-only reviewers (R1–R3, R4–R8) against the pre-task state: no blocking finding; 4 should-fix and 12 minor/evidence items, all fixed and retested, plus A6 and C1/C2 found by the lead (`REVIEW_FINDINGS.md`; internal `review_input/reviewer_*_report.md`). |
| V5 | Compact final folder/ZIP verified, with actual screenshots and no data/code dumps | Done | `reports/ifvg_dashboard_repairs/ifvg_dashboard_repairs_20260923_v1/` (24 files + manifest, 16 screenshots) and `ifvg_dashboard_repairs_20260923_v1.zip` (1,099,611 bytes, SHA-256 `f46f1e6a…f8ed`); verified by the builder and by an independent extraction (inventory, hashes, CSV shape, links, no local paths). `reports/` is ignored by git. |

Use Reproduced, In progress, Fixed and verified, Not reproduced with evidence, Blocked, or Not verified when work advances. A test not run is not passed. Record what remains before stopping.

## Decisions, corrections, and job record

Append dated facts, owner answers, root-cause corrections, task-specific changed files, job start/finish states, and verification references here. Keep earlier conclusions as history when superseded; do not invent approvals. All owner-facing times use Chicago with AM/PM.

### September 23, 2026 (Chicago)

- 6:30 PM — P0 reconnaissance and baseline (see "Current work"). Read-only helper investigations launched: R3+R4 (trade review, time presentation), R5+R7+R8 (wizard threshold, default baseline, research period), R6 (loading stall). Helpers write only to `investigation/` in the internal evidence folder.
- R1 root cause (established by reproduction and code reading): `scripts/ifvg_funded_comparison_study.py` built each multiselect's default from the saved values filtered to the options the running registry offers (`[v for v in saved if v in options]`) and skipped the exit-rule setting entirely when the pinned Core has no `exit_policy` axis; it then saved whenever the widget-derived settings differed from the stored ones (`updated != current`), so merely opening the draft wrote the reduced selection to disk. The same pattern silently substituted the first study or base configuration when a saved one was missing, dropped unknown firms, and wrote normalized defaults into older drafts. The job `start` command queued a worker before any approval check (the worker refused later).
- R1 fix: new `src/alpha_lab/propsim/funded/comparison_draft.py` (faithful saved-draft reading, compatibility check, engine-independent configuration count, exact saved-plan matching, shared plan rebuild); configurator shows unrepresentable drafts read-only with the saved settings table and the launcher prerequisite; saves only on a widget change (`on_change`), never on open/refresh/navigation; widget state resets when another draft is opened or the draft changes on disk; launch re-reads the draft from disk, re-checks it, rebuilds the plan and requires the identical plan id and its approval (`dispatch_problem`); job `start` refuses an unapproved plan before queuing. Chips show the value itself (`_chip_label`), long chips wrap (scoped CSS), and a "Selected: …" line lists every complete value. `presentation/workspace.py` gains `axis_value_text` (additive).
- R1 fail-before evidence: a pre-repair tree (HEAD `5ece4d6` + the pre-task uncommitted copy + only the new helper module and test) — 10 of the new tests fail, 1 errors at teardown. In that pre-repair run the job `start` test queued a worker from pytest's temporary store; that worker stopped at "no stored owner approval" (state `Failed`, temporary store only; no study ran; real stores untouched).
- R2 root cause and fix: see the ledger row. `scripts/ifvg_funded_comparison_results.py`: one firm selector above the ranking drives the ranking table and the detail; the selected firm/configuration is stored per result id (`selected_context`), widget keys are scoped to the result, the account selector is keyed by result + configuration + firm; the first opening says which firm's leader it opened on. The full-study review folder caption now says it covers every configuration and firm. Three screen tests were updated from tab assertions to the single selector with the same figures asserted.
- ~7:45 PM — R3+R4 investigation returned (internal `investigation/R3_R4/` probes). R3 cause: `scripts/ifvg_search_review.py` consulted only strategy-search job states and returned early ("No saved searches…") before looking at the funded comparisons that `load_studies` already lists; the dedicated app has no strategy-search jobs. R4: the review screen, verifier charts/captions and research review tables formatted New York or raw UTC ISO times for people; no shared Chicago presentation helper existed. The "no overnight holding" wording is already corrected in current wording and on load of saved results (not reproduced).
- ~8:05 PM — R5+R7+R8 investigation returned (internal `investigation/R5_R7_R8/probes/`). R5: 2050 is the saved value (not display/parse/autosave): draft `9aa2072d…` field `steps.benchmarks.strategy_gates.min_independent_days = 2050.0`, inherited from frozen draft `caca5ab4…`; approval `8369bbf0…` and charter `79485ce6…` record 2050; that completed search ran with it (all 192 configurations failed gates; 46 first-failing on independent days, observed 18–47). No layer compared the threshold with the evaluated-date count. R7: S0_D80_W1_P1 re-resolves to `86261cc9…` under all three Cores; registering it as a new profile would rename its canonical identity (→ `74b81e47…`), so it must be a verified preset (legacy profile + its 10 saved axis values), not a registered profile. R8: the only verification/authorization records cover 2026; local files start 2021-12-02 in two blocks (2021-12-02–2022-03-10 and 2025-06-02–2026-06-10); no prepared IFVG day files exist before 2026; preparing earlier chains would overwrite 2026 prepared files under the current cache layout.
- INCIDENT (read-only, reported by the R5/R7/R8 investigator): one recursive text search over `data/` ran without a file-type filter for about two minutes before it was stopped. It may have opened raw market-data files under `data/databento/NQ`; the investigator cannot tell whether it reached partitions dated June 11, 2026 or later. It printed nothing, wrote nothing, and no study, preparation or dataset used it. All later searches excluded `data/databento`. Helpers are now told never to search `data/` recursively. Owner to be informed in the handoff.
- R4 display sites in the verifier/research-review/results-chart files delegated to a helper (file ownership recorded in its brief); R5 delegated to a helper owning `charter.py` (validate_charter call only), `charter_satisfiability.py`, `strategy_approval_review.py`, `strategy_executor.py`, `study_presentation.py` (benchmarks step only), a new check module and test fixtures. The lead owns the funded screens, the review screen, and (after R5) the wizard/date files for R8 and R7.
- ~7:55 PM — browser checks on isolated copies (main app on port 8611, dedicated app pinned on 8612 and with the research engine on 8613); screenshots and log in the internal `screenshots/` folder. Two review screens were corrected during them (dollar amounts rendered as math in the facts table; the entry and moved-stop labels overlapped) and the validation step's charter warmup for pre-2026 ranges (was "1 validation error for DatePolicy").
- 7:57 PM — full regression suite started detached; two fresh read-only reviewers started against the task-only patch (`review_input/task_changes.patch`, 55 files): R1–R3 (funded-payout reviewer) and R4–R8 (general-purpose, told to stay read-only).
- ~8:14 PM — R1–R3 review returned: no blocking finding; 1 should-fix (Trade review silently fell back to another study for funded results that were Incomplete, review-only or archived), 4 minor (launch crashed instead of refusing when a draft names no saved study; gap-zone caption stated an unchecked cause and hid verification errors; the job refusal test did not stub process launch; the results page's account was not carried to Trade review) and evidence gaps. Report: internal `review_input/reviewer_R1_R3_report.md`.
- ~8:20 PM — R4–R8 review returned: no blocking finding; 3 should-fix (review holding statement over-counted when a Search varies the holding rule; two real legacy Evaluate drafts, `f3099b0b…` and `708c133a…`, were still rewritten on opening by an injected gap-invalidation rule — pre-existing, but the R7 row above over-claimed; two updated tests depended on the local study archive), 8 minor (saved dates outside the pickers crashed the step; the "inputs not prepared" note ignored warmup days; a changed unusable range silently kept the old list; the legacy warning and holding statement failed open; one Eastern-time caption; the developer "Start new draft" path skipped the named baseline; the listing cache ignored `envelope.json`; NumPy integers refused as times). Report: internal `review_input/reviewer_R4_R8_report.md`.
- 8:19 PM — the running full suite (8% done) was stopped because the review fixes change the code it was testing; its partial log is kept as `test_receipts/full_suite/superseded_run_stopped_at_8pct.log`. A new full suite runs on the final code.
- 8:20–8:45 PM — every review finding fixed with a test (none required an owner decision). Writing the stale-window test exposed one more defect: the draft's save time has one-second precision, so a save from another window within the same second did not reset the open page, and "Record my approval" there recorded an approval of the plan that page showed (launch still refused it as out of date). The page's reset is now keyed on the saved file's digest. Evidence gaps closed: the Chicago instant-ordering test fails on the pre-task presenter (`test_receipts/r4/pre_fix_tree_instant_order.txt`); every completed pair of both saved results (128 + 64) matches its stored trades, accounts, payouts and months to the cent (`isolated/r2/r2_detail_tables.json`); the isolated pre-repair tree is documented (`isolated/prefix_tree_verification.json`: nine task files there were added only so tests could import — new modules/tests and the additive `axis_value_text`).
- 8:50–9:35 PM — full suite on the final code (run 1): 3,539 passed, 6 failed, 2 skipped (44 min 50 s). Five failures are pre-existing and fail identically on the recorded pre-task tree: `test_catboost_bundle_model::test_frozen_m0_m3_catboost_lane_is_byte_and_identity_unchanged` (frozen bundle hash), `ifvg_search/test_htf_cap_experiment::test_exact_four_effective_profiles_and_distinct_cap_identities` (A_cap1_opp80_control) and three `test_ifvg_capture_scheme` tests (default capture tag 92b4ab17… vs 035d9e14…); receipts in `test_receipts/review_fixes/pre_existing_*`. One was caused by this task: the new R7 "Update the saved baseline to the current engine" button had no help text (`test_ifvg_help_scans`). Help text added in both wizards; 89 passed in the affected files; full suite started again at 9:37 PM (run 2). Run-1 log kept as `test_receipts/full_suite/final_run1_before_help_text_fix.log`.
- ~9:40 PM — post-task integrity snapshot: all 22,581 snapshotted files (both apps' drafts, stores, job states, funded plans/approvals/results, published funded exports, the review ledger, `research/core`, `pyproject.toml`) identical to the pre-task snapshot — 0 changed, 0 added, 0 removed. Launcher identity checks identical before and after (pinned `9247f16e…`; research `962ad56a…`); `pyproject.toml` pin `7c7111e` unchanged. Isolated servers stopped; the isolated copy's junction to `data/databento` removed with `rmdir` (link only; live folder intact).
- 10:14 PM — full suite run 2 on the final code: 3,540 passed, 5 failed (all pre-existing), 2 skipped (37 min 40 s). 10:18 PM — package built and verified; independent extraction check passed. Handoff complete.
- ~11:30 PM — The owner asked to record the remaining constraints, update the relevant documentation, and commit and push the changes to `main`. These are now listed in full in [OPEN_DECISIONS.md](OPEN_DECISIONS.md): the open owner decisions, the known limitations, the behavior changes that make older descriptions out of date, the pre-existing test failures, the read-only search incident and the evidence locations. Recording them changed no draft, plan, approval, result, export or engine pin, and no study was run. The handoff package under `reports/` (ignored by git) and the internal engineering store are not committed.
