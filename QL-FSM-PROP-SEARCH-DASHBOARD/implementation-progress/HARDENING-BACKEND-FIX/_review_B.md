# Reviewer B — HARDENING-BACKEND-FIX (read-only adversarial review)

## Reviewed

- Diff: `C:/Users/gonza/.claude/jobs/6befc40d/tmp/review_diff.patch`, sha256 `662bc4423bc3dad02ac54eef03ea50c0e9458bce09e359f958e16f4b77cd716b` (7,987 lines, 50 files) against parent HEAD `e56f9376a5b4ba269f7fa11cdc6e37b08200638f`. Plan §1, §3.2, §5, §6, §7, §9, §11, §12.2; COMPLETION_REPORT §2, §3, §5.
- Diff hunks read in full: `ml/regime_contracts.py`, `ml/regime_oos_assignment.py`, `ml/regime_fold_features.py`, `ml/regime_assignment_sources.py`, `ml/comparison_rows.py`, `ml/controlled_feature_study.py`, `ml/regime_controlled_study.py`, `ml/regime_cohort_model.py`, `ml/regime_supervised_stage.py`, `ml/regime_stratification_service.py`, `search/pipeline.py`, `search/store.py`, `search/trading_calendar.py`, `search/verification_window.py`, `search/seed_production.py`, `propsim/event_detail.py`, `propsim/search_bridge.py`, `scripts/hardening_capacity_benchmark.py`, and the test diffs for OOS assignment, assignment evidence, fold features / fold-feature evidence, label identity, store sidecar probe, public source-kind surface, event-detail streaming, account event detail.
- Working-tree call paths inspected: `_fit_assignment_frame_for_schema` → `validate_native_values` → `to_numeric`; `fit_assignment_frame_from_bytes`; `validate_assignment_rows` (reason vocabulary per kind); `VerifiedFitAssignments` / `_verified_fit_frames` / `consulted_assignment_frame` → `candidate_fold_oos_assignment`; `_frame_for_schema` → `_assert_candidate_sets_equal` → `verify_assignment_table_bytes` (save and load); `validate_fold_feature_rows` → `assert_fold_feature_spine_bound` (build line 970, load line 1062); `_verified_manifest` → `_validate_manifest_entries` → `_sidecar_path` on all seven read paths; `has_envelope`; the whole-tree sweep for other `manifest.json` readers; `save_controlled_feature_study` / `save_regime_*_study`; `_verified_child_tables`; `_PHYSICAL_STEM_RESOLVER` and every `mbp10` hit under `src/` and `scripts/ifvg_*.py`; `_require_no_legacy_provenance`; `build_account_event_detail` (`_flush`, cleanup, manifest write), `_external_uniqueness_check` (DuckDB, memory limit + spill dir, never in-memory), `load_account_event_detail(_manifest)`; the benchmark's `_evaluate_b1_shapes` gate.
- Tests run (one file at a time): `tests/agents/ifvg_search/test_store_sidecar_probe.py` 31 passed, 0 skipped (the symlink test executed on this host); `tests/agents/data_infra/ifvg/test_regime_fold_feature_evidence.py` 5 passed; `tests/propsim/test_event_detail_streaming.py` 14 passed; `tests/agents/data_infra/ifvg/test_regime_oos_assignment.py` 10 passed.
- Probes (synthetic inputs only, under `C:/Users/gonza/.claude/jobs/6befc40d/tmp/review_B/`): `probe_lane1.py` (numpy/pandas subtleties, coercion order, set equality), `probe_spine.py`, `probe_manifest.py` (16 entry shapes), `probe_kind.py`, `probe_event_detail.py` (mid-write failure, manifest-stage refusal, 1,000-event whale path under a 7-row bound).

What held up under probing: native validation refuses `np.bool_` as int, `np.int64` as bool, NaN / `pd.NA` in non-nullable columns, `pd.NaT` as id, bytes ids, bools inside distance vectors; nullable `Int64`/`boolean` dtypes validate correctly; the no-reason invalid OOS row is typed `assignment_provenance_incomplete`; the manifest validator refuses NUL, trailing space, NFD/NFC collision, envelope-declared-twice, float/bool/negative bytes, non-hex/newline hashes, ADS colons; forged `"mbp10"` kinds are refused by `assert_public_source_kind`, `trading_day_ref_from_inventory` and `seed_chain_source_inventory_hash`; the inventory JSON never carries the stem; the whale path split into 149 partitions of ≤ 7 rows across 3 blocks.

## Findings

| ID | Checklist item | Finding | Severity | Evidence (file:line) | Proposed disposition | Test proving resolution |
|---|---|---|---|---|---|---|
| RB-01 | Plan §7.2, HB-FIX-09 | The exact-label proof is opt-in. `run_controlled_mbp1_study(label_artifact_id=<any 64-hex>, label_policy_id=None)` skips `assert_exact_label_artifact`, still stamps `label_identity_source="label_artifact"`, and `save_controlled_feature_study` only refuses `content_hash_unpersisted` — so a study whose label id is an arbitrary caller string is persistable. `run_controlled_regime_study` / `run_regime_cohort_model_study` default `label_policy_id=None`, their payloads accept any `SHA256_PATTERN` string and `save_regime_*_study` performs no label check. Only the pipeline seams pass the policy. The plan's "Persisted controlled studies require the exact immutable label-artifact identity" is met for the pipeline path only. | MAJOR | `ml/controlled_feature_study.py:345-355, 475`; `ml/regime_controlled_study.py:446-452, 596`; `ml/regime_cohort_model.py:233-239, 479` | Require `label_policy_id` whenever `label_artifact_id` is supplied (or stamp a distinct `label_artifact_exact` source only after the proof and make every `save_*_study` refuse anything else). | Add to `test_label_identity.py`: `run_*` with a label id and no policy → typed refusal; and `save_*_study` on a run built without the proof → refusal. `test_persisting_seam_proves_the_exact_label_artifact` today only exercises the policy-present branch. |
| RB-02 | Plan §6.4, HB-FIX-06 | A fold whose `FoldFitRef` binds a fit accepts a row typed `no_valid_regime_fit` with a null `regime_fit_id`: `validate_fold_feature_rows` only refuses (`fit_less & fit_nonnull`) and (`~fit_less & ~fit_nonnull`), and `assert_fold_feature_spine_bound` treats an empty fit set as inside the bound. Probe: such a table passes `validate_fold_feature_rows`, `assert_fold_feature_spine_bound` and `fold_feature_table_bytes`, i.e. a known invalid assignment of an applicable fit can be collapsed into fit-less generic absence and re-loaded as lawful. The builder never emits this shape, so the hole is loader/verification-side. | MEDIUM | `ml/regime_fold_features.py:700-712, 734-747, 1062` | In `assert_fold_feature_spine_bound`, when `ref.regime_fit_id` is not None, refuse any row whose `regime_fit_id` is null (every row of a fit-bearing fold names that fit); reject `no_valid_regime_fit` there. | Extend `test_invalid_fold_feature_rows_retain_the_complete_reconciliation_spine`: set `regime_fit_id=None` AND `missing_reason="no_valid_regime_fit"` on the victim row → `assignment_provenance_incomplete` from both validators; mirror in `test_loader_refuses_a_stored_row_outside_the_bound_fold_fit_refs`. |
| RB-03 | Plan §9.2 "cleanup after failure", HB-FIX-11 | Cleanup does not cover the whole build. (a) The detail-manifest stage (line 806-813) is outside the `try/except BaseException`; a refusal there (probe: pre-existing `event_detail_manifest.json` → `EventDetailIntegrityError`) leaves every partition file (15 in the probe) in a caller-owned directory. (b) `written.append(path)` runs after `_write_parquet` (a direct, non-atomic `pq.write_table`); an `OSError` mid-write (probe: ENOSPC on partition 3) leaves the partial `event_detail_block_000000_002.parquet` behind. The store path discards its temporary directory, so only direct-directory callers are affected, but the plan's cleanup rule and the report's "cleanup of every written partition on refusal" are not met for these two shapes. | MEDIUM | `propsim/event_detail.py:678-679, 778-784, 806-813` | Append `path` to `written` before `_write_parquet` (or write to a temp name and rename), and move the manifest write inside the guarded region. | Add to `test_event_detail_streaming.py`: monkeypatch `_write_parquet` to write partial bytes then raise → directory empty; pre-create the manifest → refusal with directory empty. |
| RB-04 | Plan §9.3 gate "maximum resident writer batch", HB-FIX-11 | The benchmark's resident-batch gate compares `bundle.max_partition_rows` with the bound, but `max_partition_rows` is the largest partition the writer *wrote*, and `_flush` itself raises above the bound (line 664-667) — the gate cannot fail and measures neither the generator's resident rows nor the DuckDB check. Related: the reader's bound proof consults the manifest's loose top-level `max_rows_per_partition` (line 929) instead of the identity-bound `payload.event_detail_budget.max_rows_per_partition` (only `manifest["budget"]` is compared to the payload, line 892-897). | MINOR | `scripts/hardening_capacity_benchmark.py:381, 780-786, 824-826`; `propsim/event_detail.py:664-667, 929` | Gate on an instrumented maximum of `len(columns["event_id"])` observed inside the writer (or on the tracemalloc peak of the writer frame), and bind the reader's bound to the payload budget. | Benchmark assertion on the instrumented writer maximum; reader test: manifest top-level bound ≠ budget bound → `EventDetailIntegrityError`. |
| RB-05 | Plan §6.1-6.2, HB-FIX-04/05 | `regime_for_trades` and `candidate_fold_oos_assignment` coerce their INPUT `valid` column (`bool(row["valid"])`, `astype(bool)`) before any native validation: probe `regime_for_trades` with `valid="False"` on an invalid descriptive row projected `valid=True, missing_reason=None` (the plan's "apply the same provenance rule when projecting to executed trades"); `candidate_fold_oos_assignment` with `valid="False"` on an invalid fit row treated it as valid (refused later only because the row had no distances). Production callers pass verified-loaded frames (`regime_stratification_service.py:495`, `regime_stratified_strategy.py:261`), so the risk is confined to direct API use. | MINOR | `ml/regime_assignment_sources.py:119`; `ml/regime_oos_assignment.py:507-513, 638-646` | Run `validate_native_values(frame, OOS_ASSIGNMENT_NATIVE_SPEC)` at the top of `regime_for_trades` and the fit spec at the top of `candidate_fold_oos_assignment` / `assign_panel_regimes_to_candidates`. | Add `"False"`-string cases to `test_trade_projection_retains_invalid_assignment_provenance` and `test_oos_and_fold_feature_tables_validate_native_values_too` asserting `native_value_refused`. |

Lanes with nothing to report: manifest validator and `_sidecar_path` (§7.1, HB-FIX-08) — every store read path goes through `_verified_manifest`; the remaining `manifest.json` readers in `src/` belong to other artifact families (see carry-forward 2). Public source-kind boundary (§5, HB-FIX-03) — the stem is confined to the resolver; no serialized contract, inventory, window, seed hash or descriptor-consuming code path carries or branches on it; `_require_no_legacy_provenance` refuses the legacy kind at the feature layer. Schema identity / candidate-set equality (§6.5, HB-FIX-07) — the hash consulted at load is recomputed from the exact bytes verified, and the test forces the bytes hash to pass so the schema proof itself is exercised.

## Verdict

No BLOCKER. Counts: MAJOR 1 (RB-01), MEDIUM 2 (RB-02, RB-03), MINOR 2 (RB-04, RB-05). RB-01 should be fixed before closure because the plan's §7.2 MUST is enforced only where the caller volunteers the policy; RB-02 and RB-03 are one-line-class fixes with obvious tests.

I did not open, list or construct any path under data/.

## Carry-forward (out of scope, ≤5)

1. COMPLETION_REPORT §4 (`<<TESTS>>`) and §9 (`<<FINAL_GATE>>`) are unfilled placeholders — the test-count and final-gate claims are absent from the report as it stands.
2. Non-store manifest families still read `manifest.json` with `json.loads` + `entry["path"]` outside the central validator: `ifvg/artifact_io.py:94-119`, `context_run_store.py:137-311`, `fsm_audit_io.py:57-79`, `replay_chart_store.py:607-659, 1120-1151`, `ml/logistic_model.py:550` (M0–M3 / exploration lanes; outside §7.1's store scope).
3. `PhysicalSourceDescriptor.replay_bytes_only` and `physical_descriptors_from_permitted_source_hashes` have no consumer in `src/` — the capability statement is declared, not enforced; the legacy boundary is enforced by `mbp1_source_artifact._require_no_legacy_provenance`.
4. The public-surface scan regex `["']mbp[-_ ]?10["']` misses dotted literals such as `"mbp10.parquet"` (none present today), and `physical_source_descriptor` does not validate the filename segment of a partition key (`.../mbp10.parquet/../mbp1.parquet` resolves by its last segment).
5. `validate_native_values` raises an untyped `OverflowError` for an int beyond float range in a float column (`np.isinf` on a huge Python int); and a lawful single-path whale (10M events on one path) is held upstream in the walk result's `events` tuple, outside the writer's bound and unmeasured by the benchmark (skewed shape = 180k events/path → 217.5 MiB).
