# Reviewer A — HARDENING-BACKEND-FIX (lock, namespace init, chain proof, seed canonicalization)

## Reviewed

- Diff: `review_diff.patch` sha256 `662bc4423bc3dad02ac54eef03ea50c0e9458bce09e359f958e16f4b77cd716b` (7,987 lines); parent HEAD `e56f937`; working-tree files cited below at their current line numbers.
- Plan §1, §3.2, §4, §8, §10, §11, §12.2 (HB-FIX-01/02/10/12); COMPLETION_REPORT §2–§3.
- Read in full: `search/file_mutex.py`, `search/owner_decision_lock.py`, `search/store_namespace.py`, `search/supersession_chain.py`; the diff hunks plus working-tree bodies of `owner_decisions.py` (`verify_complete_owner_authority_chain`, `CompleteOwnerAuthorityChain`, `load_supersession_chain`, `load_owner_decision`, `assert_owner_decision_authorizes`), `authorization.py` (`assert_authorization_bound_to_store`, `validate_owner_authorization`), `seed_production.py` (`_witness_current`, `persist_/synthetic_/verify_seed_production_authorization`, `_canonical_utc`, `run_seed_production_chain` persist tail), `bounded_verification.py` preflight step, `child_replay.py` (`canonicalize_seed_datetimes`, `canonical_seed_hash`, `save_seed_snapshot`).
- Seam enumeration (grep of `src/` + `scripts/`): every caller of `assert_authorization_bound_to_store` (charter.py:537, executors.py:133, verification.py:205, pipeline.py:1096/3035, mbp1_coverage_diagnostic.py:292, authorization.py:384) and of `load_supersession_chain` (regime_block_activation.py:287, regime_store.py:709, regime_stratification_gate.py:183, regime_study.py:405) now runs the complete proof; `assert_head_witness_current` has no production caller; `current_supersession_head_witness` survives only as a witness MINTER (seed_production.py:558/961/1076, scripts/ifvg_store_namespace.py:42) whose product is re-proven at persist/verify. Every `load_owner_decision` outside the module is paired with `assert_owner_decision_authorizes(... load_supersession_chain(root))`; namespace-only consumers (`load_store_namespace`/`namespace_class_of`) grant no authority without the head. Seed persistence: `save_seed_snapshot` is the only writer of `seed_snapshots`/`seed.pickle` (grep); the runner calls it (seed_production.py:887).
- Test diffs read: conftest, test_owner_decision_lock, test_store_namespace, test_authority_chain_seams, test_authorization, test_bounded_verification, test_child_replay, test_seed_production.
- Tests run (one file at a time, `-p no:cacheprovider`): test_owner_decision_lock.py 15 passed; test_store_namespace.py 15 passed; test_child_replay.py 18 passed; test_authority_chain_seams.py 10 passed.
- Probes (all under `.claude/jobs/6befc40d/tmp/review_A/`, synthetic tmp roots only): `probe_mutex.py` (msvcrt semantics: second handle in the same process times out, second process times out, unlink-while-held refused, a persistent non-contention `OSError` surfaces as a timeout), `probe_canon.py` (uncovered carriers), `probe_seed_types.py` (walks the synthetic `IfvgDaySeed`: dataclasses only, 433 stdlib `datetime` with `timezone` tzinfo, 112 `date`, no dict keys/sets/init=False fields/pandas objects), `probe_release.py` (RA-01), `probe_acquire_orphan.py` (RA-04).

Test adequacy: `test_two_stale_reclaimers_cannot_delete_the_winner` uses a real barrier + event so both threads hold a stale verdict for S before the loser re-reads A under the mutex; on the parent code the loser would unlink A and acquire, so the test discriminates. `test_many_reclaimers_never_overlap` asserts max concurrent holders = 1. The namespace concurrency tests would fail on the parent for the class-only and divergent cases. The HB-FIX-12 test proves the structure-only witness still passes four no-head-move corruptions while the complete proof and every seam refuse with the typed reason; the seed tests prove same-instant → same id and byte-identical sidecar with no caller mutation.

The acquisition-vs-reclaim window is closed: a lock can only be CREATED by `O_EXCL` when the path is absent, and the only unlinkers are the mutex-holding reclaimer (byte-equal dead body), the token owner's `release()`, and `_discard_partial_lock` on a file the caller created microseconds earlier; a lock created after the reclaimer's unlink is observed afresh (`current != observed`, owner_decision_lock.py:474-479). Namespace: both files are written under one mutex, recovery is exact (class + explicit instance, deterministic bytes compared, records-exist refusal), no consumer reads authority from a half pair. No defect found in the namespace or chain lanes.

## Findings

| ID | Checklist item | Finding | Severity | Evidence | Proposed disposition | Test proving resolution |
|---|---|---|---|---|---|---|
| RA-01 | §4.1 "release() must raise a typed failure if it cannot read or verify its own lock" (HB-FIX-01 context) | A writer that HELD the lock and finds, at release, a body carrying ANOTHER writer's token returns silently (`_held=False`, no error) — the one state that means an overlapped writer exists. Absent-while-held and malformed-while-held raise `lock_release_failed`, and `verify_held()` on the identical state raises `lock_lost`; `__exit__` after a completed publication therefore reports success. Reproduced by `probe_release.py`. Exposure needs a non-conforming or manual actor (a conforming reclaimer cannot reclaim a live holder), so HB-FIX-01 itself holds; owner may elevate as a plan-MUST reading. | MAJOR | owner_decision_lock.py:593-595 vs :575-592 and :544-556 | When `was_held` and `body.lock_token != self.token`, raise `lock_release_failed` ("replaced by another writer; a publication may have overlapped") after leaving the foreign lock untouched. | Add `test_release_of_a_held_lock_replaced_by_another_writer_is_typed` in test_owner_decision_lock.py (acquire → overwrite body with a fresh live foreign token → `release()` raises `lock_release_failed`; foreign file untouched). |
| RA-02 | §4.1 "persistent I/O failure is a typed error, never silence" | `ExclusiveFileMutex._try_lock` swallows EVERY `OSError` and spins to the deadline, so a persistent non-contention failure on the mutex file (EBADF/EIO/ENOSPC; probe: `FileMutexTimeoutError`) is reported as contention: `_reclaim` maps it to `lock_held_by_live_holder` and init to `store_namespace_initialization_busy`. Fail-closed but mistyped, so the operator is told to wait for a holder that does not exist. | MINOR | file_mutex.py:54-62, :82-88; owner_decision_lock.py:490-491; store_namespace.py:573-578 | In `_try_lock`, treat only contention errnos (EACCES/EDEADLOCK on win32, EWOULDBLOCK/EAGAIN on POSIX) as "busy"; re-raise anything else as a typed mutex failure (`lock_read_failed`-class reason). | Add `test_mutex_persistent_io_error_is_not_reported_as_contention` (monkeypatch `msvcrt.locking`/`fcntl.flock` to raise EBADF; assert a non-contention typed reason). |
| RA-03 | §8.2 "every nested value … mappings" / HB-FIX-10 uncovered carriers | Not canonicalized: (a) `pandas.Timestamp` (a `datetime` subclass) stays a `Timestamp` (pickle bytes differ from stdlib and the seed sandbox unpickler admits stdlib only); (b) datetime dict KEYS; (c) `set`/`frozenset` elements; (d) pydantic `extra="allow"` fields; (e) `numpy.datetime64` passes through; (f) a dataclass `init=False` field is silently RESET to its default in the canonical copy (value dropped, hash computed on the dropped copy). Not reachable today: `probe_seed_types.py` shows the `IfvgDaySeed` graph is dataclasses with init fields, stdlib datetimes and `date` only, and the Parquet loader converts via `to_pydatetime()` (day_artifacts.py:439-480). | MINOR | child_replay.py:174-180 (subclass kept), :181-188 (extras), :189-196 (`if field.init`), :204-205 (keys), :206 (pass-through) | Canonicalize keys and set members; convert `pd.Timestamp` via `to_pydatetime()` before the UTC rebuild; refuse (typed) rather than drop `init=False` fields and any non-stdlib datetime-like carrier reaching the seam. Goldens unchanged (no such carrier exists in current seeds). | Extend `test_seed_canonicalization_covers_nested_containers_and_keeps_naive_datetimes` with a datetime-keyed dict, a set, a `pd.Timestamp`, and an `init=False` dataclass field. |
| RA-04 | §4.1 typed reads / "a lost ordinary lock token aborts before publication" | After a successful `O_EXCL` create, `acquire()` re-reads the body; a persistent read failure raises `lock_read_failed` with `_held=True` and OUR live lock left on disk; because `__enter__` raised, `__exit__`/`release()` never runs. The orphan is non-reclaimable while this process lives (`lock_held_by_live_holder` for every other writer — `probe_acquire_orphan.py`). Fail-closed, but a self-inflicted denial of the store for the life of the process. | MINOR | owner_decision_lock.py:497-500 | On `lock_read_failed` right after creation, run the token-safe `_discard_partial_lock()` (the file was created by this writer microseconds earlier; no reclaimer can reclaim a fresh file) before re-raising, or skip the re-read and return the body just written. | Add `test_read_failure_after_exclusive_creation_leaves_no_orphan_lock`. |

Namespace initialization (HB-FIX-02) and the supersession-chain proof (HB-FIX-12): no findings.

## Verdict

No BLOCKER. Counts: MAJOR 1 (RA-01), MEDIUM 0, MINOR 3 (RA-02, RA-03, RA-04). HB-FIX-01, -02, -10 and -12 hold as asserted on the code and tests I ran; RA-01 is the one plan-contract gap and is a one-line fix plus one test.

I did not open, list or construct any path under data/. (One allowed `git status --porcelain` call printed pre-existing untracked entry names under `data/ifvg_datasets/`; I did not open, glob or inspect them.)

## Carry-forward (out of scope)

1. `git status` shows untracked `data/ifvg_datasets/{context_pair_catalog_v1.json, context_views/, fsm_audit/, replay_chart/}`; the owner should reconcile these (by mtime) against the completion report's "data/ gained zero files" access claim — not inspected by me.
2. `synthetic_seed_production_authorization(..., persist=False)` returns an envelope whose witness is minted structure-only (seed_production.py:558); harmless because an unpersisted envelope cannot pass `verify_seed_production_authorization`, but the mint-then-prove asymmetry deserves a comment.
3. `verify_complete_owner_authority_chain` re-reads and re-hashes the namespace file twice per chain record (`load_owner_decision` → `require_owner_decision_namespace`) — performance only.
4. `_reclaim` records `self._reclaimed` before the creation retry (owner_decision_lock.py:488); a reclaimer that then loses the `O_EXCL` race still reports `reclaimed_from` when it later acquires — telemetry only.
5. `day_artifacts.py:474` keys a dict by datetime (DayArtifacts, not the seed); if such a map ever enters `IfvgDaySeed`, RA-03(b) becomes live.
