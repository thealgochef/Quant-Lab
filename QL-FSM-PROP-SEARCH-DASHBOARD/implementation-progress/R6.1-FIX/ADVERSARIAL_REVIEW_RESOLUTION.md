# R6.1-FIX — Adversarial Review Resolution

Every finding dispositioned; fixes verified by the re-run suites (final
counts in `TEST_RESULTS.md`). "FIXED" = code/test change in this release;
"ACCEPTED" = recorded without code change (with rationale). Reviewer A =
semantic identity / immutable evidence / point-in-time / access safety
(RA-01…RA-08); Reviewer B = pipeline integration / fail-closed evidence flow
/ typed failures / test adequacy (B-01…B-10). Overlaps: RA-08 ≡ B-08; RA-01 ∩
B-03 (the S14 ↔ service binding of the executed-trade table); B-10 ∩ the
pre-review docstring correction; B-05 ∩ DEV-R6.1-8.

Session note: the fix round was executed by three parallel workstreams with
disjoint file ownership (pipeline / evidence flow; stratification contracts;
assignment contracts) plus the main agent (the enum copy seam; the label
identity helpers), each writing its tests red first; the release-final full
suites ran once more over the combined tree (`_final_pytest*.txt`). Every
disposition below states what the code does NOW.

| # | Sev | Disposition | Resolution |
|---|---|---|---|
| B-01 | MAJOR | **FIXED** | Every costed evaluation derives from ONE canonical frame — the verified persisted projection: after `save_executed_trade_table` the fresh and the regime-reuse paths exact-load the table back (`_persist_and_load_executed_trade_table`) and the LOADED frame is the child's evidence; `_child_costed_evaluation` computes / publishes from `evidence.frame` only (fresh: compute + publish; reused: load the persisted evaluation, else compute + publish from the verified table), so `costed_evaluation_id → strategy_metrics.json` is provenance-independent. The metrics + lineage publication moved INSIDE per-child containment: a store refusal is a typed child failure (`FailureReason.INVARIANT`, "costed evaluation refused: …"), never a half-processed stage with `queued` siblings. S12/S13 keep consuming the raw `result.tables`. Tests: `test_costed_evaluations_are_provenance_independent_across_run_orders` (the reviewer's regime P1 → plain P2 → regime P2 sequence on one store: S02 completes, every child `reused`, every P1/P2 evaluation byte-equals a fresh computation from the persisted table); the parametrized `test_costed_evaluation_from_the_persisted_projection_equals_the_raw_evaluation` compares the SERIALIZED evaluation bytes (the int-typed fixture differs from its own projection bytes — the root of the finding; the v2-capture typing equals the projection). DECISIONS_TAKEN #114. |
| B-02 | MEDIUM | **FIXED** | `_verified_manifest`, `load_verified_envelope`, `load_sidecar_bytes` and `has_envelope` raise `store_entry_missing` ONLY when the entry directory does not exist; an existing directory without `manifest.json` raises the NEW registered reason `manifest_missing_for_existing_entry` (the vocabulary is now nine reasons); `probe_executed_trade_table` maps only `store_entry_missing` to "absent"; `has_envelope` therefore raises typed on a manifest-less existing entry (corrupt is never absent — the store no longer disagrees with itself between read and write). Tests: `test_a_manifest_less_entry_is_corrupt_never_absent` (six read paths), `test_probe_types_corrupt_entries_by_the_store_reason_never_absent`, pipeline `test_a_manifest_less_table_entry_is_a_typed_child_failure` (child `failed` with the reason; S14 `child_not_completed_or_reused`). DEV-R6.1-FIX-1 / -2 amended; #114. |
| B-03 | MEDIUM | **FIXED** | The S14 prior-reports recovery branch and `_prior_reports_record` are DELETED: with persisted tables an unavailable child that a prior record covered is store corruption, not lawful reuse; S14 always builds from the present children plus typed `children_skipped` (identical evidence re-mints identical report ids → store-level REUSED). `regime["children_evidence"]` is threaded to S15 and `s15_regime_reload_failures` now reloads `executed_trade_tables/<id>` (re-checking the recorded sha256) and `regime_stratified_reports/<id>` for every S14 record entry. `_delivered_by_s09c` stays and is tested on a tampered S09 record (`test_delivered_by_s09c_fails_closed_on_a_tampered_s09_record`, which also asserts the recovery branch is gone). DEV-R6.1-FIX-19; #114. |
| B-04 | MEDIUM | **FIXED** | `_reset_publication_block` runs on every halt / cancel whose not-run stages include S15 (`state="prepared_not_published"`, `gates=None`, `activated=False`, `pipeline_result_id=None`, `control_flow_gates_passed=False`, `reload_failures={}`); `_publication_gates(state, store_root)` is the ONE derivation used by `run_publication_gates` and re-run inside `activate_pipeline_result`, which refuses when the gates "do not pass on the latest attempt". Test: `test_a_halted_attempt_resets_the_publication_block_and_activation_re_derives_the_gates` (a real halt → the exact reset dict, all gates false, activation refused; a crafted all-true checklist under development scope → refused by re-derivation). DEV-R6.1-FIX-6 amended; #114. |
| B-05 | MEDIUM | **FIXED (hash) + ACCEPTED-recorded (replay policy)** | `_reuse_child_with_regime_tables` requires `source_core_table_hash` equality with the stored envelope as well as projection-byte equality (typed refusal). Replay policy — option (a): zero replay holds for non-stratified verified reuse; a run with stratified reporting re-derives every reused child exactly ONCE as a verified reproduction against the persisted executed-trade table (projection bytes AND raw core-table hash must reproduce), because S12/S13 consume raw tables the store does not hold. Both double-run tests now assert the counts: `== 0` for every reused child in `test_second_attempt_with_different_workers_shares_every_semantic_identity`, `== 1` in `test_second_attempt_reuses_every_regime_stage`. The §3.10 gate evidence reads accordingly (`GATE_SUMMARY.md`). DEV-R6.1-FIX-20; #114. |
| B-06 | MEDIUM | **FIXED** | The surviving mutations are now falsified: `test_a_drifted_runner_cannot_reproduce_the_persisted_table` (an inside-projection drift → child `failed` "byte-for-byte"; an outside-projection column → "source core-table hash"), `test_delivered_by_s09c_fails_closed_on_a_tampered_s09_record` (`sidecar_hash_mismatch`), `test_a_neutrality_report_disagreeing_with_the_table_hash_fails_the_child`, `test_s15_records_a_real_artifact_tamper_and_the_publication_gates_fail` (a REAL post-S14 tamper of the OOS-assignment sidecar and of one executed-trade table → both recorded by `<store>/<id>`, the gate false, `run_publication_gates` fails), the bytes comparison of B-01. |
| B-07 | MINOR | **FIXED** | `_record_schema_version_for(core_replay_id, envelope)` is the ONE helper of both S02 paths; a bare core id (no core replay envelope) for a child whose table must be persisted raises `PipelineWiringError`, re-raised through every containment (the stage fails typed — the identity derives from the core replay, never from the frame). The string-returning resolvers of `study_ui_fixture.py` / `test_orchestrator.py` feed `run_search` / `enumerate_children` only and never reach `_stage_s02_replays`; no scoping was needed. |
| B-08 / RA-08 | MINOR | **FIXED** | `load_verified_envelope` and `load_sidecar_bytes` raise `SidecarLoadError` with the typed reason at the point of detection (missing directory, manifest-less directory, malformed manifest / entry, manifest hash, identity, declared-but-absent artifact, artifact hash, envelope JSON / `ValidationError` → `malformed_sidecar`, `OSError` → `unexpected_io_error`); the legacy message substrings are kept (tests and the UI match on them). `probe_executed_trade_table` has no `except Exception` and no text heuristics — it classifies by exception type only. Test: `test_verified_envelope_load_raises_the_typed_reason_at_detection`. |
| B-09 | MINOR | **FIXED (variant)** | The reload-failure reasons are immutable as `PipelineResultPayload.reload_failure_reasons` (`ImmutableMap`, default empty) — part of the persisted pipeline result — rather than an S15 stage sidecar: two failing attempts with identical gate booleans but different reasons would share one S15 stage-result id with different sidecar bytes (publication refused; observed in the module run). Pipeline result ids re-mint (S15 output, synthetic only). DEV-R6.1-FIX-21; #114. |
| B-10 | MINOR | **FIXED** | The two "44 columns" docstrings were corrected to 42 before the reports landed (`_PROGRESS_CHECKPOINT.md` 20:50); the projection-equality test is parametrized over the int-typed fixture and a v2-capture-typed frame (float64 ticks, `date` trading day, tz-aware timestamps), comparing serialized bytes. |
| RA-01 | MEDIUM | **FIXED** | `build_regime_stratified_reports` runs `_verified_child_tables` before any gate or publication: for every child naming an `executed_trade_table_id` it exact-loads the artifact (`load_executed_trade_table`), refuses a table whose payload `core_replay_id` is not the child's, refuses a caller frame whose `executed_trade_table_bytes(project_executed_trades(child.trades))` differs from `loaded.table_bytes` ("does not reproduce … byte-for-byte"), and the LOADED frame is the only frame the child's classes consume (`cohort_descriptive`, the frontier inputs, `stratified_prop`'s `regime_for_trades`). `CohortDescriptiveBody.executed_trade_table_artifact_sha256` (the envelope's projection-bytes hash) is bound beside the normalized-frame hash (id and artifact hash together or neither). S14's caller inputs (`trades=loaded.frame` + id) reproduce byte-for-byte (the projection round trip is idempotent). Test: `test_service_refuses_a_caller_frame_that_does_not_reproduce_the_bound_table` (tables A / B). Cohort report ids re-mint. DEV-R6.1-FIX-14; #111. |
| RA-02 | MEDIUM | **FIXED** | `RegimeNetRAccounting._coherent` recomputes from `net_r_by_regime` with `math.isclose(rel_tol=1e-9, abs_tol=1e-9)`: `assigned_regime_count == len(net)`, `assigned_net_r_total == Σ net`, `abs_net_r_mass == Σ|net|`, the zero-mass / zero-total reasons exactly when the recomputed values are zero, the abs shares (keys + values) and the top share, the signed fractions (keys + values), the null / reason pairing, "null requires unassigned trades", the claim equal to the assigned-side predicate under the RA-03 rule, and `works_only_in_regime_id` naming the unique positive regime; `CohortDescriptiveBody._coherent` requires the regime strata to be exactly the accounting's regimes with `trade_count_by_regime[r] == stratum.executed_trades`. Test: `test_net_r_accounting_refuses_arithmetically_impossible_bodies` (the reviewer's body and each single-field corruption refused with its own message). DEV-R6.1-FIX-13; #111. |
| RA-03 | MEDIUM | **FIXED** | `regime_net_r_accounting`: `assigned_claim` = the plan §3.5 predicate on the assigned side; `works = assigned_claim` when no trade is unassigned, else `None` + `incomplete_assignment_accounting` ONLY when the assigned side supports the claim, else `False` with no reason. `assigned_regime_count` is persisted and the class docstring states the single-regime vacuous TRUE (the plan predicate is unchanged; the count makes the vacuity visible). Tests: `test_works_only_in_regime_is_false_when_the_assigned_side_refutes_it`; the lane tests re-derive the claim from the assigned side. DEV-R6.1-FIX-13; #111. |
| RA-04 | MEDIUM | **FIXED** | `_enum_field_shape` replaces `_enum_annotation`: a bare `Enum` / `Enum | None` is a scalar field (member required); a homogeneous `tuple` / `list` / `set` / `frozenset` of ONE enum class is a sequence field (every element a member; a string or non-iterable refused); a union of two enums, a mapping and every non-enum annotation pass through. Tests (`test_r61_fix_review_fixes.py`): shapes, lawful members / containers accepted, raw strings refused in scalar and sequence fields, and a structural walk over every registered identity payload's sequence-of-enum field (incl. `PipelineSemanticSpecPayload.stage_plan`) proving a lawful copy succeeds and a tuple of raw strings is refused. DEV-R6.1-FIX-18; #113. |
| RA-05 | MINOR | **FIXED** | `run_supervised_ladder`, `run_catboost_bundle_model` and `run_logistic_fold_models` default `label_artifact_id` to `label_artifact_content_id(None, labeled)` (the FULL consumed-column hash; the narrow pair hash is never a rung's label identity); `SupervisedLadderRun.label_identity_source` is `label_artifact` when the caller passed an exact id and `content_hash_unpersisted` otherwise (a caller may declare the helper form explicitly; any other value is refused); `ControlledFeatureStudyPayload.label_identity_source` is REQUIRED. In-pipeline every ladder receives S07's exact id (no persisted pipeline identity moves); only the bundle-path ladder ids of helper runs re-mint; two R6.1 CatBoost-bundle expectations pinned to the narrow default were updated. Tests: `test_helper_ladder_runs_bind_the_full_consumed_column_hash_and_are_stamped`, `test_bundle_ladder_feature_source_carries_the_full_column_label_hash`, `test_controlled_study_payload_requires_the_label_identity_source`. DEV-R6.1-FIX-17; #113. |
| RA-06 | MINOR | **FIXED** | `validate_assignment_rows` requires the linkage key on EVERY row (`row_id` for the `fit` / `model_facing` kinds; `candidate_id` for the `descriptive` kind, falling back to `row_id` for a fit-schema frame under the descriptive rule), a lawful non-null `partition`, a 64-hex non-null `regime_fit_id` and a non-negative non-null `fold_index` on invalid rows (the all-null `_typed()` shape stays lawful), and on valid rows `assigned_distance == distances[fold_local_cluster_id] == min(distances)` with `assignment_margin == second_smallest − smallest ≥ 0` (`_ASSIGNMENT_TOLERANCE = 1e-9`) — distinct messages each; `load_regime_fit_assignments` requires the sidecar's `resolved_regime_protocol_id` column to equal the envelope's. Tests: `test_invalid_rows_retain_linkage_and_valid_rows_are_self_consistent`, `test_loader_refuses_a_sidecar_of_another_protocol`; `test_reuse_refuses_a_fit_whose_stored_assignments_differ_in_values` now perturbs a diagnostic column to reach the byte-for-byte refusal and asserts an inconsistent margin is refused earlier by the invariants; `test_regime_store.py` accepts either refusal message for a forged local id (the arithmetic check now fires before the estimator-reproduction check; fail-closed either way). DEV-R6.1-FIX-15; #112. |
| RA-07 | MINOR | **FIXED** | `candidate_as_of_source_hash` routes through `_as_of_ns`: an unparseable NON-null anchor is a hard `ValueError` on every grain, a null anchor hashes as `None` deterministically; `RegimeOosAssignmentPayload.candidate_as_of_stage: AvailabilityStage` is REQUIRED on both grains (equal to `panel_context.candidate_as_of_stage` on the panel grain — validator-enforced; on the candidate grain it records which anchor column the hashed instants came from, so the same rows under a different stage mint a different id); `build_regime_oos_assignment_artifact(..., candidate_as_of_stage=)` is a required keyword and the executor passes it. Tests: `test_candidate_as_of_hash_refuses_unparseable_anchors_and_records_the_stage`, `test_executor_refuses_an_unparseable_anchor_and_types_a_null_one`. Every OOS-assignment id re-mints (already re-minted this release). DEV-R6.1-FIX-16; #112. |
| — | — | midpoint failure | `test_regime_service.py::test_panel_pit_assignment_is_last_completed_bar_never_future_and_oos_only` built a panel-assignment frame without the F-05-required value columns; the frame now carries the complete value set per valid row (distance vector with the minimum at the local id, `assigned_distance == min`, `assignment_margin == second − first`, `missing_reason=None`); assertions unchanged. |

## Post-fix verification

Lane suites after the round (mixed worktree, before the commit): pipeline /
evidence flow — `test_pipeline_evidence_integrity.py` **18 passed**,
`test_pipeline_regime.py` **19 passed** (4:56), `test_pipeline_run.py` 19,
`test_store_sidecar_probe.py` 4, `test_executed_trade_table.py` 5, plus a
regression sweep (identities / regime_store / fold_schedules / child_replay /
mbp1_coverage_evidence / orchestrator / pipeline_job_script **83 passed**;
pipeline tab + owner_decisions + propsim **203 passed**); stratification —
`test_regime_stratification_evidence.py` **7 passed**,
`test_regime_stratification.py` 15; assignment contracts —
`test_regime_assignment_evidence.py` (12) + `test_regime_oos_assignment.py`
+ `test_regime_service.py` + `test_regime_store.py` +
`test_regime_fold_feature_evidence.py` + `test_regime_fold_features.py` +
goldens **66 passed**; the main agent's lane — `test_r61_fix_review_fixes.py`
(7) + `test_label_identity.py` + `test_supervised_ladder.py` +
`test_controlled_feature_study.py` + `test_logistic_model.py` +
`test_catboost_bundle_model.py` + `test_identities.py` +
`test_mbp1_materializer.py` + goldens **99 passed**. `ruff check src tests
scripts` clean; `git diff --check` clean over the tracked and the
intent-to-add new files (`_ruff_and_diffcheck.txt`). Release-final full-suite
counts over the COMBINED tree (as-is and with the provider keys cleared):
`TEST_RESULTS.md`.
