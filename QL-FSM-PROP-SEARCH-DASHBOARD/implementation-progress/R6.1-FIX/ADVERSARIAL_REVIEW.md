# R6.1-FIX — Adversarial Review (two independent read-only reviewers)

Per the kickoff rules and plan §3.10 ("one focused two-reviewer adversarial pass"): two independent reviewers attempted to falsify the uncommitted R6.1-FIX change set (working tree over HEAD `6c0b60a`, R6.1) against `../R6.1-FIX-HARDENING-READINESS-PLAN/IMPLEMENTATION_PLAN.md` revision 3 (Phase 1, §3.1–§3.10, §9 Phase-1 rows), the kickoff rules, and `DEVIATIONS.md`. Reviewer A took the semantic-identity / immutable-evidence / point-in-time / access-safety lens (findings RA-01…RA-08); Reviewer B took the pipeline-integration / fail-closed evidence-flow / typed-failure / test-adequacy lens (findings B-01…B-10). Both produced findings only (no edits); every probe ran on synthetic fixtures under scratch / tmp roots; no real source path was constructed; the protected day and the sealed range were never touched. The midpoint full suite that ran concurrently with the reviews is `_midpoint_pytest.txt` (1967 passed, 1 failed — the `test_regime_service.py` panel-PIT fixture lacking the F-05-required value columns; 412 warnings). Both reports are reproduced VERBATIM below; dispositions and post-fix verification: `ADVERSARIAL_REVIEW_RESOLUTION.md`.

Combined verdict: **0 blockers; 1 major (B-01) + 9 mediums (RA-01…RA-04, B-02…B-06) + 8 minors (RA-05…RA-08, B-07…B-10)**. Overlaps: RA-08 ≡ B-08 (typed reasons inferred from exception text in the executed-trade probe); RA-01 ∩ B-03 (the S14 ↔ stratification-service binding of the executed-trade table); B-10 ∩ the pre-review docstring correction (the "44 columns" text was already fixed to 42 before the reports landed; the test half of B-10 remains); B-05 ∩ DEV-R6.1-8 (the re-derivation of reused children for stratified reports).

---

# R6.1-FIX adversarial review — Reviewer A (semantic identity, immutable evidence, point-in-time, access safety)

Scope: worktree vs HEAD `6c0b60a` (uncommitted R6.1-FIX). Read-only. Method: full read of the changed `ml/` and `search/` modules and the `git diff HEAD -- src scripts`, static `python` probes against the worktree contracts (no store, no data), and one targeted run of the eight NEW synthetic test files (`26 passed in 129s`; nothing under `data/` was constructed, listed or read; no full suite, no replay, no fit outside the tmp-rooted fixtures).

Verdict: no blocker. Four medium findings — one unbound-source seam in S14 (an executed-trade table id is bound by assertion), two §3.5 accounting defects (a validator that accepts an impossible body; a null semantics that swallows `false`), and a latent mis-typing in the new enum guard. Four minor findings. Everything else I tried to break held (see "Affirmed").

---

## RA-01 · medium · S14 binds `executed_trade_table_id` by assertion; the frame it stratifies is never tied to that artifact

- Contract: plan §3.7 ("S14 … binds the table artifact ID/hash in its own identity"; verified bytes reuse, different bytes fail closed); §3.1 last bullet (refuse a caller-supplied frame/hash that differs from the verified artifact); kickoff §10 "identity projection".
- File:symbol: `src/alpha_lab/agents/data_infra/ifvg/ml/regime_stratification_service.py:ChildStratificationInputs.executed_trade_table_id` (98–109), `build_regime_stratified_reports` (263; 342 `trades=child.trades`, 352, 358–359, 419–420), `_publish` (304–314); `ml/regime_stratified_strategy.py:build_cohort_descriptive_body` (225; body 350–351); `ml/regime_stratified_contracts.py:CohortDescriptiveBody` (394–447).
- What is wrong: the service does not import `load_executed_trade_table` / `project_executed_trades`. `child.executed_trade_table_id` is copied verbatim into the body and into `source_metric_refs` (`tuple(sorted(set(refs)))` — no load, no hash check). The body binds `executed_trade_table_sha256(ordered)` — the hash of the *caller's* normalized frame — next to an artifact id that nothing relates to it, and the artifact's own `executed_trade_table_sha256` (projection bytes) is not bound at all. The pairing is correct only because the pipeline caller (`regime_report_stage._child_table_evidence`) happens to take both from one verified load; the invariant lives in the caller, not in the seam that mints the immutable report.
- Reproduction: with the lane fixture of `tests/agents/data_infra/ifvg/test_regime_stratification_evidence.py`, persist two executed-trade tables A and B (`save_executed_trade_table`), then call `build_regime_stratified_reports` with `ChildStratificationInputs(trades=<B's frame>, executed_trade_table_id=<A's id>, …)`. A `cohort_descriptive` report persists whose body names table A and hashes B's rows; no refusal.
- Minimal resolution: in `build_regime_stratified_reports`, when `child.executed_trade_table_id is not None`, exact-load it from `inputs.root` and require `executed_trade_table_bytes(project_executed_trades(child.trades)) == loaded.table_bytes` (or source `trades` from the loaded artifact and drop the caller frame); bind the artifact's `executed_trade_table_sha256` in `CohortDescriptiveBody` beside the normalized-frame hash (re-mints report ids, which §3.1 permits). Post-fix: the reproduction refuses; the S14 pipeline test still produces identical reports across a double run.

## RA-02 · medium · `RegimeNetRAccounting` accepts an arithmetically impossible body

- Contract: plan §3.5 (definitions persisted "verbatim"); kickoff §10 "immutable reuse" (persisted evidence must be self-consistent, not merely well-typed).
- File:symbol: `ml/regime_stratified_contracts.py:RegimeNetRAccounting._coherent` (328–391); `CohortDescriptiveBody._coherent` (419–447).
- What is wrong: the validator checks key-set equality, the count sum, and the null/reason pairings only. It never recomputes `abs_net_r_mass`, `assigned_net_r_total`, the shares, the top share, the signed fractions, or the `works_only_in_regime` predicate from `net_r_by_regime`, and never requires `works_only_in_regime_id ∈ net_r_by_regime`. `CohortDescriptiveBody` mirrors two scalars and two totals but never checks `trade_count_by_regime[r] == strata[regime r].executed_trades`.
- Reproduction (ran against the worktree; accepted without error):
  `RegimeNetRAccounting(trade_count_by_regime={0:3,1:2}, net_r_by_regime={0:5.0,1:3.0}, assigned_trade_count=5, unassigned_trade_count=0, unassigned_net_r=0.0, assigned_net_r_total=-99.0, abs_net_r_mass=0.0, abs_net_r_share_by_regime=None, signed_contribution_fraction_by_regime={0:0.1,1:0.9}, top_regime_abs_net_r_share=None, works_only_in_regime=True, works_only_in_regime_id=7, works_only_in_regime_reason=None, zero_denominator_reasons=("zero_abs_net_r_mass",))`.
- Minimal resolution: recompute inside `_coherent` (tolerance 1e-12): mass = Σ|net|, total = Σ net, shares = |net|/mass with identical keys, top = max(shares), signed = net/total, `works_only_in_regime_id` must be the unique positive regime, `works is None ⇒ unassigned_trade_count > 0`; in `CohortDescriptiveBody`, per-regime counts must equal the regime strata's `executed_trades`. Post-fix: the body above is refused; `regime_net_r_accounting` output still validates (`test_thin_regimes_count_in_concentration_but_not_in_reportability`).

## RA-03 · medium · `works_only_in_regime` is null on any unassigned trade, and true for a single assigned regime

- Contract: plan §3.5 — "true only when [assigned total > 0, exactly one regime > 0, every other ≤ 0, unassigned == 0]; **otherwise false, or null … when unassigned trades prevent the claim**".
- File:symbol: `ml/regime_stratified_strategy.py:regime_net_r_accounting` (149–222; 197–205).
- What is wrong: `if unassigned_count > 0: works = None` is evaluated before the assigned-side predicate. When the claim is already false on the assigned side, unassigned trades did not "prevent" it, yet the persisted flag is null. Probe results: two positive regimes + one unassigned trade → `None / incomplete_assignment_accounting`; assigned total negative + one unassigned → `None`. Real populations essentially always contain unassigned trades (`panel_warmup`, `no_oos_assignment`, `candidate_as_of_missing`), so the persisted flag will be null in practice and a report can never state `false`. Separately, a single assigned regime with positive net R yields `True, id=0` (probe) — a vacuous concentration claim (the R6.1 code required ≥ 2 regimes; §3.5's wording admits the vacuous case but the class docstring does not warn of it).
- Reproduction: `regime_net_r_accounting(net_r=pd.Series([1.0,2.0,0.5]), clusters=np.array([0,1,-1]), valid=np.array([True,True,False]))` → `works_only_in_regime is None`.
- Minimal resolution: `assigned_claim = total > 0 and len(positive) == 1`; `works = assigned_claim if unassigned == 0 else (None if assigned_claim else False)`; set the reason only when null; persist `assigned_regime_count` (or require ≥ 2) so a vacuous single-regime truth is visible. Post-fix: extend `test_works_only_in_regime_claim_requires_complete_assignment_accounting` with the two-positive + unassigned case expecting `False`.

## RA-04 · medium · The F-10D enum guard mis-types container fields (latent false refusal on six identity-bearing fields)

- Contract: plan §3.9 F-10D ("pass enum members into enum-typed payloads").
- File:symbol: `search/identities.py:_enum_annotation` (243–251), `FrozenContract.model_copy` (265–278).
- What is wrong: `_enum_annotation` walks `get_args()` of *any* generic, so `tuple[Enum, ...]` resolves to the Enum and the guard then demands `isinstance(<tuple>, Enum)`. Probe over all 284 `FrozenContract` subclasses importable from `ifvg` + `propsim`: six fields affected — `PipelineSemanticSpecPayload.stage_plan`, `TradePathArtifactPayload.capabilities`, `TradePathBundlePayload.bundle_capabilities`, `PropRulePathRequirement.required_path_capabilities`, `PropRulePathRequirement.accepted_fidelity_classes`, `PathCapabilityReport.available_capabilities`. A lawful `model_copy(update={"stage_plan": (QuantLabPipelineStage.…,)})` raises `TypeError`; a tuple of raw strings is refused only as a side effect (no element check), and `Mapping[str, Enum]` / nested generics would be mishandled the same way. No current `src/`, `tests/`, or `scripts/` call site updates these six fields (grep), so the defect is latent, not breaking. `Enum | None` is handled; no union of two enums exists; `GeneratedProfileCapability.status` is a `Literal`, so `tests/agents/ifvg_search/test_orchestrator.py:531`'s raw-string update is unaffected.
- Reproduction: `spec.model_copy(update={"stage_plan": spec.stage_plan})` on any `PipelineSemanticSpecPayload` → `TypeError: … is enum-typed (QuantLabPipelineStage)`.
- Minimal resolution: use `typing.get_origin`: treat only bare `Enum` and `Enum | None` as scalar-enum fields; for `tuple[Enum, ...]` validate each element; otherwise pass through. Post-fix: a test that a tuple-of-members update succeeds and a tuple-of-strings update is refused.

## RA-05 · minor · The narrow `label_content_hash` is still the default label identity of every helper path except `run_controlled_mbp1_study`

- Contract: plan §3.6 ("Unpersisted helper runs may retain a **full consumed-column** content hash, but must stamp `label_identity_source="content_hash_unpersisted"`").
- File:symbol: `ml/supervised_ladder.py:441` (`label_id = label_artifact_id or label_content_hash(...)`), 587/611 (the ladder id binds it); `ml/catboost_bundle_model.py:311`; `ml/logistic_model.py:322`; `ml/comparison_rows.py:167 label_content_hash` (candidate/target pairs only) feeding `comparison_row_id` (D13); `ml/controlled_feature_study.py:140` (`label_identity_source` defaults to `"label_artifact"`).
- What is wrong: only the controlled-study entry point was migrated. A direct `run_supervised_ladder` / `run_catboost_bundle_model` / logistic call still mints `ladder_id`, `BundleModelPayload.label_artifact_id` and the comparison-row ids from the pair hash with no stamp. In-pipeline every one of these receives S07's exact id (verified by reading `regime_supervised_stage.run_supervised_substeps` and `pipeline.py` 1759/1816), so no persisted artifact currently carries the narrow hash — but `ControlledFeatureStudyPayload.baseline_ladder_id/challenger_ladder_id` (121–122) embed ladder ids, and `label_identity_source` is a self-declared default that a hand-built payload satisfies with any 64-hex string.
- Reproduction: `run_supervised_ladder(view, labeled, folds, …)` without `label_artifact_id` → `ladder_id` derived from `label_content_hash` (pairs), no `content_hash_unpersisted` marker anywhere on the result.
- Minimal resolution: default the helper paths to `label_artifact_content_id(None, …)` and carry `label_identity_source` on the ladder result; make `label_identity_source` required (no default) on `ControlledFeatureStudyPayload`. Post-fix: a ladder run without the exact id cannot feed `save_controlled_feature_study`.

## RA-06 · minor · §3.4 invariants not fully validated (invalid-row linkage, protocol column on load, assignment self-consistency)

- Contract: plan §3.4 ("For an invalid row, retain … row ID, candidate/panel ID, fold, partition, protocol and applicable fit reference"; "Validate on build, save, verified load, and downstream consumption").
- File:symbol: `ml/regime_contracts.py:validate_assignment_rows` (528–660), `_ASSIGNMENT_ROW_INVARIANT_COLUMNS` (421 — no `row_id`/`candidate_id`); `ml/regime_store.py:load_regime_fit_assignments` (487–524).
- What is wrong: (a) invalid rows are checked only for null outputs + a registered reason — `row_id`/`candidate_id` may be null and `partition` may be `None`/`"x"` on a fit-kind or model-facing row; (b) the loader re-checks `regime_fit_id` and `fold_index` against the envelope but not `resolved_regime_protocol_id` (persist-time `_bound_assignments` only); (c) no internal consistency of a valid row: `assigned_distance == distances[local] == min(distances)`, `assignment_margin == d2 − d1 ≥ 0`. Bytes are manifest-hashed, so this is defense-in-depth rather than an exploit; it is nevertheless the validator §3.4 names.
- Reproduction: fit-kind frame with a valid row `distances=[1,2,3], fold_local_cluster_id=2, assigned_distance=99.0, assignment_margin=-5.0` and an invalid row `partition="x"` → `validate_assignment_rows(kind="fit", cluster_count=3)` passes.
- Minimal resolution: add the three checks (1e-9 tolerance on the distance equalities) and the protocol-column check in the loader.

## RA-07 · minor · Candidate as-of identity conflates "unparseable" with "null" and does not record the stage on the candidate grain

- Contract: plan §3.3 ("an unparseable non-null timestamp remains a hard error"; "the source hash represents null deterministically").
- File:symbol: `ml/regime_oos_assignment.py:candidate_as_of_source_hash` (611–617, `errors="coerce"`), `_as_of_ns` (260–274); `ml/regime_executor.py:execute_regime_protocol` (229–234).
- What is wrong: on the candidate grain the as-of frame is only hashed — `_as_of_ns` (the hard-error path) never runs — so a garbage non-null anchor is silently represented as `None` in `candidate_as_of_source_hash`, indistinguishable from a true null. On the panel grain the same coerce runs after the rule already raised, so it is latent there. Additionally, the candidate grain hashes the anchors of `candidate_as_of_stage` but the payload records no stage (`panel_context=None`): the same output bytes get a different id per stage and the provenance of the hashed instants is unreadable from the artifact.
- Reproduction: candidate-grain observations with `entry_ts_utc="not-a-time"` for one candidate → `execute_regime_protocol` succeeds; the payload hash equals the one produced with `entry_ts_utc=None`.
- Minimal resolution: hash through `_as_of_ns` (raise on unparseable non-null); record `candidate_as_of_stage` for the candidate grain or hash only candidate ids there.

## RA-08 · minor · Typed sidecar reasons inferred from exception text

- Contract: plan §3.8 (typed sidecar probe/load contract; "sanitized typed failures").
- File:symbol: `search/executed_trade_table.py:probe_executed_trade_table` (302–319).
- What is wrong: `except Exception as error: message = str(error); reason = "envelope_identity_mismatch" if "does not match directory" in message … else "manifest_hash_mismatch"`. The reason S02 records in `row["explanation"]` and S15 reports is a substring heuristic: a pydantic message containing "failed verification" is typed as a hash mismatch, and every unmatched exception — including `OSError` — becomes `manifest_hash_mismatch` rather than `unexpected_io_error`. Fail-closed in all branches, so evidence integrity holds; reason fidelity does not.
- Reproduction: corrupt `envelope.json` of a persisted table so that `load_verified_envelope` raises a `ValidationError` whose text contains "failed verification" → reported as `sidecar_hash_mismatch`.
- Minimal resolution: raise `SidecarLoadError` with the reason at the point of detection inside `load_verified_envelope` (or classify by exception type: `SearchStoreError` subclasses / `ValidationError` / `OSError`).

---

## Affirmed (actively tried to break; held)

- Executor derivation: after `persist_regime_fit`, every consulted value comes from `verified_fit_assignments_for_run` (exact loads, id and fold re-checked); `run.assignments` reaches only the persist input; `RegimeExecutionResult.oos_assignment_frame` has no consumer in `src/`; S10's `decided_at` reads `result.consulted_assignments`; `panel_event_assigner` re-verifies every `FitAssignmentRef` (`verified.ref != ref` → refuse).
- Descriptive identity: refs sorted, unique, projection-checked; `consulted_assignments_hash` covers the eleven consulted values order-invariantly; the panel frame is pinned to the protocol (`assert_source_matches_protocol`: artifact id, interval, as-of policy) and the fold set to the loaded source; `candidate_as_of_source_ref` comes from the loaded envelope; `assignment_schema_hash`, row count and table hash are re-checked on every load, then `validate_assignment_rows(kind="descriptive")`.
- Fit sidecar: serialized only under `FIT_ASSIGNMENT_SCHEMA` (metadata stripped); decode refuses any `name:type` deviation; `Int64`/`None`, NaN distances, negative or out-of-range local ids, missing canonical ids and non-finite margins are refused for valid rows; `observation_ts_utc` is already an ISO string in the run frame; alignment is a full permutation per fold, so every valid row of a fitted fold carries a canonical id (no false persist refusal); reuse is byte-for-byte after envelope equality and bundle reproduction.
- Fold features: `_verified_fit_frames` refuses any non-`VerifiedFitAssignments`, requires evidence for every run fit with matching id/fold/protocol; fold k consumes fit k's own (already fold-filtered) frame; `verify_fold_fit_refs_against_store` runs in `load_regime_fold_features`, `load_regime_fold_feature_source` and S15; `FoldFitRef` is all-or-nothing; the ladder seam re-validates rows.
- F-04: `candidate_as_of_missing` is registered in the panel and fold-feature vocabularies, preserved as a typed row in both artifacts (fold-feature rows keep fold/partition/fit id), and flows through `regime_for_trades` into `unassigned_reasons`.
- F-10B: `normalized_executed_trades` is the only frame used for the join, every stratum, every `compute_strategy_metrics` call and the binding hash; `per_trade_net_r` is the same `(realized − cost)/risk` vector `compute_strategy_metrics` averages.
- F-10C: full `scope` equality in `Mbp1PartitionEvidence._scope_agrees` and again in `compute_partition_coverage`; a positive claim requires non-null, duplicate-free content refs whose set equals `verified_partition_refs`; the compilation report must match the manifest's id, scope, provenance and intervals. (Set rather than ordered-tuple equality is the only slack; it does not admit a wrong ref.)
- F-10D: `Enum | None` is guarded; no union of two enums exists; every `model_copy(update=…)` site in `src/` passes members or non-enum fields.
- Access safety: the diff introduces no `data/`, store-root, dataset or date constants; `executed_trade_table.py` takes only `root` parameters; Strategy-Core worktree is clean; Trade-Lab's dirty files date 2026-07-31 (pre-existing, not touched by this release — worth stating explicitly in the evidence packet); no M0–M3 lane module is in the change set; `regime_service.py` and `RegimeFitPayload` are unchanged in the worktree, so the R6 golden fit id cannot move; `test_r61_fix_goldens.py` passes.

---

# R6.1-FIX adversarial review — Reviewer B (pipeline integration, fail-closed evidence flow, typed failures, test adequacy)

Scope: worktree vs HEAD `6c0b60a`; plan `R6.1-FIX-HARDENING-READINESS-PLAN/IMPLEMENTATION_PLAN.md` §3.7–§3.10, §9 Phase-1 rows; kickoff §9–§10. Read-only. Synthetic probes only (tmp roots; no real data, no full suite). Targeted runs executed: `test_executed_trade_table`, `test_store_sidecar_probe`, `test_r61_fix_goldens`, `test_label_identity`, `test_mbp1_scope_equality` (10 passed); `test_pipeline_evidence_integrity` (11 passed); `test_pipeline_regime::test_second_attempt_reuses_every_regime_stage`, `test_pipeline_regime::test_s02_reused_children_are_adopted_only_by_verified_reproduction`, `test_pipeline_run::test_second_attempt_with_different_workers_shares_every_semantic_identity` (3 passed); `git diff --check` clean; no production `assert` under `src/alpha_lab/agents/data_infra/ifvg` or `src/alpha_lab/propsim`.

Probe scripts (scratchpad, not part of the tree): `probe_provenance_drift.py` (three synthetic pipeline runs on one tmp store), plus two inline probes (raw-vs-table metrics bytes; manifest-less store entry).

---

## Findings (ordered by severity)

### B-01 — MAJOR — The costed evaluation of one `costed_evaluation_id` is published from two different frames (raw child tables vs the persisted projection) whose `strategy_metrics.json` bytes differ; a lawful later run fails S02 as a whole stage

- **Contract:** plan §3.7 ("`EXECUTED_TRADE_TABLE_SCHEMA_V1` is the exact ordered executed-trade projection consumed by costed evaluation and S14"; identical bytes reuse / different bytes fail closed), §3.10 (double-run reuse, identical ids), F-06; kickoff §10 "identity projection / immutable reuse".
- **Where:** `src/alpha_lab/agents/data_infra/ifvg/search/pipeline.py::_stage_s02_replays` (evaluation step after the per-child branch: `if result is not None: metrics = compute_strategy_metrics(result.tables, …)` vs `if metrics is None and evidence is not None: metrics = compute_strategy_metrics({EXECUTED_TRADE: evidence.frame}, …)`), `_reuse_child_with_regime_tables`; `search/executed_trade_table.py::project_executed_trades` (int→float64 ticks, date32 trading day); `search/orchestrator.py::_publish_child_evaluation` (sidecar bytes = `json.dumps(metrics.model_dump(mode="json"))`).
- **What is wrong:** S02 computes the costed evaluation from the RAW child tables for fresh children and for regime-reused children (re-derived `result.tables`), but from the Arrow-round-tripped projection (`evidence.frame`) for a reused child without a persisted evaluation. `trade_stats.trade_list[*].entry_ticks/stop_ticks/risk_ticks` serialize as `80000` from the raw frame and `80000.0` from the projection, so the same `costed_evaluation_id` (core id + cost policy only) gets byte-different `strategy_metrics.json` depending on provenance order. `save_or_reuse_envelope` then refuses with "exists with DIFFERENT sidecar content" — and `_publish_child_evaluation` sits OUTSIDE the per-child containment, so the whole S02 stage fails and the remaining children are left non-terminal (`queued`). The plan's statement that costed evaluation consumes the exact projection is not structurally true: only the reused-without-evaluation branch does.
- **Reproduction (synthetic, executed):** `probe_provenance_drift.py` — run 1: `build_pipeline_fixture(regime_study="candidate")`, cost P1 → 4 children `completed`; run 2: plain fixture (no regime), same store, cost 1.25 → 4 children `reused` (0 replays), `E(P2)` published from the table (`strategy_metrics.json` contains `80000.0`); run 3: regime fixture, same store, cost 1.25 → S02 `failed`: "store entry costed_evaluations/883b3e9b… exists with DIFFERENT sidecar content for 'strategy_metrics.json'"; child rows: one `reused`, three `queued`. Unit probe: `compute_strategy_metrics` raw vs `load_executed_trade_table(...).frame` on `make_resolved_trades_frame` — `model_dump` dicts equal, serialized bytes differ (72 diff lines, all int/float).
- **Minimal resolution:** derive every costed evaluation from ONE canonical frame — the verified persisted projection (after `save_executed_trade_table`, evaluate `load_executed_trade_table(...).frame` in the fresh and regime-reuse paths too), so `costed_evaluation_id → strategy_metrics.json` is provenance-independent; move `_publish_child_evaluation` inside per-child containment (typed REPLAY/INVARIANT child failure, never a half-processed stage); make `test_costed_evaluation_from_the_persisted_projection_equals_the_raw_evaluation` compare the serialized sidecar bytes, not `model_dump` dicts. (Alternative: declare the projection with the raw capture dtypes and refuse int-typed tick frames in `project_executed_trades`; the fixture would then have to be re-typed.)

### B-02 — MEDIUM — `probe_executed_trade_table` reports a corrupt store entry (manifest removed, envelope + sidecar present) as lawful "absent"

- **Contract:** plan §3.8 ("`has_sidecar` returns false only when a verified manifest proves that the sidecar was not produced"; `store_entry_missing` is a typed failure reason), §3.7 ("missing/corrupt evidence creates a typed child-stage failure … never silent omission").
- **Where:** `search/store.py::_verified_manifest` (`if not manifest_path.exists(): raise SidecarLoadError("store_entry_missing", …)` — no distinction between a non-existent directory and an existing directory without a manifest); `search/executed_trade_table.py::probe_executed_trade_table` (`if error.reason == "store_entry_missing": return "absent"`); `pipeline.py::_stage_s02_replays` reuse branch (`table_state == "absent"` → no evidence → `executed_trade_table_unavailable`).
- **What is wrong:** an entry whose `manifest.json` was removed (envelope.json + `executed_trade.arrow` still there) is classified `store_entry_missing` and mapped to "absent". In the non-regime reuse path (and in the regime path when no evaluation exists for the cost policy) the child is recorded `executed_trade_table_unavailable` — a lawful typed absence for a corrupt entry. In the regime path with a persisted evaluation, the re-persist hits `save_envelope_immutable`'s `destination.exists()` → `FileExistsError` "immutable search-store entry already exists" — a per-child REPLAY failure with a misleading reason. `has_envelope` shares the blind spot, so the store is inconsistent with itself (read: missing; write: exists).
- **Reproduction (executed):** save a table; `unlink(manifest.json)`; `has_envelope → False`; `probe_executed_trade_table → "absent"`; `probe_sidecar → SidecarLoadError(store_entry_missing)`; `save_executed_trade_table` → `FileExistsError`. Pipeline shape: after a completed run, delete the manifest of one child's table entry, run a different cost policy → row explanation `executed_trade_table_unavailable`, S14 `children_skipped[child] == "executed_trade_table_unavailable"`.
- **Minimal resolution:** in `_verified_manifest`, raise `store_entry_missing` only when `destination` does not exist; when it exists without a manifest raise a typed corruption reason (`malformed_manifest` or a new registered `manifest_missing_for_existing_entry`); `probe_executed_trade_table` maps only the former to "absent". Add the manifest-removed case to `test_store_sidecar_probe` and `test_pipeline_evidence_integrity`.

### B-03 — MEDIUM — The prior-reports recovery branch of S14 returns the prior attempt's record verbatim: failed children of THIS attempt are re-reported, `children_skipped` of this attempt is discarded, and the record names a table the store no longer holds; S14 then reads REUSED and S15 does not reload tables or reports

- **Contract:** plan §3.7 ("S14 iterates the charter child set … typed child-stage failure or `children_skipped` record — never silent omission"; "S14 … binds the table artifact ID/hash in its own identity"), §3.8 (fail closed), §3.10 (reuse evidence).
- **Where:** `src/alpha_lab/agents/data_infra/ifvg/ml/regime_report_stage.py::build_reports` lines 257–274 (`if unavailable: prior = _prior_reports_record(context); if prior is not None and unavailable <= set(prior["children"]) and decision ids equal: return (prior["report_ids"], prior, …)`); `search/pipeline_regime.py::s15_regime_reload_failures` (checks protocols/assessments/oos assignments/fold sets/promotions/panel|view/fold features — not `executed_trade_tables`, not `regime_stratified_reports`).
- **What is wrong:** the recovery condition only checks that the UNAVAILABLE children were reported before and that the decision id matches. It does not check (a) that no child is `failed`/`blocked` in this attempt — a child that failed at S02 with a typed corrupt-table failure (`child_not_completed_or_reused` in `skipped`) is re-reported through the prior report ids and its typed skip vanishes; (b) that the present children's `children_evidence` (table id/sha256/row_count) equals the prior record's; (c) that the unavailable child's table still exists — the prior record's `children_evidence` asserts an id/sha256 the store cannot reload. Because the returned sidecar bytes are the prior bytes, the S14 stage result reproduces byte-identically and is marked REUSED, and S15's reload gate never touches `executed_trade_tables` or `regime_stratified_reports`, so `artifacts_published_and_reloaded` passes. Reachability is narrow (same semantic id; the child's table entry AND its costed evaluation under this cost policy both gone — a partially restored/deleted store), but that is exactly the corruption case §3.8 is about, and the outcome is silent.
- **Reproduction (test shape):** `completed_candidate`; copy the state root; move `executed_trade_tables/<id(X)>` and `costed_evaluations/<E(X,P)>` away; tamper the executed-trade sidecar of child Y; retry with `operational_retry_reason` → S02: X `reused` (unavailable), Y `failed` (typed); S14: `children_skipped == {}` in the persisted record (prior record), `children` includes X and Y, status REUSED; S15 `control_flow_gates_passed == True`.
- **Minimal resolution:** recover from the prior record only when `set(prior["children"]) == set(children) | unavailable`, `prior["children_skipped"] == {c: r for c, r in skipped.items() if c not in unavailable}`-consistent (i.e. no failed/blocked child this attempt), and `prior["children_evidence"][c] == evidence_by_child[c]` for every present child; otherwise raise a typed S14 failure. Given that tables are now persisted, consider deleting the branch: an unavailable child with a prior record is store corruption, not a lawful reuse. Add `executed_trade_tables/<id>` for every `children_evidence` entry and `regime_stratified_reports/<id>` for every `stratified_report_ids` entry to `s15_regime_reload_failures`.

### B-04 — MEDIUM — `_mark_downstream_not_run` resets stage statuses but not the terminal `publication` block; `activate_pipeline_result` can activate on a prior attempt's gates after a later attempt failed closed

- **Contract:** plan §3.8 (typed stage failure must not be converted into a lawful state), §3.10 gate; the function's own docstring ("a prior attempt's terminal status is never carried forward as if this attempt had reached it"); kickoff §9 "No … strategy is automatically selected or promoted".
- **Where:** `pipeline.py::_mark_downstream_not_run` (touches `status/explanation/started_at/ended_at` of later stages only); `run_pipeline` (never resets `state["publication"]`; only S15 writes it, with `gates: None`); `run_publication_gates` (writes `publication["gates"]`); `activate_pipeline_result` (requires only `publication["gates"]` all true and non-verification scope — does not re-derive gates or check the latest attempt).
- **What is wrong:** after attempt 1 completes and `run_publication_gates` records all-true gates, an attempt 2 that halts at S12 with a typed sidecar failure leaves S13–S15 PENDING (correct) but `state["publication"]` — `pipeline_result_id`, `control_flow_gates_passed: True`, `gates: {all True}`, `state: "gates_passed"` — is attempt 1's. `activate_pipeline_result` then activates. The publication block is the carried-forward terminal state the docstring says is never carried forward. (Cancel has the same gap; pre-existing, but the FIX introduced the reset and stopped short of the block that matters for activation.)
- **Reproduction (test shape, development scope):** run → `run_publication_gates` → all gates true; tamper the S12 `prop_vectors.json` sidecar; retry → S12 FAILED, S13–S15 PENDING; `activate_pipeline_result(state_root, id, store_root=root)` → returns the attempt-1 `pipeline_result_id` (no `PublicationError`).
- **Minimal resolution:** in `_mark_downstream_not_run` (and the cancel branch) reset `state["publication"] = {"state": "prepared_not_published", "gates": None, "activated": False, "pipeline_result_id": None, "control_flow_gates_passed": False, "reload_failures": {}}` whenever S15 is among the not-run stages; additionally make `activate_pipeline_result` refuse unless every planned stage of the LATEST attempt is terminal (re-run `run_publication_gates` inside it).

### B-05 — MEDIUM — "Verified reuse by reproduction" is neither zero-replay nor fully verified: every reused child re-runs the child runner even when its persisted table is present, the double-run tests do not assert replay counts, and the raw core-table hash of the re-derived tables is never compared with the stored envelope

- **Contract:** plan §3.10 ("double-run pipeline reuse with identical stage-result ids and **zero replay on verified reuse**"), §3.7 (`source_core_table_hash` is a bound envelope fact; "identical bytes reuse, different bytes under one semantic ID fail closed"); §9 row "Executed-trade artifact".
- **Where:** `pipeline.py::_reuse_child_with_regime_tables` (`result = context.wiring.child_runner(...)` unconditionally; `row["replay_invocations"] = 1`; compares `stored.table_bytes != table_bytes` only — `table_envelope.source_core_table_hash` vs `stored.envelope.source_core_table_hash` is never compared); `tests/agents/ifvg_search/test_pipeline_regime.py::test_second_attempt_reuses_every_regime_stage` and `test_pipeline_run.py::test_second_attempt_with_different_workers_shares_every_semantic_identity` (assert stage ids + REUSED only); `test_pipeline_regime.py::test_s02_reused_children_are_adopted_only_by_verified_reproduction` asserts `replay_invocations == 1` on reuse.
- **What is wrong:** with stratified reporting requested (every regime fixture), a same-semantic second attempt replays every child once (the runner is invoked before anything is checked), so the §3.10 "zero replay on verified reuse" gate is not met by the fixture the gate evidence is drawn from, and no test would notice either way. R6.1's DEV-R6.1-8 justified re-derivation because no table artifact existed; §3.7 removed that premise for S14 (S12/S13 still consume `result.tables`, which is the remaining reason). Separately, the reproduction check is on the 42-column projection only: the raw table's `source_core_table_hash` (bound in the stored envelope, and the value the neutrality report hashes) is available on both sides and not compared, so a runner drift confined to non-projected columns (which feed S12's `gross_trade_stream_hash` via `search_bridge`, the lineage maps and the neutrality hashes) is adopted as "reproduced byte-for-byte".
- **Reproduction:** read `state["children"][*]["replay_invocations"]` after `test_second_attempt_reuses_every_regime_stage` → 1 per child (not 0). For the hash gap: monkeypatch `child_runner` to add/alter a non-projected executed-trade column (e.g. `geometry_entry_ticks`) on the second run → row stays `reused` with "reproduced … byte-for-byte".
- **Minimal resolution:** compare `source_core_table_hash` as well (typed refusal on mismatch); either (a) accept and document the deviation and reword the §3.10 gate evidence to "zero replay for non-stratified runs; one verified reproduction per reused child for stratified runs", asserting `replay_invocations` explicitly in both double-run tests, or (b) skip the runner when the table is present and S12/S13 have prior vectors/simulations for the same semantic id (load-only reuse), replaying only when a stage needs raw tables the store does not hold.

### B-06 — MEDIUM — Test adequacy: several §3.7/§3.8 claims are not falsified by the new tests (mutations that survive)

- **Contract:** plan §3.10 ("every new identity/tamper/leakage test red before code and green after"), §9 rows "Executed-trade artifact", "Prior-stage sidecar failures"; kickoff §10 ("Do not dismiss a finding because the tests are green").
- **Where / what survives:**
  1. `pipeline.py::_reuse_child_with_regime_tables` — deleting `if stored.table_bytes != table_bytes: raise RuntimeError(...)` passes the suite: no test drives a drifted child runner against a present table (the only tamper test corrupts the sidecar, which the probe catches earlier).
  2. `regime_report_stage.py::_prior_reports_record` and `_delivered_by_s09c` — reverting either to `except Exception: return None` passes: in `test_prior_attempt_sidecar_tamper_fails_the_stage_closed` the S14 and S09 cases fail at the owning stage's republication (`load_verified_envelope` → "failed verification"), never through these probes, and the assertion accepts either message (`"sidecar_hash_mismatch" in explanation or "failed verification" in explanation`). `_delivered_by_s09c`'s typed branch is unreachable in a normal run (S09 republishes before S14 reads it) — it is defensive code with no test.
  3. `pipeline.py::_stage_s02_replays` neutrality agreement (`declared != table_envelope.source_core_table_hash`) is dead in the synthetic suite: the fixture runner's neutrality report carries `audit_disabled_core_table_hashes={}` (`child_replay.py:1031`), so `declared is None` always.
  4. `pipeline_regime.py::s15_regime_reload_failures` per-artifact reasons are exercised only via a monkeypatched `insights` loader (`test_s15_records_reload_failures_by_reason`); no regime artifact is actually tampered, so a loader that swallows errors would pass.
  5. `test_executed_trade_table.py::test_costed_evaluation_from_the_persisted_projection_equals_the_raw_evaluation` compares `model_dump(mode="json")` dicts (`80000 == 80000.0`) — see B-01; it cannot see the persisted-bytes divergence it is meant to rule out.
  6. `test_s15_records_reload_failures_by_reason` asserts the stage is `COMPLETED or REUSED` and `control_flow_gates_passed is False`, but not that `reload_failures` reached the immutable S15 output (see B-09) nor that `run_publication_gates` fails.
- **Reproduction:** apply each mutation with a one-line edit in a scratch worktree and run the listed tests.
- **Minimal resolution:** add (1) a drifted-runner reuse test (perturb one `realized_ticks`/`resolution` on the second run → child `failed`, explanation contains "byte-for-byte"); (2) a test that reaches `_prior_reports_record` (double deletion of B-03 + tampered S14 sidecar → S14 FAILED with `sidecar_hash_mismatch`) and a unit test of `_delivered_by_s09c` on a tampered S09 record; (3) a fixture neutrality report with a declared executed-trade hash and one disagreeing hash → child `failed`; (4) a real tamper of a regime artifact (e.g. `regime_oos_assignments` sidecar) → `reload_failures` key/reason; (5) byte comparison in the projection-equality test; (6) assert `run_publication_gates` fails when `reload_failures` is non-empty.

### B-07 — MINOR — `record_schema_version` for the table identity is derived inconsistently when the identity resolver returns a bare core id (no core replay envelope)

- **Contract:** plan §3.7 (`ExecutedTradeTablePayload.source_record_schema_version`; table id derived from the core replay, "may not be inferred from a frame").
- **Where:** `pipeline.py::_executed_trade_evidence_for_result` (envelope None → version from `trades["record_schema_version"].iloc[0]`, or 2 when the table is empty) vs `_stage_s02_replays` reuse branch (`version = … if envelope is not None else 2`).
- **What is wrong:** the fresh path infers the identity input from the data (a v3 table mints id(v=3); an empty table mints id(v=2)); the reuse path always probes id(v=2) → a present v3 table is reported "absent" → `executed_trade_table_unavailable` (typed, but wrong), and `row["executed_trade_table_id"]` recorded at completion disagrees with the id probed on reuse. `_resolve_core_id` returns `(str, None)` whenever `wiring.identity_resolver` returns a string, which the type `Callable[..., object]` permits; production resolvers (`executors._slice_identity_resolver`, `orchestrator._resolve_identity`) return envelopes today, so this is latent.
- **Reproduction:** wire an `identity_resolver` returning the core id string with a runner emitting `record_schema_version=3` rows; run twice under a regime charter → first run persists id(v=3); second run probes id(v=2) → "unavailable".
- **Minimal resolution:** derive the version in one helper used by both paths, and raise `PipelineWiringError` when no core replay envelope is available for a child whose table must be persisted (the identity must come from the core replay, not the frame).

### B-08 — MINOR — `probe_executed_trade_table` classifies envelope-load failures by message substrings inside a broad `except Exception`

- **Contract:** plan §3.8 ("Never catch broad `Exception` around immutable evidence loads"; closed reason vocabulary).
- **Where:** `search/executed_trade_table.py::probe_executed_trade_table` lines 302–319.
- **What is wrong:** any exception from `load_verified_envelope` that matches none of the substrings ("does not match directory", "identity mismatch", "failed verification", "validation error") is labeled `manifest_hash_mismatch` — an `OSError`, a corrupt `envelope.json` (`json.JSONDecodeError` message "Expecting value…"), or a `KeyError` from a manifest entry lacking `sha256` all become "manifest_hash_mismatch". The reason is typed and never becomes absence (fail-closed holds), but the recorded reason is wrong for those states.
- **Reproduction:** overwrite `envelope.json` of a table entry with `{not json` and re-sign the manifest → probe raises `SidecarLoadError("manifest_hash_mismatch")` instead of `malformed_sidecar`/`malformed_manifest`.
- **Minimal resolution:** have `store.load_verified_envelope` raise `SidecarLoadError` subtypes (or `SearchStoreError` with a `reason` attribute) and classify by type; catch `SearchStoreError | OSError | ValueError` only.

### B-09 — MINOR — S15 reload-failure reasons live only in the mutable state file, not in the S15 stage result or `PipelineResultPayload`

- **Contract:** plan §3.8 ("Persist reload-failure reasons in the S15 record").
- **Where:** `pipeline.py::_stage_s15_verify_publish` (`context.state["publication"]["reload_failures"] = …`; nothing added to `context.stage_sidecars`; `PipelineResultPayload` unchanged).
- **What is wrong:** the reasons are written to `pipeline_state.json`, which the next attempt overwrites (S15 rewrites `publication`), so an attempt that observed reload failures leaves no immutable trace once a later attempt reaches S15; the S15 stage-result envelope and the persisted `PipelineResultEnvelope` carry only the boolean gate.
- **Reproduction:** `test_s15_records_reload_failures_by_reason` then a third attempt without the monkeypatch → `state["publication"]["reload_failures"] == {}`; no store artifact records the earlier failure.
- **Minimal resolution:** emit `reload_failures.json` (canonical bytes) as an S15 stage sidecar when non-empty (it changes the stage result's sidecar set only on failing attempts, which are not reusable anyway) or add `reload_failure_reasons` to `PipelineResultPayload`.

### B-10 — MINOR — Projection docstrings state 44 columns; the schema (and the test) have 42; projection dtypes intentionally diverge from the synthetic fixture

- **Contract:** plan §3.7 ("exact ordered projection … may not be inferred from a frame"); §3.9 ("Golden table bytes/hashes and legacy schemas must remain unchanged").
- **Where:** `search/executed_trade_table.py` line 80 (`#: The exact ordered projection (44 columns).`) and `project_executed_trades` docstring ("the 44 declared columns"); `tests/agents/ifvg_search/test_executed_trade_table.py::test_projection_schema_is_exact_and_never_inferred` asserts 42.
- **What is wrong:** documentation of the frozen schema disagrees with the schema; the declared `float64` tick columns are "typed the way the real v2 capture types them" but the synthetic fixture (`make_resolved_trades_frame`) emits `int64`, which is the mechanical root of B-01 — the pipeline verification fixture exercises a dtype path the real capture never takes, and the real capture's path (float ticks, date objects, tz-aware timestamps) is not exercised by the projection-equality test at all.
- **Reproduction:** `len(EXECUTED_TRADE_TABLE_SCHEMA_V1.names) == 42`.
- **Minimal resolution:** fix the two docstrings; parametrize `test_costed_evaluation_from_the_persisted_projection_equals_the_raw_evaluation` over a frame typed like the v2 capture (`float64` ticks, `date` trading day, `datetime64[ns, UTC]` timestamps) and the int-typed fixture, comparing serialized bytes.

---

## Affirmed (actively tried to break; could not)

- **No production `assert`** remains under `src/alpha_lab/agents/data_infra/ifvg` or `src/alpha_lab/propsim` (`grep -rn '^\s*assert\b'` → none); the five pipeline seams and the two fold-feature checks raise `PipelineWiringError` (subclass of `RuntimeError`, sanitized).
- **Typed sidecar probe:** `SidecarLoadError` refuses any reason outside `SIDECAR_LOAD_FAILURE_REASONS`; `probe_sidecar/has_sidecar/load_optional_sidecar_bytes/load_json_sidecar` return absence only for a manifest-proven not-produced name; declared-but-missing, hash mismatch, manifest hash/identity mismatch, malformed manifest, malformed JSON, I/O errors all raise typed (test + code). No reason string outside the vocabulary is constructed anywhere in `pipeline.py`, `regime_report_stage.py`, `executed_trade_table.py`.
- **Broad recovery removed** from `_prior_stage_vectors`, `_prior_stage_simulations`, `_persist_cross_profile_deltas` (`suppress(Exception)` gone), `_prior_reports_record`, `_delivered_by_s09c`; the remaining `except Exception` around evidence loads are S15's recording loops (record, never silence) and the classifier of B-08 (re-raises typed).
- **Executed-trade table store semantics:** identical bytes reuse; different bytes under one derived id → `SearchStoreError` at save (test); tampered sidecar → typed `SidecarLoadError` on probe and on load; envelope binding (`sha256`, `byte_size`, schema hash, row count) re-checked on load; the table id is derived (`executed_trade_table_id_for`) — no store listing anywhere on the S02/S14 paths.
- **S02 fail-closed on corrupt tables:** a tampered table sidecar under a reused child is a typed per-child REPLAY failure (`sidecar_hash_mismatch` in the explanation), other children continue, S14 records the child as `child_not_completed_or_reused` (test executed, green).
- **S14 iterates `context.children`** (the S01 enumeration, i.e. the charter child set); every non-report child carries a reason from the closed `CHILD_SKIP_REASONS`; `children_evidence` binds id + sha256 + row_count; `CohortDescriptiveBody.executed_trade_table_id` and the `stratified_prop` refs carry the table id into `source_metric_refs` (both classes); the reports are built from `load_executed_trade_table(...).frame` — never from `tables_by_child`.
- **F-10B:** `build_cohort_descriptive_body` joins, stratifies, computes and hashes the normalized frame only (test: shuffled/extra-column/lower-cased inputs hash identically); `per_trade_net_r` matches `compute_strategy_metrics` expectancy.
- **Double-run invariant:** both double-run tests still pass (identical stage-result ids, every non-S11 stage REUSED, S11 BLOCKED) — with the caveat of B-05 on replay counts.
- **`_mark_downstream_not_run`** leaves S11's registered blocked state untouched and does not alter the cancel path; `run_publication_gates` derives `all_planned_stages_terminal=False` for PENDING stages (the gap is the stale `publication` block of B-04, not the gate derivation).
- **UI/CLI consumers:** `scripts/ifvg_regime_panels.py::_render_assignment_view` unpacks `VerifiedFitAssignments`; every other `load_regime_fit_assignments` caller (executor, fold features, report stage) consumes the dataclass; `works_only_in_regime` remains `tuple[int, ...]` on the body (frontier + panel unchanged); `RegimeAssignmentEvidenceRef` constructors (service, contracts example) pass the two new hashes and `regime_stratified_prop` only receives the ref; `ControlledFeatureStudyPayload.label_content_hash` still exists (`ifvg_mbp1_panels.py:434` safe); S09b passes `fit_assignments=execution.verified_fit_assignments`; S10 reads `consulted_assignments`.
- **Goldens** (`test_r61_fix_goldens`) green: B0 bundle id, candidate protocol id, core replay id, account simulation id, feature-block registry hash unchanged.
- **`git diff --check`** clean on `src`, `scripts`, `tests`.
