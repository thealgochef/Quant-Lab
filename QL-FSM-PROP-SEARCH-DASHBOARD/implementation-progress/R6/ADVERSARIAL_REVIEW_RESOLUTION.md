# R6 — Adversarial Review Resolution

Every finding dispositioned; fixes verified by the re-run suites (final
counts in `TEST_RESULTS.md`). "FIXED" = code/test change in this release;
"ACCEPTED" = recorded without code change (with rationale); "REFUTED" =
disproven with evidence. Reviewer 1 = contract fidelity (F1–F16); Reviewer 2
= safety/access (S1–S9). Overlaps: S1 ≡ F5, S4 ≡ F2 (+ F1's bundle half).

| # | Sev | Disposition | Resolution |
|---|---|---|---|
| F1 | MAJOR | **FIXED** | `RegimeFitPayload` requires ≥1 verified 64-hex `source_artifact_ids` (contract validator) and gains `training_feature_matrix_hash` (canonical hash of the fold's training INPUT values, computed in `fit_regime_preprocessing`); `run_regime_protocol` takes `source_artifact_ids` as a REQUIRED keyword and validates it; `resolve_kmeans_protocol`/`assert_inputs_permitted` resolve `input_feature_bundle_ref` against the registered AVAILABLE bundles and refuse any input outside the bundle (`resolved_bundle_feature_names`). Tests: `test_fit_identity_requires_verified_sources_and_binds_training_values`, `test_fit_identity_binds_the_training_feature_values` (the reviewer's affine-perturbation probe: different fit ids, same row-id hash), `test_inputs_must_belong_to_the_referenced_bundle`. DECISIONS_TAKEN #60/#61. |
| F2 | MAJOR | **FIXED** | `assert_no_regime_leakage` applies the availability-stage rule ALWAYS: an explicit map first, then `registered_feature_stages()` (feature → block stage from `FEATURE_BLOCK_RESOLUTION_REGISTRY` × `FEATURE_BLOCK_REGISTRY`); a feature with no registered stage is refused as unprovable. The resolver and `run_regime_protocol` both call it (`assert_inputs_permitted`). Test: `test_leakage_validator_is_default_on_from_the_registries` (the reviewer's `direction @ htf_tap` probe now refuses). |
| F3 | MAJOR | **FIXED** | Temporal facts are computed by `_temporal_facts` over the OOS timeline (`oos_regime_timeline`: valid TEST rows with `observation_ts_utc`, ordered by `(fold_index, ts, row_id)`), transitions counted WITHIN each fold, with `temporal_order_policy` and `temporal_transition_count` in the report; the assignment frame carries `observation_ts_utc` (first present of `entry_ts_utc`/`feature_as_of_ts`/`bar_close_ts_utc`; a frame without one is refused). Canonical ids are additionally geometry-ranked (see F8/F9 → DECISIONS_TAKEN #64) so the transition matrix is invariant to id spelling. Test: `test_temporal_facts_are_id_spelling_invariant_and_oos` (the reviewer's sha-renaming probe: persistence, matrix, and count identical). |
| F4 | MAJOR | **FIXED** | `assign_panel_regimes_to_candidates` requires `partition`/`regime_fit_id` columns, consults only `partition == "test"` valid rows, sorts by `(row_id, fold_index)` (lowest OOS fold wins, independent of input order), and emits `regime_fit_id` + `partition`; a bar with no OOS assignment is `coverage_gap`. Test: `test_panel_pit_assignment_is_last_completed_bar_never_future_and_oos_only` (forward AND reversed input order; a train-only bar → gap) and the end-to-end panel path (F14). DECISIONS_TAKEN #57 rewritten. |
| F5 | MAJOR | **FIXED** | The contract enforces the ladder (`assert_lawful_promotion` inside the validator: one step, SUPERSEDED terminal, PLANNED never a target), `previous_decision_ref` chaining, ISO-8601 `decided_at`, and a 64-hex `owner_ratification_ref` pattern; `persist_regime_promotion` loads the referenced assessment (must exist/verify/name the protocol) and the chained previous decision, and re-runs the ladder with the assessment's OWN `gates_passed`. Tests: `test_promotion_ladder_is_structural_at_the_contract`, `test_promotion_persists_only_against_verified_evidence` (the reviewer's fabricated `"f"*64` assessment now refuses; FEATURE_ELIGIBLE over the failing under-sampled assessment refuses; the lawful chain persists). DECISIONS_TAKEN #59 corrected. |
| F6 | MAJOR | **FIXED** | `persist_regime_fit` refuses frames whose `regime_fit_id`/`fold_index`/protocol id disagree with the envelope or whose labels the fit's own estimator does not reproduce, verifies the exact bytes pre-publish (transform allclose + re-predict equals persisted labels over EVERY valid row), and re-verifies through the store; `load_regime_fit` also checks the artifact/assignment sidecars name this fit. Tests: `test_foreign_or_mismatched_assignment_frames_are_refused` (the reviewer's fold-1-frame probe), `test_persist_verify_reload_and_reuse` (reloaded estimator == every persisted label). |
| F7 | MAJOR | **FIXED (delivery) + ACCEPTED (documented scope)** | The panel now renders per-fold coverage, the fit identities, assessment/fold-set/input-bundle identity blocks, AMI 5th percentile, transition count, alignment space, per-cluster agreement, centroid profiles, the exact-fit-id ASSIGNMENT view (artifact facts, coverage by partition, distance/margin quantiles, the OOS regime timeline), the STRATIFICATION of the assignment frame (regime × partition), and the promotion role/status view; 4 new AppTests (panel-grain identity, assignment/stratification view incl. the cross-protocol refusal reason, promotion view, no-control source scan). Stratified RESULT views (metrics by regime) are the ML §5.5 comparison classes — recorded as DEV-R6-4 and an owner-visible gate row, not claimed. |
| F8 | MINOR | **FIXED** | `hungarian_lexicographic` enumerates every optimal assignment (k ≤ 8) and returns the lexicographically smallest column tuple — ascending fold-local id priority, exactly; shared by alignment, recurrence, and the bootstrap. Test: `test_hungarian_tie_break_is_ascending_fold_local_id` (tie-prone matrices incl. the reviewer's swap case). |
| F9 | MINOR | **FIXED** | Alignment, recurrence, separation, and descriptors use `feature_space_centroids` (the scaled INPUT-feature prefix); differing input-feature tuples refuse; `alignment_space` is recorded in the report. Test: `test_alignment_uses_the_scaled_input_feature_space` (a fold with an extra indicator column aligns on the 2-d prefix). |
| F10 | MINOR | **FIXED** | All-missing training rows are excluded from the training matrix and recorded as `training_rows_all_missing`; `training_row_ids`, `training_row_count`, both hashes, and the typed-null report agree. Test: `test_all_missing_training_rows_never_enter_the_fit` (fixture 4: every fold). |
| F11 | MINOR | **FIXED** | `keyed_observations` refuses duplicated observation keys (service and preprocessing). Test: `test_duplicate_observation_keys_are_refused`. |
| F12 | MINOR | **FIXED** | (a) `initialization_policy` is `str` validated against `INITIALIZATION_POLICIES` (one per registry entry) — every planned protocol is expressible (`test_planned_protocol_policies_fail_closed_before_any_fit` constructs a GMM protocol) and refused only by executability; (b) `pinned_parameters_hash` enters the protocol identity and is re-verified at run time (drift refuses; `test_protocol_identity_is_sensitive_to_science_and_pins_the_seed`); (c) the Nyström nested config is an immutable pair tuple (`test_algorithm_identities_are_distinct_with_distinct_pinned_payloads`). |
| F13 | MINOR | **FIXED** | `RegimeAssignmentColumns` matches the persisted frame exactly (`distances` now emitted; `probabilities` dropped until the GMM release; `observation_ts_utc` added). |
| F14 | MINOR | **FIXED** | `SAMPLE_ADEQUACY_MINIMUMS` removed; `sample_adequacy_minimum(grain)` reads `REGIME_PROPOSED_DEFAULTS` (the decision-row minimum is now a stamped entry); the panel-grain fit path executes end-to-end on a synthetic 480-bar 5m panel with hand-built walk-forward folds (`test_panel_grain_fit_path_runs_end_to_end_and_assigns_candidates_pit`); the absence of a panel materializer/fold builder is DEV-R6-7. |
| F15 | MINOR | **FIXED** | Every overclaim rewritten to what the code does: ACCESS_SAFETY_EVIDENCE (promotion, input permission, executability), DEVIATIONS (DEV-R6-4/5 rewritten; DEV-R6-6…8 added), DECISIONS_TAKEN (#57/#59 corrected; #60–#66 added), the module docstrings (alignment tie-break, temporal policy, preprocessing seam wording), `ARCH_APPEND` and D-044 (promotion wording). |
| F16 | MINOR | **FIXED** | New proofs: bootstrap/silhouette/separation training-only under poisoning (`test_bootstrap_and_internal_scores_see_training_rows_only`), `software_versions` and pinned-hash sensitivity, winsorization end-to-end incl. persist/reload of clip bounds (`test_k2_margins_and_winsorization_execute`, `test_winsorized_fit_persists_and_reloads`), k=2 margins, reloaded-estimator-vs-persisted-assignments (F6), the panel no-control source scan, the panel-grain identity AppTest. |
| S1 | MAJOR | **FIXED** | ≡ F5 (contract + persistence enforcement). The reviewer's `PLANNED→MODEL_FEATURE, ratification="x", decided_at="t"` probe now fails at construction on three independent checks (ladder skip, ratification pattern, timestamp). |
| S2 | MAJOR | **FIXED** | `assert_lawful_role` + `ROLE_MINIMUM_STATUS` + `V1_UNREPRESENTABLE_ROLES`: decision-policy / execution-gate-candidate / frozen-execution-gate roles are unrepresentable in V1 (the exact `S11_BLOCKED_REASON` in the error); other roles require the status that earns them. Test: `test_role_ladder_makes_execution_roles_unrepresentable_in_v1`. |
| S3 | MAJOR | **FIXED** | Registry entries carry `executable_policies`; `assert_protocol_executable` refuses — before any preprocessing or fit, in `run_regime_protocol` AND `_fit_one_fold` — any planned algorithm, version/pin drift, unregistered initialization policy, or policy value outside the executable set (`inner_train_only_selection`, PCA, kernels, `none_training_only`, …). Tests: `test_planned_protocol_policies_fail_closed_before_any_fit`, `test_planned_algorithm_or_policy_cannot_reach_a_fit` (zero preprocessing fits before refusal). Evidence wording updated ("no implementation" → "refused"). |
| S4 | MEDIUM | **FIXED** | ≡ F2 + F1's bundle half: `input_feature_bundle_ref` is 64-hex, resolved against the bundle registry, inputs must be members, stages come from the registries by default. |
| S5 | MINOR | **FIXED** | `_UI_SCRIPTS` gains `ifvg_mbp1_panels.py` and `ifvg_regime_panels.py`; all 14 FUX scans pass over both. |
| S6 | MINOR | **FIXED** | `load_sidecar_bytes` verifies `manifest_payload_sha256` and the manifest identity, `fullmatch`es the name against `_SIDECAR_NAME_PATTERN` (store-owned names refused), and hashes the bytes it returns; `RegimeFitArtifact._relative_ref` mirrors the whitelist (`SIDECAR_REFERENCE_PATTERN`). Test: `test_sidecar_loader_is_hardened` (the reviewer's traversal and stale-manifest probes refuse). |
| S7 | MINOR | **ACCEPTED (documented) + partial FIX** | The trust boundary is recorded (DEV-R6-8, ACCESS_SAFETY_EVIDENCE); the UI path never unpickles (`load_regime_fit_assignments`, `test_ui_loader_never_unpickles` with `joblib.load` forbidden); signing / non-pickle serialization is a post-V1 hardening candidate. |
| S8 | MINOR | **FIXED** | Verification runs on the exact bytes BEFORE `save_or_reuse_envelope`; the post-publish store reload is re-verified and a fresh failing entry is withdrawn. Test: `test_failed_verification_never_publishes` (a simulated reload failure leaves no entry on disk). |
| S9 | MINOR | **FIXED** | `assert_protocol_executable` precedes `fit_regime_preprocessing` in `_fit_one_fold` (test-pinned: zero preprocessing fits before refusal). |

## Post-fix verification

The full affected re-run after all fixes: `test_regime_contracts.py` 14 +
`test_regime_service.py` 20 + `test_regime_store.py` 9 +
`ifvg_search/test_identities.py` 19 + `test_ifvg_pipeline_tab.py` 30 +
`test_ifvg_study_scans.py` 14 = **106 passed**; `ruff check src tests
scripts` clean; `git diff --check` clean. Release-final full-suite counts:
`TEST_RESULTS.md`.
