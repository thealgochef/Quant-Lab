# R2 — Adversarial Review Resolution

Two independent read-only reviewers: **A** (contracts lens — 1 blocker, 6
majors, 12 minors) and **B** (safety/compat lens — 0 blockers, 4 majors, 9
minors). Deduplicated: **1 blocker, 10 distinct majors, 19 minors. Every
finding was resolved — fixed in code/tests, or explicitly ruled with the
reviewer's own criteria; none was dismissed because tests were green.**
Post-fix verification: the full lane suite (216 tests incl. 20 new
finding-driven tests) and the full repository suite re-ran green after every
fix; ruff clean.

| Finding | Severity | Resolution |
|---|---|---|
| A-F1 (≡ the blocker): failed/absent neutrality did not block child publication; slice published before neutrality | blocker | **Fixed** three ways: `run_child_replay` now raises typed `ChildNeutralityError` on a failed dual-drive proof (worker seam); `run_baseline_verification_slice` refuses fail-closed on a missing/failed report BEFORE any store write and follows the §3.3 order (companions → publish); `run_search` duck-checks a runner result's `neutrality` and ends the child `failed`/`INVARIANT` with **no** envelope/membership publication (non-reusable). Tests: `test_run_child_replay_raises_on_failed_neutrality`, `test_slice_refuses_failed_neutrality_before_any_publication` (asserts empty stores), `test_failed_neutrality_child_never_publishes` (incl. rerun-replays-it proof). |
| A-F2: stale-lock break could kill a live run; TOCTOU; holder deletes breaker's lock | major | **Fixed**: default `stale_lock_seconds` raised to 86,400 s (beyond any real child/prewarm duration; checkpoint heartbeats continue); the break is an atomic rename-to-quarantine (exactly one breaker wins — no unlink/create TOCTOU); release is nonce-verified (the holder deletes the lock only when its body still holds the holder's own nonce). Residual: a single child/prewarm exceeding 24h with zero checkpoints could still be mis-broken by an explicit low-threshold caller — recorded in DEVIATIONS (operator guidance; the R5 pipeline adds progress heartbeats). Existing lock tests re-pass; resume-after-kill re-proven. |
| A-F3: contrast declaration not charter-anchored (spoofable); charter schema lacked the field (plan gap) | major | **Fixed**: `SearchCharterPayload.declared_contrast_ids` added (inside the hashed charter identity — a new declaration mints a new `search_id`, proven by `test_declared_contrast_ids_are_inside_the_charter_identity`); `evaluate_declared_contrast` now takes the charter and refuses both an unlisted id AND a contrast bound to a different charter. Plan-gap closure recorded as DEV-R2-9. |
| A-F4: cross-profile native deltas constructible | major | **Fixed**: `build_native_population_delta` extracts `envelope_section_config_hash` from both sides and disables the comparison (`not_comparable`, exact reason) on mismatch; None-tolerant for hand-built frames without the column. Misuse test over the two REAL profile chains: `test_native_basis_refuses_cross_profile_tables`. |
| A-F5 ≡ B-MAJOR-3: reused children excluded from gates/frontier; nothing persisted | major | **Fixed**: completed children publish a `CostedEvaluationEnvelope` (deterministic id = hash(core_replay_id, cost_policy_sha256); study-independent `StrategyMetrics` as a manifest sidecar; the bootstrap seed is now replay+cost-scoped, never study-scoped); reused children RELOAD the metrics and re-evaluate charter-scoped gates; the `FrontierResult` publishes into the `frontiers` store. `test_repeat_identical_gates_and_frontier_across_full_reuse` proves a fully-reused second run reproduces the byte-identical frontier and gate outcomes (also closes B-m5). A reused child with no published evaluation (pre-fix store) is explicitly annotated, never silently gate-free. |
| A-F6: placeholder registry hash; unreachable `blocked_invariant_failure`; no re-enforcement at enumeration | major | **Fixed**: `enumerate_children` defaults `registry_hash` to the real `registry_sha256(axes, values)` (E2E asserts every capability carries it); per-combination `assert_axes_authorized` re-runs fail-closed (hand-built blocked-value charter refused — `test_enumeration_refuses_blocked_values_even_without_charter_validation`); the locked-invariant check is implemented (`_locked_invariant_violations` vs the resolved baseline) making `blocked_invariant_failure` reachable, with exactly one exemption: the resolver's own `qualification_mode → "custom_profile"` provenance relabel (reducer stores but never branches on it — verified; DEV-R2-8). |
| A-F7: collision records unpersisted; false "persisted" wording | major | **Fixed**: `LineageUniquenessEnvelope` + `persist_lineage_uniqueness` + the `lineage_reports` store (registered in the identity-projection audit); the misleading reason text corrected to name the persistence helper. `test_uniqueness_report_persists_immutably`. R4's comparison surfaces must call the helper before surfacing any cross-profile delta (noted in DEVIATIONS). |
| B-MAJOR-1: warmup-stamp contradiction (audit rows vs DAY_FUNNEL; slice cfg default 10) | major | **Fixed**: the slice capture cfg sets `warmup_days=0` (Path A: zero real warmup — also fixes the v2 invariant-audit evidence counts), and `build_child_fsm_audit` fail-closes on ANY disagreement between capture-time `is_warmup` stamps and the `warmup_days` argument. Tests: fixture rebuilt at zero warmup; `test_warmup_stamp_disagreement_refuses`. |
| B-MAJOR-2: `resolve_selection(setup_id=…)` untested/unreachable | major | **Fixed**: `test_setup_drill_through.py` proves all three exact outcomes (unique resolve; zero-candidate refusal toward setup mode; multi-candidate refusal naming the exact count) plus the exactly-one-ID rule including `setup_id`. The tab path deliberately routes to setup mode (documented in the provider docstring: the candidate-mode branch serves programmatic/results callers, wired by R4's drill actions). |
| B-MAJOR-4: no end-to-end funnel-delta→setup→verifier proof | major | **Fixed**: `test_funnel_delta_drill_through_chain_end_to_end` drives divergence entity → `queue_jump` → setup-mode routing → exact provider resolution over the artifact's own tables, including the clean refusal on the side where the entity does not exist. |
| A-F8: EvidenceRef kinds beyond CS §12 | minor | **Fixed**: dead `artifact`/`report` members deleted; `candidate`/`decision` retained as the population entity kinds the delta layer emits — recorded as DEV-R2-7 (plan-vocabulary patch). |
| A-F9: max-cap gates passed silently on missing observables | minor | **Fixed**: a cap that could not be measured while trades exist fails closed with an explicit "unmeasurable" explanation; the zero-trade vacuous case is preserved (min gates fail first). `test_max_gates_fail_closed_when_unmeasurable_with_trades`. |
| A-F10 ≡ B-m1: vacuous drill-target pass reached the gate boolean | minor | **Fixed**: the slice summary now stamps `verifier_link_vacuous_zero_targets` beside the gate policy; the acceptance runner must require `target_count > 0` on the real slice (recorded in DEVIATIONS + GATE_SUMMARY as an acceptance-time requirement). |
| A-F11: report-object read-hole; ImmutableMap coerce dup-collapse | minor | **Fixed (partial by design)**: `ContrastResult.effect_estimate` values are nested `ImmutableMap`s; the pydantic-coerce path now refuses duplicate keys exactly like the constructor. `StrategyMetrics.trade_stats` remains a deep-copied evidence blob behind `ImmutableMap` (mutating a retrieved nested dict cannot alter any identity or stored artifact — sidecar bytes are hash-pinned); recorded in DEVIATIONS as the accepted evidence-blob semantics. |
| A-F12: canonicalize compared only the claimed baseline | minor | **Fixed**: content is compared against ALL registered baselines (cached name-free hash map); identical content adopts the owning registered name. `test_identical_content_under_a_wrong_registered_name_adopts_the_owner`. |
| A-F13 ≡ B-m3: cancel sentinel never consumed; skip reasons unrecorded | minor | **Fixed**: the sentinel is consumed when honored AND cleared at run start (cancel binds to a running search); `phase_notes` in every checkpoint records the R3-owned phases as explicitly skipped with reasons. Cancel test extended; E2E asserts the notes. |
| A-F14 ≡ B-m2: sidecar reuse unverified; audit published before its gate evidence | minor | **Fixed**: `save_or_reuse_envelope` verifies provided sidecar hashes against the stored manifest on reuse (fail-closed on divergence); the neutrality report publishes BEFORE the audit companion. |
| A-F15: referential integrity fail-open on an empty lifecycle | minor | **Fixed**: the `and setups` guard removed — any referenced setup id with no lifecycle row fails the proof. |
| A-F16: hardcoded minimize set; None-objective crash path | minor | **Fixed**: `OBJECTIVE_DIRECTIONS` registry (strategy + CS §5.6 prop metrics) with charter-validation refusal of unregistered objective/tie-break names (DEV-R2-9); a gates-passing child missing an objective metric is excluded from the frontier with an explicit explanation instead of crashing the run. |
| A-F17: interaction refusal semantics; strata keep-last overwrite | minor | **Fixed**: typed `InteractionEvaluationUnavailableError` naming the R4 landing (DEV-R2-5 stands as ruled lawful by reviewer A); duplicate grid positions refuse as ambiguous. Both tested. |
| A-F18: forbidden substring in the frontier trace; FrontierResult prop fields absent | minor | **Fixed**: trace line rephrased ("development lane only; an owner-approved outer evaluation protocol is required before any publication claim"); the CS §12 prop-champion fields are the R3 extension — recorded in DEVIATIONS. |
| A-F19: CWD-relative canonical allowlist marker | minor | **Fixed**: the slice passes `canonical_root=repo_root / SEARCH_TEST_STORE_ROOT`. |
| B-m4: failed-replay child path untested | minor | **Fixed**: `test_failed_replay_child_is_typed_and_sanitized` (REPLAY reason, path-free text). |
| B-m5: repeat-identical untested | minor | **Fixed** with A-F5 (`test_repeat_identical_gates_and_frontier_across_full_reuse`). |
| B-m6: job-shim hardening/coverage gaps | minor | **Partially fixed**: the `job.log` handle is context-managed; the `--runner-entry` registry-gating requirement is recorded in DEVIATIONS as an R4 obligation (the UI never passes user-shaped strings to it); the `start` detached arm remains covered by the source-scan + refusal tests only — recorded as a residual (R4 wires and tests the UI launch path per FUX-MON/PIPE rows). |
| B-m7: change-set hygiene (stale doc hunks; dataset.py out-of-list) | minor | **Ruled + handled**: the "stale doc edits" are the USER'S pre-existing uncommitted hunks — never part of any release commit (the hash-object staging discipline rebuilds shared docs from HEAD + R2 lane transforms only, byte-verified post-commit); dataset.py was already recorded as DEV-R2-1. |
| B-m8: lineage validity caller-asserted; P1-E sub-clauses implicit | minor | **Fixed**: `derive_lineage_validity` derives the verdict from the axis registry (geometry/TF/thesis axes break keys; clock axes do not) with tests; direction/family are structural key fields (collision impossible across them by construction — reviewer's own note). |
| B-m9: unsourced cost literal in the slice publication | minor | **Fixed**: `cost_points` parameter defaulting to the registered `CostPolicy` (tick size too). |

## Residuals (explicitly open, none silently dropped)

1. The `start` arm of the job shim (detached Popen) has no automated test —
   R4 wires and tests the UI launch path (FUX-MON/FUX-PIPE rows).
2. A child/prewarm exceeding 24h with zero checkpoints could be mis-broken by
   an explicit low-threshold caller; the R5 pipeline adds in-child progress
   heartbeats.
3. The real slice's acceptance runner must require
   `verifier_link_vacuous_zero_targets == False` (a funnel-quiet window makes
   the link gate vacuous by construction) — carried in GATE_SUMMARY.
4. `StrategyMetrics.trade_stats` remains a deep-copied evidence blob (see
   A-F11 ruling).
