# HARDENING-BACKEND — Adversarial Review Resolution

Two independent read-only reviewers (`ADVERSARIAL_REVIEW.md`, verbatim):
Reviewer A (semantic authority / immutable evidence / access safety / Phase 3
and 4 contracts) — **0 blockers, 2 major, 4 medium, 5 minor**, access safety
AFFIRMED; Reviewer B (capacity / warning policy / sequential truth /
integration / test adequacy) — **0 blockers, 0 major, 2 medium, 7 minor**,
capacity-gate verdict YES for the measured shape, access safety AFFIRMED.
**All 20 findings dispositioned below**; the fix round ran as two disjoint
lanes (the main agent; Fix-E for the bounded-verification module, the runner
and the test-adequacy items — `_fix_E_NOTES.md`). Every fix was written red
first inside its test module (`test_hardening_fix_round.py`, the Fix-E
modules) and the touched suites were re-run before the release-final suites.

| Finding | Severity | Disposition | What the code does now | Proof |
|---|---|---|---|---|
| RA-01 | MAJOR | **FIXED** | `assert_diagnostic_authorized` binds the store namespace (`test` class, coherently deployed) and the CURRENT head witness right after the run-envelope checks — before the coverage matrix loads, before `register_program_allowlist`, before any path; the pathname rule stays as defense in depth (DEV-HB-24) | `test_hardening_fix_round.py::test_mbp1_diagnostic_seam_binds_namespace_and_current_head_before_any_path` (unmarked root, research class, moved head — no marker written); `test_mbp1_coverage_evidence.py` (18) updated for the earlier refusal |
| RA-02 | MAJOR | **FIXED** | `release` / `refresh` / the reclaim unlink / the body read retry transient `OSError`s (40 × 25 ms); a persistent failure is typed `lock_release_failed` / `lock_refresh_failed`, never silent (DEV-HB-23) | `test_release_survives_a_transient_reader_and_never_orphans_silently` (a real read handle held across the release; then a persistent simulated violation → typed, token intact); `test_refresh_retries_transient_failures_and_types_persistent_ones`; `test_owner_decision_lock.py` (8) green |
| RA-03 | MEDIUM | **FIXED** | `activate_pipeline_result` verified-loads the charter after the re-derived gates; a synthetic-marker charter never activates; a real bundle must be bound to this namespace and the CURRENT head (DEV-HB-25) | `test_activation_re_verifies_the_charter_bundle_against_the_current_head` (bound bundle activates; a supersession landing after the launch refuses "no longer bound"; synthetic refused); the R6.1-FIX crafted-state test still refuses "latest attempt" first |
| RA-04 | MEDIUM | **FIXED (Fix-E)** | the runner captures attempt 1's `search_results` envelope BEFORE attempt 2 (`first_attempt_gates` ≠ `second_attempt_gates` by construction), reads the executed-trade table ids from the state's children rows (the previous `executed_trade_table_id_for(store_root, core_id)` call had the wrong signature — found and fixed by the round), and GATHERS the four store proofs per table via `store_behavior_proofs` (scratch copies under the evidence dir: identical bytes load and a same-identity publication reuses; a flipped sidecar byte, a missing manifest, a corrupt manifest and a truncated sidecar each fail closed with the typed reason; the scratch root is removed in `finally`); `native_ids_repeat` also requires identical pipeline-result ids across the attempts; the test-suite refs stay as `supplementary_store_suite` evidence | `_fix_E_NOTES.md`; `test_bounded_verification.py::test_store_behavior_proofs_probe_the_fixture_table_and_detect_a_tampered_source` (an already-tampered source reports a failed identical-bytes proof) |
| RA-05 | MEDIUM | **FIXED (Fix-E)** | the preflight seed step maps failures by cause: `SeedSnapshotError` → `seed_profile_mismatch`, `StoreNamespaceError` → `store_namespace_refused`, `SearchStoreError` / `SidecarLoadError` → the new registered `seed_snapshot_unverifiable`; anything else propagates | `test_bounded_verification.py::test_preflight_types_seed_failures_by_their_cause` (missing entry / corrupt sidecar → `seed_snapshot_unverifiable`; loader profile refusal → `seed_profile_mismatch`; a `RuntimeError` propagates) |
| RA-06 | MEDIUM | **FIXED** | the synthetic fixture initializes a `test` namespace only for an unmarked root under the process temp directory; any other unmarked root is a typed `store_namespace_missing` refusal — the operator's explicit `init` is the only way to mark a real store (DEV-HB-26) | `test_synthetic_fixture_never_marks_a_root_outside_the_temp_directory` (a repository-local probe directory: refused, no namespace file created) |
| RA-07 | MINOR | **FIXED** | `run_seed_production_chain` snapshots per-store entry COUNTS before the replay and refuses `prohibited_output_written` if any store other than `seed_snapshots` / `seed_production_runs` changed (counts only, no id listing); the git identity reads are documented as non-path reads (DEV-HB-29) | `test_seed_production.py` (9) green with the post-condition in place |
| RA-08 | MINOR | **FIXED** | `_verification_run_envelope(store_root, pipeline_semantic_id=…)` selects by the pipeline id being launched (the pipeline entry passes it) and refuses more than one candidate — never positional (DEV-HB-27) | `test_verification_run_is_selected_by_pipeline_id_and_ambiguity_refuses` |
| RA-09 | MINOR | **FIXED** | `assert_namespace_deployment_coherent` runs inside `assert_authorization_bound_to_store` (every bound seam) (DEV-HB-28) | `test_bound_check_refuses_an_incoherently_deployed_test_namespace` |
| RA-10 | MINOR | **FIXED** | `_atomic_write_text` retries `os.replace` and types a persistent failure `atomic_write_failed` (the temp file removed; the prior head intact) (DEV-HB-23) | `test_atomic_head_write_retries_and_types_a_persistent_failure` |
| RA-11 | MINOR | **ACCEPTED (documented)** | the shortlist's "highest lifecycle/candidate/decision/trade coverage" entry maximizes a documented tuple (trades, decisions, candidates, lifecycle rows, then rank); `rank_logical_windows` itself is the §5.1 lexicographic order with no hidden score (DEV-HB-17 / `_ws_D_NOTES.md` DEV-D-6) | `test_verification_window.py` (ranking order = the plan order) |
| B-01 | MEDIUM | **FIXED (Fix-E)** | `_s14_component` reads the sidecar S14 actually persists (`regime_stratified_reports.json`) and carries `children_skipped`, `fitting_performed` and `report_ids` as evidence; a record that claims fitting types `unexpected_state` | `test_bounded_verification.py::test_bounded_report_reads_the_real_s14_record_fold_summary_and_panel_artifact` (a REAL panel-grain regime run's stage result under the persisted name) + `::test_bounded_report_carries_typed_child_skips_from_the_s14_record` |
| B-02 | MEDIUM | **FIXED-VARIANT (documented; no policy change)** | the numerical B1 gates are stated for the benchmark's 200 events/path shape (50,000 rows per block); the writer is block-bounded for every shape; a row-based flush guard is a future versioned capacity-policy change requiring owner approval (DEV-HB-32; `CAPACITY_BENCHMARKS.md` "Shape parameters") | — |
| B-03 | MINOR | **FIXED** (S08 sidecar: main; reader: Fix-E) | S08 persists `fold_summary.json` `{fold_count, valid_fold_count, invalid_reasons, trading_day_count}`; the bounded report reads it (else the regime `fold_sample_adequacy.json` preview outcome; else `unexpected_state`) and the explanation regex is gone — a lying explanation is ignored (DEV-HB-31) | `test_hardening_fix_round.py::test_s08_persists_a_typed_fold_summary_sidecar`; `test_bounded_verification.py::test_fold_outcome_comes_from_the_typed_fold_summary_not_the_explanation` |
| B-04 | MINOR | **FIXED-VARIANT (Fix-E)** | the panel component loads the PERSISTED panel artifact (`load_context_bar_panel_artifact` + `load_context_bar_panel_frame`) and records `row_count`, `valid_row_count`, `typed_null_reason_counts`; `typed_insufficiency` (every row carries a typed reason) is now reachable; the failure-string branch is gone | `test_bounded_verification.py::test_bounded_report_reads_the_real_s14_record_fold_summary_and_panel_artifact` (a real panel-grain regime run) |
| B-05 | MINOR | **FIXED (Fix-E, test)** | pipeline-level proof: a FULL-development charter carrying a real bundle refuses at S00 — unmarked store, foreign namespace, stale witness after `publish_supersession` — with no stage run and no child row; a bound bundle passes S00's authority check (S00 completed; explanation names the bound namespace). Fix-E's observation (the `store_namespace_missing` message embedded an absolute path the sanitized S00 explanation withholds) was closed by naming only the root's last segment | `test_pipeline_authority_seams.py` (3) |
| B-06 | MINOR | **FIXED** | `WorkerPolicy.max_workers` before-validator: bool / str / non-int and every value other than 1 (including 0) raise the typed reason (DEV-HB-30) | `test_worker_policy_accepts_only_the_int_one_with_the_typed_reason` (0, 2, True, "1", 1.0) |
| B-07 | MINOR | **FIXED (Fix-E, test)** | a direct test of the summary byte-budget refusal (generous row budget, tiny byte budget: the written-file byte branch and the empty-table byte branch refuse typed; temp dirs cleaned; nothing published; a generous byte budget passes); the code was already correct | `test_stratified_prop_external_aggregation.py::test_byte_budget_refuses_before_publication_with_a_generous_row_budget` |
| B-08 | MINOR | **FIXED (Fix-E, test)** | a direct test of the writer's early `path_count` overrun (an ordered stream longer than the declared count refuses "exceeded while streaming" before any flush / manifest); the code was already correct | `test_event_detail_streaming.py::test_iterable_form_refuses_a_path_count_overrun_while_streaming` |
| B-09 | MINOR | **ACCEPTED (documented)** | a single oversized simulation is fully aggregated (memory-limited, spilling) before the exact row-budget refusal — memory-safe, refusal before publication, wasted work only (DEV-HB-33) | — |

Both reviewers' access-safety verdicts stand after the round: no fix
touched a data-access module, constructed a real source path, or wrote under
`data/` (`find data -type f -newermt "2026-09-02 00:00"` → 0 at the gate;
`ACCESS_SAFETY_EVIDENCE.md`).
