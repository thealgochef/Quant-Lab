# R6.1 — Adversarial Review Resolution

Every finding dispositioned; fixes verified by the re-run suites (final
counts in `TEST_RESULTS.md`). "FIXED" = code/test change in this release;
"ACCEPTED" = recorded without code change (with rationale); "REFUTED" =
disproven with evidence. Reviewer 1 = contract fidelity (F1–F16); Reviewer 2
= safety/access/immutability (S1–S12). Overlaps: S2 ⊃ F8 (supersession
fail-open / crash safety), F16 ∩ S8 (evidence-document honesty), F16 ∩ S12
(the D15 writer's memory profile), F13 ∩ S1 (S05 chart selection / binding),
F2 ∩ S6 (the report gate trusting the status label / skipping owner
authorization).

Session note: the fix round was executed by four parallel workstreams
(pipeline seam / stratification / governance / D15 writer + hygiene) on the
post-review tree; their first attempt was killed mid-edit by a session rate
limit and re-run from the half-applied state (`_PROGRESS_CHECKPOINT.md`).
Every disposition below states what the code does NOW.

| # | Sev | Disposition | Resolution |
|---|---|---|---|
| F1 | MAJOR | **FIXED** | `execute_regime_protocol(candidate_as_of_source: RegimeObservationSourceRef \| None)`: `load_candidate_as_of_source` refuses anything but a `bundle_feature_view` ref (`TypeError` "not evidence" for a `(str, frame)` tuple or a bare string; `ValueError` for a panel-kind ref) and verified-loads it through `load_regime_observations`; the as-of instants come from the LOADED frame; `candidate_as_of_source_ref` is stamped by `candidate_as_of_source_reference(observations)` = `<source_kind>:<loaded envelope id>` and the payload field is pattern-checked (`CANDIDATE_AS_OF_SOURCE_REF_PATTERN`); the candidate grain refuses the parameter. S09a passes the PERSISTED primary bundle-view id, never `context.bundle_frames`. Tests: `test_executor_end_to_end_panel_grain_assigns_candidates_pit` (tuple / bare string / wrong-kind ref / unknown id refused; the stamped ref equals `bundle_feature_view:<view_id>`), `test_candidate_as_of_source_ref_is_a_loaded_artifact_line_never_a_caller_string`. Panel-grain OOS artifact ids move where the ref differed. DECISIONS_TAKEN #95. |
| F2 / S6 | MAJOR / MEDIUM | **FIXED** | D6 on every path: `persist_regime_promotion` refuses STRATIFICATION_READY unless `assessment.payload.coverage.coverage_gates_passed and oos_assignment_available` ("structurally requires … (D6)"); `resolve_report_gate` re-derives the same gate from the LOADED assessment for any STRATIFICATION_READY-or-later decision (never the status label) and, in the `needs_owner` branch, re-runs the FULL `assert_owner_decision_authorizes(… transition, as_of=decision.decided_at, run_scope, supersession_chain)` under the new keyword-only `run_scope` (threaded from `StratificationInputs` by the service; the report stage passes the run's effective scope). Descriptive classes still load no owner (`authority_source="s10_structural"`). Tests: `test_stratification_ready_needs_coverage_gates_and_oos_at_persistence` (the §9.2 row), `test_the_structural_gate_is_rederived_from_the_loaded_assessment` (a decision minted around the store API is refused by the gate), `test_modeled_classes_re_run_owner_authorization_under_the_run_scope[feature_only\|cohort_model]`, `test_descriptive_classes_take_the_structural_gate_and_load_no_owner`, `test_a_superseded_owner_artifact_is_refused_at_the_gate` (new `test_regime_stratification_gate.py`); `test_regime_store.py` (e) rebuilt through BLOCKED. DECISIONS_TAKEN #91. |
| F3 | MAJOR | **FIXED** | The per-event JSON is gone; a `stratified_prop` report carries the plan's `account_event_regime_summary.parquet`: `regime_stratified_prop.py` streams the D15 partitions (vectorized attribution per partition; no event retained across partitions; cross-partition `path_instance_id` disjointness enforced) into one row per (simulation, path, regime stratum / typed reason, event type) with `event_count`, `amount_sum`, first/last ordinal + instant, keyed by the exact `RegimeAssignmentEvidenceRef` + simulation ids; registered schema `ACCOUNT_EVENT_REGIME_SUMMARY_SCHEMA` v1; ZSTD Parquet with the D15 writer settings (byte-deterministic); budget `account_event_regime_summary_budget_v1` (5,000,000 rows / 268,435,456 bytes; stamped in `STRATIFICATION_REGISTERED_BUDGETS`, rendered in the Regime Lane stamp table) → typed `EventRegimeSummaryBudgetError` BEFORE publication; four envelope extras `account_event_regime_summary_{sha256,bytes,rows,schema_hash}` (final ruling 7 — the `detail_sha256` precedent); `persist_regime_stratified_report(..., summary=)` refuses a mismatch / missing sidecar; `load_regime_stratified_report_summary` rehashes bytes / rows / schema; the JSON detail keeps aggregates only. Tests: `test_stratified_prop_attributes_by_source_trade_never_by_synthetic_clock` (exact aggregates, evidence keys, order, no `events` key, byte-determinism, round-trip), `test_event_regime_summary_budget_refuses_before_publication`, the tamper / missing-binding / non-prop-class refusals in `test_service_persists_descriptive_reports_and_records_modeled_delivery`. Every stratified report id moves. DEV-R6.1-16; DECISIONS_TAKEN #92. |
| F4 | MAJOR | **FIXED** | `test_s14_performs_zero_fitting` is parametrized over `candidate`, `candidate_supervised`, `panel_supervised` (KMeans / LogisticRegression / CatBoostClassifier / sklearn `Pipeline.fit` monkeypatched to raise inside S14; the supervised shapes fit at S09b/S09c earlier in the same run); new `test_model_bearing_panel_run_executes_s09b_s09c_pit_on_the_frozen_authority` (panel two-pass E2E: same persisted panel + protocol, frozen authority verified, S09b under the panel fold set, S09c both studies, S10 `feature_eligible`, S14 zero fitting, S15 "gates passed"); new `test_supervised_classes_without_s07_or_a_label_policy_are_refused` (`stage_plan_problems`, `regime_study_block_reason`, `PipelineSemanticSpecPayload`). The fixture's panel bars now plant regimes per trading day so the panel assessment passes every gate (DEV-R6.1-3 amended). DECISIONS_TAKEN #100. |
| S1 / F13 | MAJOR / MINOR | **FIXED** | `_verified_panel_chart` asks the seam for `chart_id` and refuses (typed; S05 fails closed; nothing persisted) when `replay.artifact_id != chart_id` or the pair differs; the materialized panel's `replay_chart_artifact_id` is re-checked; the S05 sidecar records the LOADED `replay_chart_artifact_id`, `panel_chart_selection_policy_id`, `panel_source_core_replay_id`, `replay_chart_source_pair_sha256`. S04 produces one chart per child, so `select_panel_chart` applies the declared `PANEL_CHART_SELECTION_POLICY_ID = panel_chart_lowest_core_replay_id_v1` (refuses no chart / more than one chart on that child). The fixture seam writes a real-shaped chart per child in S04 and loads exactly the requested id through `load_verified_replay_chart_artifact(expected_pair=…)`; `foreign_context_bar_source` reproduces the reviewer's probe. Tests: `test_descriptive_panel_study_materializes_the_panel_from_the_verified_chart` (the panel binds `chart_id`, the chart is one S04 wrote, `panel_source_core_replay_id == min(children)`), `test_s05_refuses_a_seam_that_serves_another_verified_chart` (S05 `failed`, no `context_bar_panels` store, S08/S09/S14 pending). DEV-R6.1-18; DECISIONS_TAKEN #96. |
| S2 / F8 | MAJOR / MINOR | **FIXED** | The supersession line is appended (lock-guarded, idempotent on `(superseded, replacement)`, independent of "already persisted") BEFORE `save_or_reuse_envelope` publishes the replacement; every line is a `SupersessionRecord` hash-chained `prev_line_sha256 → line_sha256` (genesis `0*64`) with the head `{line_count, head_sha256}` in `SUPERSESSIONS.head` (atomic `os.replace`); `load_supersession_chain` verifies the chain + head and that every line is backed by a verified replacement whose `supersedes` names the prior — an edited, reordered or deleted line, a missing head, or a dangling line (crash between append and publish) fails closed until the replacement is re-persisted (the same idempotent call repairs it); a replacement with weaker provenance than the prior is refused (`_PROVENANCE_RANK`). Tests: `test_supersession_line_precedes_publication_and_a_crash_fails_closed` (probe P1), `test_deleted_edited_or_headless_supersession_log_fails_closed` (probe P2: four distinct refusals; restored → verifies), `test_a_replacement_may_never_carry_weaker_provenance`, the pre-existing forged-line test, the idempotent re-persist test. Residual (DEV-R6.1-15): deleting the log AND the head together is undetectable without a store listing — the DEV-R6-8 trust boundary. DECISIONS_TAKEN #90. |
| S3 | MEDIUM | **FIXED (namespace guard; payload field deferred)** | `research_namespace_root` / `assert_run_scope_lawful_for_root` mirror P0-4 (a `search` segment that is not `search_test`): `persist_regime_promotion(run_scope="synthetic_fixture")` refuses there (`PermissionError`); synthetic-provenance owner artifacts are refused at persist AND at load (`load_owner_decision`) in that namespace, and the gate / store / `verify_frozen_authority` / block activation all load through it, so a synthetic-backed FEATURE_ELIGIBLE cannot exist in a research root. The CLI `chain` annotates every ratified row with `owner_evidence: verified (<provenance>) \| refused: <reason>`. `run_scope` is NOT recorded inside `RegimePromotionDecision` (identity-bearing; would have moved S10's decision ids mid-round) — recorded as DEV-R6.1-15. Tests: `test_synthetic_scope_and_provenance_are_confined_to_test_namespaces` (probe P3 on a tmp root shaped `…/data/ifvg_datasets/search/v1`; `search_test/v1` admits both), `test_the_synthetic_scope_is_confined_to_test_namespaces_at_the_gate`, CLI `test_synthetic_scope_is_refused_in_the_research_namespace`. DECISIONS_TAKEN #91. |
| S4 | MEDIUM | **FIXED** | S02 reuse branch: the re-derived tables are adopted ONLY when a persisted costed evaluation for THIS cost policy exists and is reproduced byte-for-byte; with none, the row stays `reused` with "reproduction unverifiable; the re-derived tables are not this run's evidence", the tables are not adopted, S03 evaluates no gates for it (its explanation preserved) and publishes no evaluation from them, and the child is out of the stratified reports. Test: `test_s02_reused_children_are_adopted_only_by_verified_reproduction` (second pipeline, same charter → every child "verified reuse by reproduction … reproduced its persisted costed evaluation"; third pipeline with a different `cost_points_round_turn` → "reproduction unverifiable", no `costed_evaluations` entry for that policy). DEV-R6.1-8 / DECISIONS_TAKEN #82 / `ACCESS_SAFETY_EVIDENCE.md` reworded; #98. |
| S5 | MEDIUM | **FIXED** | `MODEL_FEATURE_PROMOTION_REFUSAL` (one constant in `regime_contracts.py`; the CLI re-exports it as `MODEL_FEATURE_REFUSAL`) is raised by `persist_regime_promotion` for `status is MODEL_FEATURE` before any load; `MODEL_FEATURE` stays representable at the contract (ladder / role tests) but is unpersistable through every V1 path. Test: `test_model_feature_is_unpersistable_through_the_store` (probe P4; nothing written). DECISIONS_TAKEN #91. |
| F5 | MINOR | **FIXED (+ name mapping recorded)** | `test_arms_share_rows_labels_folds_on_comparison_row_id_with_distinct_views` → `…_across_distinct_bundles` (asserts one static view, distinct bundle / ladder ids, the three-rung ladder on both arms); new `test_challenger_arm_with_a_distinct_view_id_pairs_only_on_comparison_row_id` runs the challenger ladder over a view with a different `view_id` on the REAL fold-feature artifact and proves `assert_regime_cross_arm_identity` (now public) pairs on `comparison_row_id` with disjoint `oos_row_id`s and refuses a different fold set; `_STUDY_PROTOCOLS = DEFAULT_BUNDLE_LADDER_PROTOCOLS` (prevalence + logistic + CatBoost bundle) for the controlled regime study (`test_controlled_study_persists_and_reloads` asserts the bundle rung ran on both arms with a paired delta; the cohort fixture keeps the two linear rungs for runtime). The §9.1 name `test_decision_25_algorithm_snapshot_is_authorized` ≡ the three `algorithm_*` cases of the parametrized decision-values test — DEV-R6.1-12. DECISIONS_TAKEN #94. |
| F6 | MINOR | **FIXED** | `StratificationInputs.delivered_by` (class → exact S09c study id) → the service verifies each id by exact reload (`load_regime_controlled_study` / `load_regime_cohort_model_study`; a non-64-hex string refuses before any load) and records it in `StratificationOutcome.delivered_by`; a modeled class requested without a delivery is a typed refusal ("… this run recorded none for it …; nothing here promotes"); a class not requested has no entry. `regime_report_stage._delivered_by_s09c` takes the ids from this attempt's S09c results, else the run's OWN verified S09 stage record (never a listing); the S14 record and `load_regime_report_index` carry `delivered_by`; the Monitor's S14 table renders "delivered by S09c — <id>" rows beside report ids and refusals (the `regime_status_below_minimum` empty state stays for refusals only). Test: `test_service_persists_descriptive_reports_and_records_modeled_delivery`. DEV-R6.1-10 rewritten; DECISIONS_TAKEN #86 (amended), #92. |
| F7 | MINOR | **FIXED** | `_stage_s08_folds` derives `days` from the candidate view's observed trading days whenever a regime study is planned and builds the labeled folds from those SAME days; `s08_regime_folds` derives the schedule from the same source and records `trading_days_source = candidate_view_observed_days` + `days_without_labels`. "Authorized" = charter allowlist ∩ observed view days (DEV-R6.1-17). Test: `test_s08_derives_the_schedule_once_from_the_candidate_view_days` (label builder omitting the last day → S08/S09 completed, 55 authorized days, `days_without_labels == [last_day]`). DECISIONS_TAKEN #97. |
| F9 | MINOR | **FIXED** | `event_detail_frame` imports `EVENT_TYPE_PRECEDENCE` from `propsim.event_detail` (the local `_EVENT_PRECEDENCE` is deleted; an unregistered type refuses) and uses the record's own `trading_day` (required under `historical_calendar_clock_v1`, typed refusal when absent; null under the synthetic clock, which is never consulted). Test: `test_event_detail_frame_uses_d15_precedence_and_the_events_own_trading_day`. DECISIONS_TAKEN #92. |
| F10 / S11 | MINOR | **FIXED** | `--decided-at` removed from the CLI; `decided_at` = the previous decision's `decided_at` or, for a ratified status, the verified owner artifact's `effective_from` (whichever is later; `decided_at_source` reported) — `promote --to stratification_ready` therefore reproduces S10's exact decision id (a reuse). The store enforces the monotone chain (`decided_at >= previous.decided_at`); `decided_at >= approved_at` holds transitively (the artifact validator enforces `effective_from >= approved_at`; effectivity refuses `decided_at < effective_from` as "not yet effective"). Tests: `test_decided_at_is_derived_from_verified_artifacts_and_reproduces_s10` (the parser refuses `--decided-at`; `status == "reused"`, id == S10's), `test_decision_time_is_monotone_along_the_chain`, the updated `test_feature_eligible_requires_verified_owner_evidence_in_a_lawful_scope`. DEV-R6.1-15; DECISIONS_TAKEN #91. |
| F11 | MINOR | **FIXED** | ONE vocabulary in `regime_fold_features.py`: `HARD_ID_ENCODING_NONE = "none"`, `HARD_ID_ENCODING_CATEGORICAL = "fit_local_categorical_v1"`, `HARD_ID_ENCODINGS`, `HardIdEncoding` — imported by `RegimeStudyRequest`; S09b passes `request.hard_id_encoding` through unchanged (the `!= "none"` remap is gone). The supervised zero-fitting runs use `fit_local_categorical_v1` and assert the fold-feature artifact's `hard_id_encoding`, that `categorical_model_features ⊆ model_feature_names` and end with `_local_id`; `test_regime_fold_features.py` migrated to the new value. DEV-R6.1-7 rewritten; DECISIONS_TAKEN #100. |
| F12 | MINOR | **ACCEPTED (recorded)** | `panel_source_bar_incomplete` is the typed null the PIT rule assigns to a panel row invalidated under final ruling 3; no UI surface enumerates the reasons (`study_status.py` registers no per-reason empty state; the assignment view renders whatever `missing_reason` values the artifact carries), so no UI change was needed. DEV-R6.1-11. |
| F14 | MINOR | **FIXED (partial — deviation)** | `panel_event_assigner` verified-loads by exact id: the OOS assignment artifact → its protocol → the panel envelope + frame (`load_context_bar_panel_frame`) → every fit's assignment sidecar (`load_regime_fit_assignments`, concatenated); no in-memory run object. `build_reports` still consumes `context.tables_by_child` because the executed-trade tables are NOT a persisted artifact (a core replay persists identity only — `CoreStrategyReplayPayload` has no tables or stream hash): S14's trade tables are S02's this-run tables (fresh, or re-derived and adopted only by verified reproduction — S4). Proven by the panel E2E (`stratified_prop` over the persisted assigner) and the zero-fitting panel run. DEV-R6.1-18; DECISIONS_TAKEN #99. |
| F15 | MINOR | **FIXED (identity-stable)** | `regime_controlled_study.py` adds `FrozenTree` (read-only `Mapping`; nested maps → `FrozenTree`, lists → tuples; `__deepcopy__` returns self), `freeze_json` / `thaw_json`, `FrozenJson = Annotated[Any, PlainValidator(freeze_json), PlainSerializer(thaw_json)]`; `baseline_summary` / `challenger_summary` / `paired_deltas` are `ImmutableMap[str, FrozenJson]` and `RegimeCohortStratumRecord.paired_deltas` likewise. The serializer emits the plain JSON tree, so the canonical bytes — and the study ids — are unchanged (golden probe identical before / after). Test: `test_study_payload_values_are_deep_immutable_and_identity_stable`. DECISIONS_TAKEN #94. |
| F16 / S8 | MINOR | **FIXED (docs) + writer FIXED (see S12)** | `DEVIATIONS.md` and `ACCESS_SAFETY_EVIDENCE.md` headers now state that they were written before the round and corrected after it; the missing D15 summary (F3), the S08 observed-days semantics (F7 → DEV-R6.1-17), `panel_source_bar_incomplete` (F12 → DEV-R6.1-11) and the §9.1 name mapping (F5 → DEV-R6.1-12) are recorded; "the Launch handler stays the only spawn" → "no new spawn site (the two pre-existing `_spawn_pipeline_job` call sites are unchanged)"; "sha256 golden-tested" → "byte-unchanged per `git status` / `git diff HEAD --stat` (no source-hash test exists)"; the S02 and supersession statements are reworded to what the code does after S4 / S2. The writer's memory profile is fixed under S12. |
| S7 | MINOR | **FIXED** | `_render_preview` calls `derive_stage_plan_readiness(spec, store_root=Path(roots["store_root"]), run_scope=_effective_run_scope(charter_payload, spec))`; the Launch handler assembles the spec and calls `assert_stage_plan_launchable(…, store_root, run_scope)` BEFORE `save_charter` / the `pipeline_specs` envelope / any spawn; `_effective_run_scope` mirrors S00 (synthetic marker → `synthetic_fixture`, else `spec.run_scope`). Test: `test_launch_and_preview_verify_the_frozen_regime_authority_before_persisting` (Preview shows `blocked_capability` + "frozen regime authority refused"; Launch error, `spawned == []`, no `charters` / `pipeline_specs` store created). DECISIONS_TAKEN #100. |
| S9 | MINOR | **FIXED (lazy import; undeclared by design)** | `regime_service.py` no longer imports `threadpoolctl` at module level; `run_regime_protocol` imports it lazily and raises `RuntimeError(THREADPOOLCTL_MISSING_MESSAGE)` (names the dependency and the reason) when it is missing; `pyproject.toml` untouched (no new declared dependency this release — DEV-R6.1-13). Test: `test_regime_kernel_names_the_missing_thread_control_dependency`. DECISIONS_TAKEN #94. |
| S10 | MINOR | **FIXED** | `_acquire_lock`: `O_EXCL` inside the guarded loop, pid + timestamp in the lock, a lock older than `_LOCK_STALE_SECONDS` (60 s) is reclaimed, bounded wait `_LOCK_WAIT_SECONDS` (30 s) → `TimeoutError`; `main()` catches `(PermissionError, ValueError, LookupError, OSError, TimeoutError)` and prints one sanitized line, exit 2. Tests: `test_stale_supersession_lock_is_reclaimed_and_a_live_lock_times_out`, CLI `test_lock_timeouts_and_filesystem_errors_exit_sanitized`. |
| S12 | MINOR | **FIXED** | `search/store.py` gains the sidecar-producer protocol (`ProducedSidecar`, `SidecarProducer`, `write_produced_sidecar`; `save_envelope_immutable(..., sidecar_producer=)` runs the producer INTO the temporary publication directory before `os.replace`, re-hashes every produced file by streaming and refuses — nothing published, temp dir discarded — any bookkeeping disagreement, undeclared file, ghost record, reserved / duplicate / non-whitelisted name; produced sidecars enter the manifest merged and sorted with `extra_files`, byte-identical to the in-memory path; `save_or_reuse_envelope` on reuse re-runs the producer into a scratch directory and requires manifest-hash reproduction). `propsim/event_detail.py` builds one path block at a time column-wise (no per-row dicts), writes each ZSTD partition straight to the directory, hashes by streaming and releases the block; the row preflight is unchanged and the cumulative byte budget refuses at the offending block; the only whole-artifact state is a 32-byte-per-row `event_id` uniqueness index. `search_bridge.persist_account_simulation` hands the store a producer for v2 (detail, then `walk_summary.json` from the produced counts) and keeps the pure `extra_files` path for `none_v0`. Simulation ids, partitions and manifests are byte-identical. Tests: `test_streaming_writer_is_deterministic_and_reproduces_the_in_memory_parquet`, `test_writer_streams_one_path_block_at_a_time_and_refuses_early`, `test_store_refuses_producer_bookkeeping_lies_stray_and_reserved_files` (7 refusal cases), `test_reuse_with_a_producer_verifies_the_produced_bytes`, `test_none_v0_artifacts_are_never_widened` (through the producer path). DEV-R6.1-14; DECISIONS_TAKEN #93. |

## Post-fix verification

Lane suites after the round (mixed worktree, before the commit): governance
(`test_owner_decisions.py` + `test_regime_store.py` + `test_ifvg_regime_promotion_cli.py`
+ `test_regime_stratification_gate.py`) **53 passed**; the governance /
stratification / propsim / supervised-study / service / contracts suites
together **255 passed**; `test_pipeline_regime.py` **19 passed** (4:36);
`test_ifvg_pipeline_tab.py` **60 passed**; `test_account_event_detail.py`
13 passed; `test_regime_supervised_studies.py` 10 passed;
`test_regime_stratification.py` 15 passed. `ruff check src tests scripts`
clean; `git diff --check` clean over the tracked and the intent-to-add new
files (`_ruff_and_diffcheck.txt`). Release-final full-suite counts (the
working tree that becomes the R6.1 commit, once as-is and once with the
provider keys cleared): `TEST_RESULTS.md`.
