# R5B — Adversarial Review Resolution

Every finding dispositioned; fixes verified by the re-run suites (final
counts in `TEST_RESULTS.md`). "FIXED" = code/test change in this release;
"ACCEPTED" = recorded without code change (with rationale); "REFUTED" =
disproven with evidence.

| # | Sev | Disposition | Resolution |
|---|---|---|---|
| F1 | MAJOR | **FIXED** | Evidence↔identity binding is now VERIFIED at every seam: (a) `materialize_mbp1_features` rehashes every supplied day's canonical bytes against the source artifact's coverage `content_sha256`, refuses days the artifact does not cover, and refuses WITHHOLDING a covered anchor day (typing it `no_mbp1_partition` would be untruthful); (b) `save_mbp1_source_artifact` refuses sidecar bytes that contradict the coverage hashes before any store write; (c) the bundle-view seam and the controlled study now take the `Mbp1FeatureArtifactEnvelope` (not a bare id) and `verify_mbp1_feature_frame` rehashes the frame against `feature_table_sha256` before the id is pinned. Tests: `test_supplied_events_must_be_the_source_artifacts_evidence`, `test_saving_bytes_that_contradict_coverage_refuses`, `test_feature_frame_binding_is_verified_not_asserted`, `test_mbp1_bundle_view_verifies_and_binds_the_artifact` (tampered-frame refusal). The fabricated-id test fixtures were replaced by `feature_artifact_for_frame` (real envelopes with true table hashes). |
| F2 | MAJOR | **FIXED** | The bundle-parametrized ladder gained `bundle_evidence_ref`, folded into the hashed `feature_source` (and refused on the tier path); the controlled study passes the challenger's exact `mbp1_feature_artifact_id`. Two studies over different evidence artifacts now have different `challenger_ladder_id`s while the baseline arm's id is stable — asserted in `test_study_identity_is_deterministic`. DEV-R5B-3 updated. |
| F3 | MAJOR | **FIXED** | `test_same_ts_after_stage_event_cannot_change_any_feature` materializes under an EXACT-KEY `cutoff_builder` over a same-ts burst, mutates the after-trigger same-ts event's book values (bid_sz/ask_sz → 999), and asserts the full feature AND evidence frames byte-equal — plus a non-vacuity check (the snapshot used the trigger event's state, bid_sz 8). This is the materializer-level §3.8/DT §6.2 row, and the first exact-key exercise of the materializer. |
| F4 | MINOR | **FIXED** | `test_window_definition_changes_mint_a_new_resolved_block_id`: four one-field window mutations (min-count, trigger semantics, lower bound, missingness policy) each mint a different `resolved_feature_block_id`. |
| F5 | MINOR | **FIXED** (structural proof + honest docs) | The seal is structural — no `source_kind` channel exists in the package — so the shipped proof is now the API-surface scan `test_features_package_has_no_source_kind_ingress` (no public callable accepts `source_kind`), alongside the direct refusal test; `_require_no_legacy_provenance`'s docstring now states it is the executable boundary statement of a structurally sealed layer, not a runtime filter. |
| F6 | MINOR | **FIXED** | `StageEvidenceCutoff` validates BOTH timestamp elements of the exact key (infinity or any non-numeric refused — `test_exact_key_refuses_inf_in_either_timestamp_element`), and the no-+inf/no-nearest source scan widened to `search/pipeline.py`, `ml/supervised_ladder.py`, and `ml/controlled_feature_study.py` (which required rewriting `_jsonable`'s infinity DETECTION with `math.isinf` so no `float("inf")` construction exists anywhere in scope). |
| F7 | MINOR | **FIXED** | `events_stored` moved from the hashed payload to the ENVELOPE (a registered extra field, like the table hashes): byte-identical evidence has ONE artifact id across storage modes — `test_storage_mode_is_operational_never_identity`. The identity audit's placeholder generator became type-aware (bool extras). |
| F8 | MINOR | **FIXED** | `candidate_anchor_hash` now covers `setup_id`; `test_anchor_setup_relabel_is_a_new_materialization_identity`. |
| F9 | MINOR | **FIXED** | The bundle-view seam REFUSES a view candidate with no ROW in the feature artifact (a cohort misalignment — the materializer preserves every anchored row, so absence is never typed-null evidence): `test_mbp1_bundle_view_refuses_cohort_misalignment`; typed-null VALUES with rows preserved stay proven (`test_mbp1_bundle_view_carries_typed_null_values`); the join docstring now states exactly when the registered reason attaches. |
| F10 | MINOR | **ACCEPTED (declared deviation)** | DEV-R5B-1 stands: the real five-day MBP-1 control-flow half is blocked on the owner's `VerificationAuthorizationRef`, exactly like every real half since R1 (DEV-R5-6 precedent); the synthetic E2E proves the identical control-flow shape and the policy-gated real readers are implemented and refusal-tested. Recorded as an open blocker in `GATE_SUMMARY.md`. |
| F11 | MINOR | **REFUTED (precedent + determinism evidence)** | The controlled study is a RESULT artifact, not a materialization recipe: like `FrontierResult`, `InsightPanelPayload`, and `Mbp1CoverageReportPayload` (all landed pattern), result artifacts hash their content — CS §0.1 forbids a payload carrying hashes *of its own artifact*, which the study does not (its bulky diagnostics ride the envelope's `detail_sha256`). The drift concern is bounded: the arm ladder ids pin package versions inside their resolved protocol hashes, so a numerically different sklearn produces DIFFERENT ladder ids first — the summaries can never silently diverge under one input identity. No change. |
| S1 | MEDIUM | **FIXED** | `read_mbp1_partition_frame` now refuses any actual column carrying a book level beyond 00 (`_refuse_deeper_book_columns` — level-suffix `_01…_99` or the deep-book regex) BEFORE any row decodes, and reads lawful files through the pinned column projection (`pq.read_table(columns=…)`) so nothing beyond the mbp-1 contract is ever materialized. `test_deeper_book_parquet_is_refused_and_reads_are_column_projected`. |
| S2 | MINOR | **FIXED** | `DEEP_BOOK_IDENTIFIER_REGEX` widened to `mbp[\W_]?(?!1(?!\d))\d+` — every depth ≠ 1 unrepresentable (mbp2…mbp9 included) while `mbp1`/`mbp-1` stay lawful; the plan's mandated pattern is a strict subset (deviation note in DEVIATIONS/DECISIONS_TAKEN context). `test_deep_book_regex_covers_every_depth_beyond_one`. |
| S3 | MINOR | **FIXED** | `build_mbp1_source_artifact_from_paths` refuses `access_policy=None` and an empty day set at the TOP of the function — the degenerate zero-partition "real" artifact is unconstructible. `test_real_builder_refuses_no_policy_and_empty_days`. (The F5 half of this finding resolved as F5.) |
| S4 | MINOR | **ACCEPTED (documented)** | All out-of-list files are enumerated with rationale in `FILES_TOUCHED.md` (the five collateral test files are the release-gated R5→R5B inversions; D-046 is the decision entry the R5-era reservation note promised — DEV-R5B-4); the staging script's commit-vs-worktree behavior is called out for the owner in `FILES_TOUCHED.md`/`GATE_SUMMARY.md` (same mechanism as R4/R5). |
| S5 | MINOR | **ACCEPTED (recorded)** | The pre-existing Trade-Lab worktree modifications (user-owned, mtimes ≤ 2026-07-31) are acknowledged in `ACCESS_SAFETY_EVIDENCE.md`; nothing in this release touches that lane. |
| S6 | MINOR | **FIXED** | The auto-fill moved into the provider layer (`study_providers.mbp1_stage_evidence_defaults`) — which also resolved the FUX raw-override source-scan failure the widened suite caught — and a store-VERIFICATION failure now returns a note the pipeline surface renders as a sanitized error instead of a cosmetic blank. |

## Post-fix verification

The full affected re-run after all fixes: search-lane + ml-lane suites
**438 passed**; pipeline E2E + AppTests + FUX scans **55 passed**; ruff
clean. Release-final full-suite counts: `TEST_RESULTS.md`.
