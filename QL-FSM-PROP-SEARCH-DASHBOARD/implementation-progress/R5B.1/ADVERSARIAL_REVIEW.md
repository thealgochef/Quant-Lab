# R5B.1 — Adversarial Review (two independent read-only reviewers)

Per kickoff §10: two independent reviewers (contract fidelity; safety/access/immutability) attempted to falsify the uncommitted R5B.1 change set against `R6.1_IMPLEMENTATION_PLAN.md` §6.I / D12 / §9.1, the owner's planning decision Q1, plan-review correction 4, final corrections #2/#7, the kickoff §4/§8/§9 rules, and the CONTRACTS_AND_SCHEMAS / TEST_MATRIX MBP-1 rows. Reviewers produced findings only (no edits; every probe ran on synthetic fixtures under scratch/tmp roots; no real source path was constructed). Both reports are reproduced VERBATIM below; dispositions and post-fix verification: `ADVERSARIAL_REVIEW_RESOLUTION.md`.

---

# R5B.1 Adversarial Review — Reviewer 1 (CONTRACT FIDELITY)

**Subject**: the uncommitted R5B.1 working-tree change on `feature/ifvg-prop-robust-config-search-v1` (HEAD `179a2c9`), MBP-1 lane only.
**Mode**: read-only; every probe is synthetic (`tests/agents/ifvg_search/mbp1_fixture.py`); probe scripts and raw outputs live in `scratchpad\review_r5b1_reviewer1\` (`probe_*.py`, `out_*.txt`, `ids_head.json`, `ids_work.json`, `pytest_targeted.txt`, `ruff.txt`). HEAD identities were computed from a `git archive HEAD src` extraction in the scratch dir (no repo write). No `data/databento/` path was constructed, listed, or read; no protected/sealed date literal was used outside the fixture's own unrepresentability test.

**Tree stability note**: every R5B.1 file in scope has an mtime before the review started (last: `mbp1_coverage_evidence.py` 21:41:16); during the review the parallel R6.1 workstream touched out-of-scope files (`ml/controlled_feature_study.py`, `ml/supervised_ladder.py` at 22:06:40, plus new untracked regime/fold modules). The suite run below (finished before 22:03) and every probe reflect the R5B.1 files as reviewed; nothing in this report depends on the R6.1 files.

## Verdict: 0 blockers, 4 majors, 8 minors

Targeted suites: **119 passed, 0 failed** (98 s) across `test_mbp1_coverage_evidence.py`, `test_mbp1_materializer.py`, `test_mbp1_schemas_and_source.py`, `test_feature_blocks.py`, `test_identities.py`, `test_pipeline_run.py`, `test_controlled_feature_study.py`. `ruff check` on the 21 R5B.1 files: `All checks passed!`. `git diff --check` on the lane: clean. The green suite does not cover the findings below; each was reproduced by running the code.

---

### F1
**Finding**: Positive completeness is not bound to the partition bytes it certifies, and the "verified compiler" accepts an unverified in-memory report. `Mbp1CompletenessCompilationReportPayload.verified_partition_refs` is never compared with the partition's `content_sha256`; `compile_mbp1_partition_gap_manifest` takes any envelope object (persisted or not); a report constructed in memory with `provenance="owner_reviewed"`, an arbitrary `owner_review_decision_id`, and refs that match nothing yields `verified_complete_outside_intervals`, and the resulting `Mbp1PartitionEvidence(provenance=OWNER_REVIEWED)` passes `build_mbp1_source_artifact_from_paths`' provenance gate (the only gate on the real path). The same evidence object certifies two different event streams as `evidenced_complete`.
**Severity**: MAJOR
**Contract citation**: `FINAL_PLAN_CORRECTIONS_2026-08-28.md` #2 ("positive completeness comes only from a verified compiler"); `R6.1_IMPLEMENTATION_PLAN.md` §6.I ("`Mbp1CompletenessCompilationReport{source_inventory_id, verified_partition_refs, …}` is itself immutable and verified-loaded. Positive completeness can be emitted only by `compile_mbp1_partition_gap_manifest` from those verified inputs"), §5 D12 ("Positive completeness is produced only by a verified compiler over source inventory/manifest evidence"); kickoff §9 identity rule "a caller-provided string is NOT evidence" (plan D2 precedent).
**Code/test evidence**: `mbp1_coverage_evidence.py:366-392` (report payload: refs only length-checked; `owner_review_decision_id` any non-empty string), `:442-478` (compiler consumes the envelope without store verification), `:494-516` (`_scope_agrees` checks partition key + utc_date only), `mbp1_source_artifact.py:371-411` (`_partition_coverage` never consults `verified_partition_refs`), `:626-632` (real builder refuses only `SYNTHETIC_FIXTURE`). Probe `probe_span.py` sections E/F:
```
=== E. completeness report NOT bound to the partition content hash ===
partition content_sha256       03827094cfa381078c552366f277b65742a5ecb66c162380e2a31458ec5c102b
report verified_partition_refs ('6666666666666666666666666666666666666666666666666666666666666666',)
status evidenced_complete -> positive completeness accepted with refs that match nothing
different bytes: True status evidenced_complete coverage 1.0
=== F. in-memory 'owner_reviewed' report passes the real builder's provenance gate ===
manifest assertion: verified_complete_outside_intervals provenance: owner_reviewed
real builder: REACHED resolve_source_path for 2026-01-13: provenance gate passed
```
`load_verified_partition_evidence` (`:898-937`) is the only store-verified route and has **no caller outside the test file** (`grep` over `src`/`scripts`).
**Reproduction steps**: `PYTHONPATH="src;." python scratchpad\review_r5b1_reviewer1\probe_span.py` (sections E, F). F uses a sentinel policy whose `resolve_source_path` raises, proving the provenance gate was already passed before any path resolution.
**Suggested resolution**: (a) in `_partition_coverage`, refuse a manifest whose compilation report's `verified_partition_refs` does not contain the partition's `content_sha256` (and record the matched ref in `evidence_sources`); (b) make the real path consume evidence by **store id** (`manifest_id` per physical partition → `load_verified_partition_evidence`) instead of in-memory `Mbp1PartitionEvidence`; (c) for `owner_reviewed` provenance require `owner_review_decision_id` to reference a verified owner-evidence artifact (or, until R6.1's verified-owner-evidence workstream lands, refuse `owner_reviewed` outright and document that only the store-verified loader may mint it).

### F2
**Finding**: The trading-day view asserts `evidenced_complete` and the materializer emits VALID features over sub-spans that no partition evidences. Two physical partitions of one day whose expected spans leave an undeclared hole ([0,200 s] and [400,600 s]) produce day status `evidenced_complete`, coverage 1.0; a candidate whose inversion (240 s) and entry (360 s) lie inside the hole receives valid `ofl_snap_inversion`, `ofl_snap_entry`, and `ofl_win_inversion_entry` (the snapshot is computed from the stale last pre-hole event). Separately, events outside every declared partition span are silently dropped (4 of 7 events counted; an out-of-span `F_MAYBE_BAD_BOOK` row is not even counted in `flag_counts`) with no coherence error — the "verified" span and the data contradict each other silently.
**Severity**: MAJOR
**Contract citation**: `R6.1_IMPLEMENTATION_PLAN.md` §5 D12 / §6.I ("a partition without partition-scope evidence is `completeness_unknown`"; "Positive completeness is produced only by a verified compiler …"); `FINAL_PLAN_CORRECTIONS` #2 ("physical partition bounds define the denominator"); `OWNER_PLAN_REVIEW_CORRECTIONS` §4 (coverage calculation must define "clipping to the expected partition/session span; missing head and tail handling").
**Code/test evidence**: `mbp1_source_artifact.py:510-553` (`day_coverage_views`: weakest-status + duration weighting over declared partitions only; no tiling check), `:321-331` (`_partition_slice` drops out-of-span events silently), `mbp1_feature_materializer.py:426-443` (`outside_span` brackets by the day's first/last in-span event, so a middle hole is never "outside"), `:452-460` (only `gap_intervals` type a window). Probe `probe_span.py` sections A/B:
```
=== A. events (incl. a bad-book flag) OUTSIDE the verified partition span ===
partition row_count 4 (day has 7 events)
status evidenced_complete coverage 1.0 flag_counts {... 'F_MAYBE_BAD_BOOK': 0 ...}
=== B. two partitions of one day with an UNDECLARED HOLE between them ===
day status evidenced_complete coverage 1.0 partitions 2
row counts per partition [5, 3] (day events: 10 )
ofl_snap_inversion: valid=True reason=None
ofl_snap_entry: valid=True reason=None
ofl_win_inversion_entry: valid=True reason=None
snap_entry spread_ticks 0.9999999959982233 bid_sz 6.0
```
**Reproduction steps**: `PYTHONPATH="src;." python scratchpad\review_r5b1_reviewer1\probe_span.py` (sections A, B).
**Suggested resolution**: in `build_mbp1_source_artifact`, require a day's partition spans (clipped to the authorized session span) to be pairwise disjoint and **contiguous**, or synthesize an explicit `completeness_unknown` pseudo-partition for every uncovered sub-span so the weakest-status rule fails the day closed; refuse (or at minimum record as a typed diagnostic and type the affected windows `coverage_evidence_unavailable`) any day whose events fall outside the union of declared spans.

### F3
**Finding**: Overlapping / duplicated physical-partition spans within one trading day are accepted and dilute declared gaps. A 600 s partition with a declared 300 s gap (coverage 0.5) plus a second evidence row with the identical span and no gap yields day coverage **0.75** (`declared_gaps`); the day view's `row_count` doubles (20 for 10 events). Contiguous partitions `[0,300]`/`[300,600]` double-count the boundary event (inclusive `<=` end in `_partition_slice`) — 4 rows for 3 events.
**Severity**: MAJOR
**Contract citation**: `R6.1_IMPLEMENTATION_PLAN.md` §6.I ("Multiple UTC partitions of one trading day are measured separately and then duration-weighted; **the full trading-day span is never repeated per partition**"); DECISIONS_TAKEN #70 and D-048 repeat the claim; §9.1 `test_multi_utc_partition_denominators_and_channel_wide_bad_book_scope` ("without double-counting span").
**Code/test evidence**: `mbp1_source_artifact.py:486-490` (only the `physical_partition_key` must be unique — no span-overlap check), `:321-331` (closed `[start, end]` slice while intervals/session span are half-open), `mbp1_coverage_evidence.py:852-864` (weights by the raw per-partition span). Probe `probe_overlap.py`:
```
single partition with a 300 s declared gap: day coverage 0.5 declared_gaps
same + a DUPLICATE-span partition without the gap: day coverage 0.75 declared_gaps | day gap_intervals [(0.0, 300.0)] | row_count 20
contiguous [0,300] + [300,600] over 3 events -> per-partition row_count [2, 2] day row_count 4
```
**Reproduction steps**: `PYTHONPATH="src;." python scratchpad\review_r5b1_reviewer1\probe_overlap.py`.
**Suggested resolution**: refuse overlapping expected spans within a day at `build_mbp1_source_artifact` (after clipping to the session span); make `_partition_slice` half-open (`ts_event < end`, matching `Mbp1DeclaredGapInterval` and `authorized_session_span_ns`); weight by the disjoint clipped spans; extend the §9.1 multi-UTC test with an overlap-refusal assertion.

### F4
**Finding**: The bounded real-data diagnostic is documented as characterizing "open-interval facts" and "declared manifests / condition records present", but its real path structurally cannot: `scripts/ifvg_mbp1_coverage_diagnostic.py:_real` hardcodes `coverage_evidence=None`, so every partition is built by `_unknown_partition_coverage`, which never calls `bad_book_uncertainty_intervals`. The persisted row reports `open_uncertainty_to_partition_end=False`, `declared_gap_count=0`, `gap_manifest_present=False`, `dataset_condition_present=False` while `flag_counts["F_MAYBE_BAD_BOOK"]=1` — a "False" that means "not computed" but reads as "no open uncertainty". No CLI seam exists to load persisted manifests/condition records (`load_verified_partition_evidence` has no non-test caller).
**Severity**: MAJOR
**Contract citation**: `OWNER_PLANNING_DECISIONS_2026-08-28.md` Q1 item 6 ("The diagnostic must characterize actual partition-quality evidence"); `R6.1_IMPLEMENTATION_PLAN.md` §6.I bounded-diagnostic bullet ("per partition: row counts, flag counts, **open-interval facts**, sequence-jump and ts_recv-gap distributions, **declared manifests / condition records present**, completeness status"); `R5B.1\stage_shared_docs.py` ARCH_APPEND, `docs/DECISIONS.md` D-048 and DECISIONS_TAKEN #72 make the same claim.
**Code/test evidence**: `scripts/ifvg_mbp1_coverage_diagnostic.py:147-154` (`coverage_evidence=None`), `mbp1_source_artifact.py:332-370` (`_unknown_partition_coverage`: `declared_gap_intervals=()`, `open_uncertainty_to_partition_end=False`, no interval computation), `mbp1_coverage_diagnostic.py:105-136` (`_row` copies those fields verbatim). Probe `probe_diag_and_misc.py` §1 (artifact built exactly as the real path builds it, over a flagged stream):
```
"evidence_scope_level": null, "completeness_status": "completeness_unknown",
"declared_gap_count": 0, "open_uncertainty_to_partition_end": false,
"gap_manifest_present": false, "completeness_report_present": false, "dataset_condition_present": false,
"flag_counts": [... ["F_MAYBE_BAD_BOOK", 1] ...]
```
**Reproduction steps**: `PYTHONPATH="src;." python scratchpad\review_r5b1_reviewer1\probe_diag_and_misc.py` (section 1).
**Suggested resolution**: add a verified-evidence seam to `_real` (`--partition-manifest-id` per physical partition → `load_verified_partition_evidence`, plus a verified scope/span source) so the real run can compute interval facts under a verified span; make `open_uncertainty_to_partition_end` tri-state (`True`/`False`/`not_computed_no_partition_scope`) or omit it when `evidence_scope_level is None`; until the seam exists, correct ARCH_APPEND/D-048/#72 to state that the real run characterizes flag counts and sequence/ts_recv diagnostics only.

### F5
**Finding**: A documented recovery boundary at exactly the flagged event's `ts_event` closes the interval **before** the flagged event whenever the uncertainty start is earlier than the flag (interval `[start, flagged_ts)`), contradicting the in-code rule "a recovery boundary AT the flagged instant still leaves the flagged event itself uncertain: keep a minimal [start, flag+1)" (that branch only fires when `end_ts <= start_ts`, i.e. when the prior row shares the timestamp). With the flag on the first in-scope event and the partition starting 1 s earlier, the flagged event is **admitted into a VALID snapshot** (`bid_sz=99` read from the flagged row).
**Severity**: MINOR
**Contract citation**: `OWNER_PLAN_REVIEW_CORRECTIONS` §4 / plan §6.I ("closes only at a documented recovery boundary"; the flagged record is by definition inside the uncertainty); DECISIONS_TAKEN #69.
**Code/test evidence**: `mbp1_coverage_evidence.py:701-712`. Probe `probe_badbook.py` 4a/4b and `probe_boundary_at_flag.py`:
```
--- 4a boundary AT flag (200s), prior at 0s
  [+0.0s, +200.0s) ... end_kind=vendor_recovery_event   flagged instant 200s inside interval? False
--- 4b boundary AT flag (200s), prior shares ts 200s
  [+200.0s, +200.0s) ...                                  flagged instant 200s inside interval? True
intervals: [(99.0, 100.0, 'snapshot_recovery')] status declared_gaps
ofl_snap_htf_tap valid: True reason: None bid_sz from the FLAGGED event: 99.0   admitted: 1
```
**Reproduction steps**: `PYTHONPATH="src;." python scratchpad\review_r5b1_reviewer1\probe_boundary_at_flag.py`.
**Suggested resolution**: `end_ts = max(end_ts, flagged_ts + 1)` unconditionally (a boundary at the flag instant closes after the flagged record), and add a test with the flag on the first in-scope event.

### F6
**Finding**: A documented recovery boundary lying **after** `partition_expected_end_ts` "closes" the interval (kind `vendor_recovery_event`); the interval is then clipped to the partition end, so it runs to the partition end with **no** recovery inside the partition, yet `open_uncertainty_to_partition_end=False`. The coverage number is right; the typed fact and the fail-closed label are wrong.
**Severity**: MINOR
**Contract citation**: plan §6.I ("without one it runs to `partition_expected_end_ts`"); ARCH_APPEND / D-048 ("otherwise runs to the partition end (`open_uncertainty_to_partition_end`, fail closed)").
**Code/test evidence**: `mbp1_coverage_evidence.py:701-708` (any boundary with `ts >= flagged_ts` is accepted, no upper bound), `:814-817`. Probe `probe_badbook.py` 7:
```
--- 7 boundary AFTER partition end (900s; span ends 600s) raw
  [+0.0s, +900.0s) ... end_kind=vendor_recovery_event
--- 7 merged/clipped
  [+0.0s, +600.0s) ... end_kind=vendor_recovery_event
  open_uncertainty_to_partition_end = False | interval end == partition end? True
```
**Reproduction steps**: `PYTHONPATH="src;." python scratchpad\review_r5b1_reviewer1\probe_badbook.py` (case 7).
**Suggested resolution**: accept only boundaries with `flagged_ts <= ts <= partition_expected_end_ts`; otherwise `PARTITION_END_FAIL_CLOSED`. Also bind `Mbp1RecoveryBoundary` to a scope/partition (it currently carries none).

### F7
**Finding**: Dataset-condition records are not scope-checked: a record for another dataset **and** another UTC date (`OTHER.DATASET`, `2026-01-14`, `degraded`) attaches to a `SYNTHETIC.FIXTURE / 2026-01-13` partition without error and drives `dataset_condition_status`. Recovery boundaries likewise carry no scope. The plan's "scope mismatch evidence is ignored with a diagnostic" is implemented only for the gap manifest.
**Severity**: MINOR
**Contract citation**: plan §6.I ("scope mismatch evidence is ignored with a diagnostic"; dataset-condition records are "coarse dataset/UTC-date evidence"); D12 ("only verified evidence at an explicit scope level").
**Code/test evidence**: `mbp1_coverage_evidence.py:494-516` (`_scope_agrees` checks the manifest only). Probe `probe_span.py` D:
```
ACCEPTED foreign condition record; scope dataset= SYNTHETIC.FIXTURE 2026-01-13 record= OTHER.DATASET 2026-01-14
```
**Reproduction steps**: `probe_span.py` section D.
**Suggested resolution**: in `_scope_agrees`, require `dataset_condition.dataset == scope.dataset` and `dataset_condition.utc_date == scope.utc_date`; give `Mbp1RecoveryBoundary` a `physical_partition_key`/`utc_date` and check them.

### F8
**Finding**: The materializer accepts a superseded resolution and stamps an inconsistent identity: `materialize_mbp1_features(..., resolved_block=MBP1_ACTIVATION_ENVELOPE)` (the exported R5B v1 resolution) produces an artifact with `resolved_feature_block_id = <v1 id>` but `formula_version = ifvg_order_flow_mbp1_formula_v2` / `materializer_version = mbp1_feature_materializer_v2` (both are read from module constants, never from the block). R5B had one resolution so this was unreachable; R5B.1 exports two and adds no version-agreement check.
**Severity**: MINOR
**Contract citation**: plan §7 (identity consequences), `DELTA_TAXONOMY.md` §6.2 ("Changing these … creates a new `FeatureBlockResolutionPayload` and resolved block ID" — the resolution's formula/materializer version IS the identity), DECISIONS_TAKEN #71.
**Code/test evidence**: `mbp1_feature_materializer.py:523-527` and `:662-663` (constants), `:335-349` (`_validate_window_specs` checks names only). Probe `probe_ids_downstream.py`:
```
v1_activation feature_artifact 498ff10493ce251b formula ifvg_order_flow_mbp1_formula_v2 materializer mbp1_feature_materializer_v2 ...
NOTE: materializing under the HISTORICAL v1 block is still accepted by materialize_mbp1_features
```
**Reproduction steps**: `PYTHONPATH="src;." python scratchpad\review_r5b1_reviewer1\probe_ids_downstream.py`.
**Suggested resolution**: in `_validate_window_specs` (or a new `_validate_resolution`), refuse a block whose `payload.formula_version`/`materializer_version` differ from `MBP1_FORMULA_VERSION`/`MBP1_MATERIALIZER_VERSION`, and take the stamped versions from the block.

### F9
**Finding**: The diagnostic's authorization gate is duck-typed and does not verify the reference: any object with `policy_id == "verification_fixed_allowlist_max5_v1"` and `allowed_dates` passes `assert_diagnostic_authorized`; `VerificationAuthorizationRef.content_hash` is returned unrecomputed; `_real` does not call the existing `validate_verification_run` (which cross-checks `allowlist_hash == allowlist_sha256(allowlist)`, seed snapshot, coverage-matrix id, policy id); the verification-namespace guard is a substring test (`"search_test" in path.as_posix()` — `…/my_search_test_x/…` passes).
**Severity**: MINOR
**Contract citation**: plan §6.I ("runs ONLY under `VerificationReplayPolicy` + a verified `VerificationAuthorizationRef`"); kickoff §9 Data safety ("Real-data access occurs only through the existing policies plus the new `VerificationReplayPolicy`"); `search/verification.py:144-190` (`validate_verification_run`, the R1 gate).
**Code/test evidence**: `mbp1_coverage_diagnostic.py:139-176`; `scripts/ifvg_mbp1_coverage_diagnostic.py:33-41`, `:115-124`. Probe `probe_diag_and_misc.py` §2:
```
=== 2. duck-typed access policy accepted by assert_diagnostic_authorized ===
returned: cccccccccccccccccccccccccccccccccccccccccccccccccccccccccccccccc
```
**Reproduction steps**: `probe_diag_and_misc.py` section 2.
**Suggested resolution**: `isinstance(access_policy, VerificationReplayPolicy)`; call `validate_verification_run` (or at least assert `run.payload.allowlist_hash == allowlist_sha256(run.payload.allowlist)` and `authorization == run.payload.verification_authorization`); check `"search_test" in store_root.parts`.

### F10
**Finding**: The §8 "MBP-1 (decision R-6 family)" defaults are pinned as contract fields but are **not stamped** `proposed_protocol_default` anywhere (no analogue of `REGIME_PROPOSED_DEFAULTS`; no UI/table/doc labels them unratified — only `MIN_DAY_COVERAGE_FRACTION` carries the "engineering default … unratified" note). Additionally `evidence_kinds_accepted` / `recovery_boundary_kinds_accepted` are validated as sets but hashed as ordered tuples, so semantically identical contracts get different identities.
**Severity**: MINOR
**Contract citation**: plan §8 ("New stamped scientific defaults (`proposed_protocol_default`; owner ratification before feature eligibility) … MBP-1 (decision R-6 family): `completeness_evidence_required=True`, `evidence_kinds_accepted`, `recovery_boundary_kinds_accepted`, `expected_span_source=…`, `bad_book_scope_fallback=…`, and the positive-completeness compiler policy id"); kickoff §11 ("implement it and log the choice. It remains unratified").
**Code/test evidence**: `mbp1_source_contract.py:301-339`; `grep proposed_protocol_default src` hits only `regime_contracts.py` / `context_bar_panel_contract.py`. Probe `probe_diag_and_misc.py` §3: `same hash? False` for reversed `evidence_kinds_accepted`.
**Reproduction steps**: `probe_diag_and_misc.py` section 3.
**Suggested resolution**: register the six R-6-family values in a frozen `MBP1_PROPOSED_DEFAULTS` table with `stamp="proposed_protocol_default"` (surfaced by the MBP-1 panel like the regime stamp table), and pin the two tuples as Literal-ordered constants (validator: equality, not set equality).

### F11
**Finding**: Documentation fidelity gaps. (a) DECISIONS_TAKEN #48 and R5B `DEVIATIONS.md` DEV-R5B-6 carry no in-place "amended by #67 / superseded by R5B.1" note although the plan requires "DECISIONS_TAKEN #48 amended; DEV-R5B-6 superseded"; #49 still lists `sequence_gap` in the precedence with no pointer. (b) ARCH_APPEND and D-048 claim the real diagnostic characterizes "open-interval facts … which manifests / condition records exist" (F4) and that an undocumented recovery yields `open_uncertainty_to_partition_end` (F6 shows a beyond-partition boundary defeats it). (c) ARCH_APPEND/D-048/#70 claim "the full trading-day span is never repeated per partition" (F3).
**Severity**: MINOR
**Contract citation**: plan §6.I last bullet ("Docs: D-048 …; DECISIONS_TAKEN #48 amended; DEV-R5B-6 superseded"); CLAUDE.md documentation rule; kickoff §10 ("Do not dismiss a finding because the tests are green").
**Code/test evidence**: `DECISIONS_TAKEN.md:296-312` (#48/#49 unchanged), `R5B/DEVIATIONS.md` DEV-R5B-6 (no marker; `grep superseded|R5B.1` empty), `R5B.1/stage_shared_docs.py` ARCH_APPEND lines "characterizes per-partition row/flag counts, open-interval facts, …".
**Reproduction steps**: `grep -n "superseded\|R5B.1" QL-FSM-PROP-SEARCH-DASHBOARD/implementation-progress/R5B/DEVIATIONS.md` (empty); read #48/#49.
**Suggested resolution**: add one-line supersession pointers to #48, #49 and DEV-R5B-6; align ARCH_APPEND/D-048/#70/#72 wording with the F3/F4/F6 resolutions.

### F12
**Finding**: Typed interval facts lose provenance on merge/labeling: `_merged_typed_intervals` keeps only the first interval's `evidence_kind`/`start_kind` (a vendor-flag interval that absorbs a manifest-declared `[100,200)` reads as `vendor_flag_maybe_bad_book` only; the manifest contribution is unrecoverable from the artifact), and `_flag_scope_level` labels an interval `channel_partition` on a scope whose own `scope_level` is `publisher_partition` when `channel_id` is set and `channel_map_verified=True` (no arithmetic narrowing — the label alone is incoherent with the scope).
**Severity**: MINOR
**Contract citation**: plan §6.I ("`declared_gap_intervals` (typed, merged)"; "every claim carries an explicit scope"); D-048 ("every negative claim names its scope, start, and recovery kind so a reviewer can falsify it").
**Code/test evidence**: `mbp1_coverage_evidence.py:726-757`, `:632-639`. Probes `probe_diag_and_misc.py` §5 (`merged=True kind=vendor_flag_maybe_bad_book` for a merge that includes a manifest interval) and `probe_badbook.py` 9 (`scope=channel_partition` under a publisher-level scope).
**Reproduction steps**: as cited.
**Suggested resolution**: carry `contributing_evidence_kinds: tuple[...]` on merged intervals; require `scope.scope_level is CHANNEL_PARTITION` for a channel-labelled flag interval (else publisher).

---

## What holds (probes that FAILED to falsify)

- **Withdrawn rule cannot resurface** — `Mbp1SourceContract.gap_semantics` is a Literal (`test_normal_sequence_skips_never_become_source_gaps` proves `sequence_gap_marks_interval_invalid_v1` fails validation); the only `np.diff` over `sequence`/`ts_recv` in the lane is `sequence_jump_diagnostics`/`ts_recv_gap_diagnostics` (`mbp1_coverage_evidence.py:600,615`); `gap_intervals` consumers are only the materializer typing (`declared_source_gap`) and counts; `sequence_gap`/`sequence_gap_intervals` have no consumer in `src`/`scripts` (grep); `sequence_positive_jump_count` is display-only. Probe: `probe_badbook.py` and `test_giant_sequence_jump_never_lowers_coverage` (coverage 1.0, `positive_jump_count == 1`).
- **Bad-book start/end/scope core rules** — flagged first row → `partition_expected_start`; multiple flags merge to one fail-closed interval `[0,600)`, coverage 0.0, `open_to_end=True`; boundary before the flag ignored; `F_BAD_TS_RECV` prior → partition start; consecutive flagged rows → second starts at partition start; flags outside the span open nothing (see F2 for the coherence gap); channel label only with `channel_map_verified`; `INSTRUMENT_PARTITION` never produced by `_flag_scope_level`; the "next unflagged row" (250 s) never closes (`test_maybe_bad_book_without_documented_recovery_fails_closed`). Probe: `probe_badbook.py` cases 1–3, 5, 6, 6b, 8, 9.
- **Coverage arithmetic** — `authorized_session_span_ns` is DST-aware and matches `IFVG_DOC_SESSION_SCHEME` (`trading_day_boundary=18:00`, `closed_window=(17:00,18:00)`) and `replay_chart_store.py:162`: `2026-01-13 → 2026-01-12T23:00Z…2026-01-13T22:00Z (23 h)`, `2026-03-08 → 22 h`, `2026-06-10 → 22:00Z…21:00Z`. Union/merge-once/clip (`merge_intervals([(5,10),(8,12),(20,25)], clip=(6,24)) == ((6,12),(20,24))`), head/tail only when declared, empty span unknown, weakest status wins, `completeness_unknown ⇒ coverage 0.0` (model validator refuses `1.0`). Two disjoint UTC partitions weight 0.4·1.0 + 0.6·0.5 = 0.7 (`test_multi_utc_partition_denominators…`). Note: the ET constants are duplicated locally rather than read from the shared scheme (drift risk only; not a finding).
- **Dataset-condition vocabulary** — five distinct states; `available` + no manifest → `completeness_unknown` → `coverage_evidence_unavailable`; `degraded/pending/missing` downgrade even with a compiled positive claim (`test_dataset_condition_available_without_partition_evidence_is_unknown`).
- **Compiler shape** — a bare `verified_complete_outside_intervals` without a report id is unrepresentable; a report with `positive_completeness_authorized=False` compiles to assertion `none`; provenance cannot be relabeled (`Mbp1PartitionEvidence` refuses `owner_reviewed` over a synthetic manifest); `load_verified_partition_evidence` refuses absent/tampered/unauthorized/foreign-scope/mismatched-interval reports (`test_evidence_stores_round_trip_and_verified_load`); `source_document_sha256` is a caller input hashed **before** the manifest id exists, so it cannot be self-referential (the artifact's own hash never enters its payload). The binding gap is F1.
- **Identities (HEAD `179a2c9` vs working tree, `ids_head.json`/`ids_work.json`)** — R5B activation envelope byte-identical: `activation_resolved_id f47bacf9…` and the activation payload JSON sha equal at HEAD and now; `PRE_R5B1` registry hash `b9265c58…` == HEAD's published registry hash; published registry hash now `3011631a…`, MBP-1 block v3 resolved id `f90090af…`; B0/B1/B4 unchanged (`668f6fa6…`, `57e5c68b…`, `33d340b5…`), B2/B3 changed (`dd8a4927…→da662036…`, `b72d8af9…→ef2d4a05…`) and `PRE_R5B1` B2/B3 equal HEAD's; B5/B6 are unresolvable at both trees (`BlockUnavailableError`, regime block planned) so nothing to re-mint; `normalized_schema_hash` changed (`4e1a3208…→74b776e0…`, `publisher_id`+`flags`) while source/feature-table/stage-evidence hashes are unchanged (hence the activation event's byte identity — `MBP1_MISSING_REASONS` does not enter any Arrow schema); feature-artifact and coverage-report ids move with the v2 block (`probe_ids_downstream.py`); controlled-study id binds both resolved bundle ids + `mbp1_feature_artifact_id` (`controlled_feature_study.py:306-313`); the three new pairs (`Mbp1PartitionGapManifest`, `Mbp1CompletenessCompilationReport`, `Mbp1CoverageDiagnosticReport`) are enumerated in `registered_identity_pairs()` and pass the projection audit (`test_identity_projection_audit_all_pairs`).
- **Materializer precedence** — day → `coverage_evidence_unavailable` → `coverage_below_threshold` → anchor → boundary → `declared_source_gap` → roll → min-count, exactly the intersecting windows (`{ofl_win_inversion_entry, ofl_snap_entry}` for `[280,300)`), candidate rows preserved; multi-partition day: `_verify_events_against_source` accepts two rows sharing the day's `content_sha256`, one unknown partition types the whole day `coverage_evidence_unavailable`, a gap declared in partition B alone types exactly the intersecting windows with day coverage 0.9667 (`probe_diag_and_misc.py` §4).
- **Bounded diagnostic gating** — `assert_diagnostic_authorized` refuses no policy / a non-verification policy / a mismatched allowlist hash / an absent ref before any path; `build_mbp1_coverage_diagnostic` refuses `verification_5d` without the hash; `completeness_inferred_from_sequence_continuity` is `Literal[False]`; the CLI real path refuses without `--verification-run-id` before loading anything, constructs only `Path` objects after the gate (`IfvgCaptureConfig.__post_init__` touches no filesystem), invokes `path_factory` only inside `resolve_source_path`; the synthetic path refuses real-shaped (`events_stored=False`) and owner-reviewed artifacts (`test_diagnostic_cli_refuses_real_path_and_runs_synthetic_shape`). Residual weaknesses are F9.
- **Synthetic-provenance scope** — `context.synthetic` is structural (`isinstance(owner_authorization, SyntheticAuthorizationMarker)`, `pipeline.py:822`); S05 calls `assert_evidence_provenance_permitted` before trusting the artifact; the real builder refuses `SYNTHETIC_FIXTURE` before `resolve_source_path` (`calls == []`); no other producer path exists (`build_mbp1_source_artifact` is the shared in-memory builder by design; `pipeline_baseline_verification_entry` wires no `mbp1_evidence_source` — DEV-R5B-1 unchanged).
- **Guards/boundaries** — deep-book identifier guards green (`test_every_schema_field_passes_the_deep_book_guard`, `test_no_deep_book_identifier_anywhere`, `test_deep_book_regex_covers_every_depth_beyond_one`); `research_only_offline` is a Literal on the coverage report and the diagnostic payload and asserted in `test_pipeline_run.py:544,579`; frozen M0–M3 modules untouched (not in `git status`); protected/sealed dates are unrepresentable in `Mbp1EvidenceScope`/`Mbp1DatasetConditionRecord` (`test_protected_and_sealed_dates_are_unrepresentable_in_evidence`); `stage_shared_docs.py` anchors exist at HEAD (`docs/README.md:96`, `docs/pipeline_state.yaml:175`).
- **Suites** — 119 passed / 0 failed; ruff clean on all 21 R5B.1 files; `git diff --check` clean on the lane.


---

# R5B.1 — Adversarial Review, Reviewer 2 (SAFETY / ACCESS / IMMUTABILITY)

**Verdict: 0 blockers, 3 majors, 1 medium, 4 minors; protected/sealed zero-counter: AFFIRMED** — no R5B.1 code path, test, or review probe constructed, listed, stat'ed, opened, or read any real source partition; `data/` gained zero files (`find data -type f -newermt 2026-08-27` → 0; no `mbp1_gap_manifests` / `mbp1_completeness_reports` / `mbp1_coverage_diagnostics` / `search_test` directory exists under `data/`); Strategy-Core clean, Trade-Lab untouched (27 pre-existing user entries, newest mtime 2026-07-31); plan package unmodified. The three MAJORs are LATENT defects on the real path this release adds — none has executed — and must be resolved before the owner's real diagnostic run.

Scope reviewed: the four new files, the ten modified src files, the seven modified tests, D-048, DECISIONS_TAKEN #67–72, `R5B.1/PRE_R5B_1_BASELINE.md`, `stage_shared_docs.py`; authority: R6.1 plan §3/§5 D12/§6.I/§7/§9.1/§11/§12, owner Q1, plan-review correction 4, final corrections #2/#7, kickoff §4/§8/§9/§10, `development_access.py`, `data_access.py`, `search/authorization.py`, `search/verification.py`, `search/store.py`, R5B/R6 access-safety evidence. All probe scripts and outputs live in `%TEMP%\...\scratchpad\review_r5b1_reviewer2\` (`probe_[a-g]_*.py`, `probe_[a-g]_output.txt`); nothing inside the repository was created, edited, staged, or committed (post-review `git status --short | grep -c "^ M"` → `27`, identical to the start; no reviewer file appears in `git status`).

---

## S1 — The real read seam consumes whole UTC-date partition files, labels every row with the FILE date as `trading_day`, and applies neither the trading-day window nor `DEVELOPMENT_CUTOFF_UTC`; on the canonical fixture's last day (2026-06-10) the file's 22:00Z–24:00Z tail is trading day 2026-06-11 (PROTECTED_BUFFER) and would be read, hashed and counted under `2026-06-10`, invisible to the protected counters

**Finding**: `read_mbp1_partition_frame` (`mbp1_source_artifact.py:566-598`) resolves ONE parquet per allowlist day (`data/databento/NQ/<day>/mbp1.parquet`) and `normalize_mbp1_events` stamps a constant `"trading_day": trading_day` (`:290`) on every row with no clip to `[18:00 ET D-1, 17:00 ET D)` and no `DEVELOPMENT_CUTOFF_UTC` (`2026-06-10T21:00:00Z`) clip — `DEVELOPMENT_CUTOFF_UTC` is referenced nowhere in the MBP-1 lane (grep: only `preparation.py:265,422` and `fsm_audit_preparation.py` use it). Strategy-Core states the physical files are partitioned at UTC midnight and a trading day is COMPOSED from two files (`../Strategy-Core/src/strategy_core/data/databento_parquet.py:258-263`); the plan's own partition contract says "one trading day is assembled from MULTIPLE UTC/source partitions … `prev_utc_date/mbp1` vs `day_utc_date/mbp1`" (`CONTRACTS_AND_SCHEMAS.md:49-55`). R5B.1's `_unknown_partition_coverage` (`:332-369`) hard-codes `source_partition_utc_date=trading_day` and `relative_logical_partition_key="day_utc_date/mbp1"` (`:342-343`), so the immutable source artifact asserts UTC-date == trading-day. The audit records opens/rows by FILE date (`development_access.py:319-331`), so `assert_zero_protected` cannot see protected-buffer rows read from the 06-10 file. R5B.1 is the release that makes this seam executable on real data (`scripts/ifvg_mbp1_coverage_diagnostic.py:147-154`) with `allowlist = run.payload.allowlist` (proposed canonical fixture ends `2026-06-10`, `verification.py:58-64`). R5B never disclosed the single-file/no-composition simplification (grep of `R5B/DEVIATIONS.md`, `GATE_SUMMARY.md`, `ADVERSARIAL_REVIEW.md` for `prev_utc|composition|cutoff` → no hit).
**Severity**: MAJOR (latent — cannot execute without the owner's `VerificationAuthorizationRef`; if executed on the current code it becomes a protected-buffer access and a mis-audited one).
**Contract citation**: kickoff §9 Data safety ("Never construct, list, stat, open, or read June 11, 2026 … All protected/sealed counters remain zero"); `development_access.py:40` `DEVELOPMENT_CUTOFF_UTC = "2026-06-10T21:00:00Z"` (the repo's own interpretation, enforced by the M0–M3 preparation chain at `preparation.py:265,422`); `tick_store.py:41-48` trading-day rule; plan §6.I / D12 ("each physical partition's denominator is `intersection(...)`; multiple UTC partitions of one trading day are measured separately"); owner Q1 #6 ("characterize actual partition-quality evidence").
**Code/test evidence** (`probe_g_utc_partition.py`, verbatim):
```
G1 authorized session span for trading day 2026-06-10: [2026-06-09T22:00:00+00:00, 2026-06-10T21:00:00+00:00)
G1 DEVELOPMENT_CUTOFF_UTC: 2026-06-10T21:00:00Z
G1 UTC-date partition file 2026-06-10 covers: [2026-06-10T00:00:00+00:00, 2026-06-11T00:00:00+00:00)
G1 hours of the 06-10 file AFTER the cutoff/authorized end: 3.0
G1 hours of trading day 06-10 that live in the 06-09 file: 2.0
G1 row at 2026-06-10T22:00:00Z -> trading_day 2026-06-11 -> class protected_buffer
G1 row at 2026-06-10T23:59:59Z -> trading_day 2026-06-11 -> class protected_buffer
G2 rows read from the UTC-date file: 3
G2 normalized trading_day column values: ['2026-01-13']
G2 per-row actual trading day (repo rule): ['2026-01-13', '2026-01-13', '2026-01-14']
G2 rows beyond the authorized session end (17:00 ET): 2
G2 immutable artifact row: trading_day=2026-01-13 source_partition_utc_date=2026-01-13 key=day_utc_date/mbp1 row_count=3
G2 artifact last_ts_event: 2026-01-13T23:30:00+00:00 (>= session end: True)
G2 ts_recv/sequence diagnostics computed over ALL rows: seq events=3 ts_recv max gap h=7.50
G2 access audit keyed by FILE date only: {'2026-01-13': 3}
```
(G1 is pure timestamp arithmetic — no path; G2 uses a synthetic parquet under the probe tmp dir on the fixture day 2026-01-13 — the mechanism is identical for 06-10.) Static: `grep -rn DEVELOPMENT_CUTOFF_UTC src scripts` → no hit in `features/mbp1_*` or the CLI.
**Reproduction steps**: (1) write three source-schema rows for the fixture day at 15:00Z, 22:30Z (17:30 EST closed window), 23:30Z (18:30 EST = next trading day) to `<tmp>/NQ/2026-01-13/mbp1.parquet`; (2) `read_mbp1_partition_frame("2026-01-13", access_policy=ExplorationDataPolicy(allowlist=frozenset({"2026-01-13"})), path_factory=..., instrument="NQ")` → 3 rows, all `trading_day="2026-01-13"`; (3) `build_mbp1_source_artifact(...)` → `row_count=3`, `last_ts_event` past 17:00 ET; (4) `policy.audit.rows_read_by_date == {"2026-01-13": 3}`. (5) For the June boundary run G1.
**Suggested resolution**: in `read_mbp1_partition_frame` (or a new `read_mbp1_trading_day_frame`) clip rows to `authorized_session_span_ns(trading_day)` AND to `DEVELOPMENT_CUTOFF_UTC` before normalization/hashing, and either compose the trading day from the `prev_utc_date/mbp1` + `day_utc_date/mbp1` files (both policy-authorized by FILE date, both inside the window) or record explicitly in the artifact that only the `day_utc_date` partition was read (`relative_logical_partition_key` truthful, `source_partition_utc_date` = the file's UTC date, and the missing prev-partition span typed). Record per-row trading-day counts in the development audit (rows whose event trading day classifies PROTECTED/SEALED must be a counter that `assert_zero_protected` checks). Add `test_real_read_clips_to_trading_day_and_cutoff` (synthetic parquet, tmp) to the R5B.1 gate and list this as a pre-condition of plan §12 blocker 3 in GATE_SUMMARY.

## S2 — The diagnostic's real-path authorization gate is weaker than the R1 real-slice gate it claims to mirror: a self-signed `VerificationAuthorizationRef` persisted into any `search_test` root passes every check; the canonical-allowlist marker, the coverage-matrix artifact, and the payload's own `allowlist_hash` are never consulted

**Finding**: `_real` (`scripts/ifvg_mbp1_coverage_diagnostic.py:88-174`) loads the run envelope (`:115-117`), then `assert_diagnostic_authorized` (`mbp1_coverage_diagnostic.py:139-176`) checks only: policy id string, ref non-None, `ref.approved_allowlist_hash == sha256(days)`, `policy.allowed_dates == days` (trivially equal — both derive from the same envelope). It never calls `validate_verification_run` (`verification.py:144-193`) nor `register_program_allowlist` (`:243-289`), never resolves `coverage_matrix_artifact_id`/`seed_snapshot_id`, and never checks `run.payload.allowlist_hash == sha256(run.payload.allowlist)` although that unvalidated hash is stamped into the immutable source artifact as `authorized_date_set_id` (`:152`). The R1 real slice does all of this (`child_replay.py:598-626`; `pipeline.py:836-857`). Store integrity is not authenticity (DEV-R6-8): a forged envelope saved through `save_or_reuse_envelope` verified-loads.
**Severity**: MAJOR (the shared trust model — an unsigned `content_hash` — is the documented R1 design and would be a design note; the DELTA versus the R1 gate — no canonicality, no matrix, no hash consistency — is a regression specific to this release's only real path).
**Contract citation**: kickoff §4 ("Do not fabricate, self-sign, or infer owner authorization … `VerificationAuthorizationRef` binding: the one canonical ≤5-day allowlist, coverage-matrix artifact, matching baseline seed snapshot, verification-policy identity, pipeline/run identity, owner/approver evidence … One canonical real-data allowlist applies to the entire implementation-verification program"); `verification.py:249-256` (V3 P0-7 "any later, different allowlist is refused regardless of release"); plan §6.I ("runs ONLY under `VerificationReplayPolicy` + a verified `VerificationAuthorizationRef` on the canonical ≤5-day allowlist"); DECISIONS_TAKEN #72.
**Code/test evidence** (`probe_a_forge.py`, verbatim; the real source builder was STUBBED so the per-day path factory was never invoked — `path_factory_invoked: False`):
```
A1 assert_diagnostic_authorized ACCEPTED the self-signed ref; returned content_hash = 3333333333333333333333333333333333333333333333333333333333333333
A2 forged VerificationRunEnvelope persisted + verified-loaded; id = 95659642d63b016ba5111595af580eed63b14e28bfaa7b0a68813a1e97e294ba
A2b wrote a canonical allowlist marker for a DIFFERENT allowlist at ...\probe_a_tmp\search_test\v1\VERIFICATION_ALLOWLIST_MARKER.json
A3 CLI passed EVERY gate with the forged envelope and reached build_mbp1_source_artifact_from_paths: {'days': ('2026-01-13', '2026-01-14'), 'policy': 'VerificationReplayPolicy', 'authorized_date_set_id': '483eaffd...', 'coverage_evidence': None, 'path_factory_invoked': False}
A4 allowlist_hash != sha256(allowlist) ACCEPTED; authorized_date_set_id stamped = 9999999999999999999999999999999999999999999999999999999999999999
A5 CLI source references 'validate_verification_run': False
A5 CLI source references 'register_program_allowlist': False
A5 CLI source references 'CoverageMatrixEnvelope': False
A5 CLI source references 'coverage_matrix_artifact_id': False
A5 CLI source references 'seed_snapshot_id': False
```
**Reproduction steps**: (1) build `VerificationAuthorizationRef(approved_allowlist_hash=allowlist_sha256(days), content_hash="3"*64, approved_by="anyone", ...)`; (2) wrap in `VerificationRunPayload`/`VerificationRunEnvelope.from_payload`, `save_or_reuse_envelope(<tmp>/search_test/v1, "verification_runs", env)`; (3) write a `VERIFICATION_ALLOWLIST_MARKER.json` for a different allowlist in that root; (4) monkeypatch `mbp1_source_artifact.build_mbp1_source_artifact_from_paths` to raise a sentinel WITHOUT calling `path_factory`; (5) `cli.main(["--store-root", <root>, "--verification-run-id", env.verification_run_id])` → the sentinel fires (every gate passed). Repeat with `allowlist_hash="9"*64` → stamped as `authorized_date_set_id`.
**Suggested resolution**: make `_real` (a) call `register_program_allowlist(store_root, VerificationDataPolicy.from_allowlist(days), canonical_root=ROOT / SEARCH_TEST_STORE_ROOT)` (idempotent-match, refuses a non-canonical allowlist), (b) verified-load the `CoverageMatrixEnvelope` named by `ref.coverage_matrix_artifact_id` from the same root and require `candidate_allowlist == days`, (c) require `run.payload.allowlist_hash == allowlist_sha256(run.payload.allowlist) == ref.approved_allowlist_hash` and `ref.verification_policy_id == run.payload.verification_policy_id`, (d) route through `validate_verification_run` (or a shared `assert_verification_run_bound` helper) so the diagnostic and the R1 slice enforce one gate; (e) add a test that a persisted-but-non-canonical run envelope is refused before the builder is reached. Update DECISIONS_TAKEN #72 / D-048 wording ("verified `VerificationAuthorizationRef`") to state what is actually verified.

## S3 — `owner_reviewed` provenance and positive completeness are unverified assertions: a fabricated in-memory compilation report (`owner_review_decision_id="anything"`, non-hex `verified_partition_refs`) compiles to `verified_complete_outside_intervals`, yields `evidenced_complete`/coverage 1.0 with `evidence_provenance=owner_reviewed`, passes the real-scope provenance gate, is accepted by the real builder, and materializes VALID features; an `owner_reviewed` label with NO manifest is also representable

**Finding**: `compile_mbp1_partition_gap_manifest` (`mbp1_coverage_evidence.py:442-478`) is a pure function over ANY in-memory `Mbp1CompletenessCompilationReportEnvelope`; the report's only structural guard is `owner_review_decision_id` non-empty and `len(ref)==64` (`:383-392`, no hex pattern); `positive_completeness_authorized: bool` (`:377`) is exactly the "unstructured boolean" the plan forbids, moved one layer up. `Mbp1PartitionEvidence._scope_agrees` checks provenance only when `gap_manifest is not None` (`:507-515`), so `provenance="owner_reviewed"` with no manifest constructs. `assert_evidence_provenance_permitted` (`mbp1_source_artifact.py:414-434`) and `build_mbp1_source_artifact_from_paths` (`:626-632`) refuse only `SYNTHETIC_FIXTURE`; `OWNER_REVIEWED` is never bound to a store-verified manifest (`load_verified_partition_evidence`, `:898-937`, has zero production callers) nor to an `OwnerDecisionEvidenceRef`. The R6 round found the same pattern (R6 ADVERSARIAL_REVIEW S1: "fabricated ratification string").
**Severity**: MAJOR (no real consumer today — the diagnostic passes `coverage_evidence=None` — but DECISIONS_TAKEN #68 / D-048 / `ARCH_APPEND` claim "Positive completeness exists ONLY through … a verified `Mbp1CompletenessCompilationReport` (source inventory + owner review) — never a bare boolean", which the contract does not enforce).
**Contract citation**: plan §6.I ("`Mbp1CompletenessCompilationReport{...}` is itself immutable and verified-loaded. Positive completeness can be emitted only by `compile_mbp1_partition_gap_manifest` from those verified inputs — never by an unstructured … boolean"); D12 ("Positive completeness is produced only by a verified compiler over source inventory/manifest evidence"); final corrections #2; kickoff §4 (never fabricate/self-sign owner evidence); §10 ("Do not dismiss a finding because the tests are green").
**Code/test evidence** (`probe_d_provenance.py`, verbatim; in memory, no store, no path):
```
D1 Mbp1PartitionEvidence(gap_manifest=None, provenance=owner_reviewed) CONSTRUCTED; status=completeness_unknown coverage=0.0 sources=()
D1 artifact row: evidence_provenance=owner_reviewed status=completeness_unknown evidence_sources=()
D1 assert_evidence_provenance_permitted(synthetic_scope=False): PASSED (owner_reviewed label with zero evidence enters a real scope)
D2 report accepted verified_partition_refs = zzzzzzzz... (non-hex)
D2 compiled manifest: assertion=verified_complete_outside_intervals provenance=owner_reviewed
D2 artifact row: evidence_provenance=owner_reviewed status=evidenced_complete coverage=1.0
D2 assert_evidence_provenance_permitted(synthetic_scope=False): PASSED
D3 build_mbp1_source_artifact_from_paths: policy reached: the provenance gate had already passed; path_factory calls=[]
D4 materialized under fabricated owner_reviewed evidence: ofl_snap_entry_valid=True missing_reasons=['']
```
**Reproduction steps**: (1) `Mbp1CompletenessCompilationReportEnvelope.from_payload(Mbp1CompletenessCompilationReportPayload(scope=synthetic_scope(...), source_inventory_id="x", verified_partition_refs=("z"*64,), evidence_refs=(), owner_review_decision_id="fabricated", provenance="owner_reviewed", compiled_intervals=(), positive_completeness_authorized=True, compiled_at="..."))`; (2) `compile_mbp1_partition_gap_manifest(report, source_document_sha256="c"*64, ...)`; (3) `Mbp1PartitionEvidence(scope, gap_manifest=manifest, provenance="owner_reviewed")`; (4) `build_mbp1_source_artifact(events, ..., coverage_evidence={day: (ev,)})` → `evidenced_complete`; (5) `assert_evidence_provenance_permitted(rows, synthetic_scope=False)` passes; (6) `build_mbp1_source_artifact_from_paths(..., coverage_evidence=...)` with a policy that raises on `resolve_source_path` → the raise proves the provenance loop passed; (7) `materialize_mbp1_features(...)` → windows valid.
**Suggested resolution**: (a) require `provenance is NONE` when `gap_manifest is None`; (b) make the REAL builder and `_ensure_mbp1_evidence` accept only evidence produced by `load_verified_partition_evidence` (e.g. a `Mbp1VerifiedPartitionEvidence` type constructible only by that loader, or carry the store-verified manifest id + root and re-verify); (c) pattern `verified_partition_refs`/`evidence_refs` as `SHA256_PATTERN` and make `owner_review_decision_id` a 64-hex `OwnerDecisionEvidenceRef.content_hash` resolved through the R6.1 owner-decision store (workstream F) before `positive_completeness_authorized` may be true; (d) `assert_evidence_provenance_permitted` should refuse `owner_reviewed` rows whose `evidence_sources` carry no `gap_manifest:` entry; (e) correct DECISIONS_TAKEN #68 / D-048 to state the enforced invariant.

## S4 — `--store-root` confinement is a substring test: `xsearch_testx`, `search_test_evil`, `not_search_test` all pass `_assert_verification_namespace`

**Finding**: `_assert_verification_namespace` (`scripts/ifvg_mbp1_coverage_diagnostic.py:33-41`) accepts any path whose POSIX string contains `search_test`; the docstring claims "Both paths write ONLY into a verification (search_test) namespace". The R1 contract pins the namespace as the Literal `"search_test/v1"` (`verification.py:127`; `store.py:39`). The test only proves `search/v1` refuses.
**Severity**: MEDIUM.
**Contract citation**: kickoff §8 ("Test artifacts must remain in temporary or explicitly test-namespaced storage"); DECISIONS_TAKEN #72 ("writes only into the verification namespace"); `VerificationRunPayload.output_namespace: Literal["search_test/v1"]`.
**Code/test evidence** (`probe_b_namespace.py`, verbatim):
```
B 'search_test/v1'       SearchStoreError: missing search-store entry verification_runs/aaaa...
B 'xsearch_testx/v1'     SearchStoreError: missing search-store entry verification_runs/aaaa...
B 'search_test_evil/v1'  SearchStoreError: missing search-store entry verification_runs/aaaa...
B 'not_search_test'      SearchStoreError: missing search-store entry verification_runs/aaaa...
B 'search/v1'            SystemExit: the MBP-1 coverage diagnostic writes only into a verification (search_
B 'verification/v1'      SystemExit: the MBP-1 coverage diagnostic writes only into a verification (search_
```
(a `SearchStoreError` here means the namespace check PASSED and the CLI proceeded to the store load.)
**Reproduction steps**: `cli.main(["--store-root", "<tmp>/xsearch_testx/v1", "--verification-run-id", "a"*64])`.
**Suggested resolution**: require `Path(store_root).resolve().parts[-2:] == ("search_test", "v1")` (or equality with `ROOT / SEARCH_TEST_STORE_ROOT` for the real path, with a tmp-rooted exception for tests), and add both the positive and the `search_test_evil` negative to `test_diagnostic_cli_refuses_real_path_and_runs_synthetic_shape`.

## S5 — "Protected/sealed dates are UNREPRESENTABLE inside coverage evidence" holds only for `Mbp1EvidenceScope.utc_date` and `Mbp1DatasetConditionRecord.utc_date`; `Mbp1PartitionCoverage.trading_day` / `source_partition_utc_date` (evidence-less rows), `Mbp1CoverageDiagnosticReportPayload.allowlist`, and `Mbp1PartitionDiagnosticRow.trading_day` accept sealed literals

**Finding**: `_UNREPRESENTABLE_DATE_CLASSES` / `assert_evidence_date_representable` (`mbp1_coverage_evidence.py:129-151`) are applied only in `Mbp1EvidenceScope._coherent` (`:264`) and `Mbp1DatasetConditionRecord` (`:336`). `Mbp1PartitionCoverage.trading_day: str` (`mbp1_source_artifact.py:115-116`), `_unknown_partition_coverage` (`:342`), `Mbp1CoverageDiagnosticReportPayload.allowlist` (`mbp1_coverage_diagnostic.py:85`) and `Mbp1PartitionDiagnosticRow.trading_day` (`:57`) are unvalidated strings, so a sealed label can be baked into an immutable source artifact or diagnostic report (as text — no path is built from it; the primary authorize-before-path gate is intact).
**Severity**: MINOR (defense-in-depth gap; the module's own claim at `:129-131` is broader than what is enforced).
**Contract citation**: `mbp1_coverage_evidence.py:129-131` (the release's stated invariant); kickoff §9 (counters/representation hygiene); plan §11.6.
**Code/test evidence** (`probe_c_dates.py`, in memory only, never persisted, no path):
```
C3 build_mbp1_source_artifact with a SEALED trading_day label (no scope): ACCEPTED trading_day=2026-06-12 utc=2026-06-12 status=completeness_unknown
C3 build_mbp1_coverage_diagnostic allowlist=(SEALED,): ACCEPTED rows[0].trading_day=2026-06-12
C4 synthetic_scope(SEALED) refused: 1 validation error for Mbp1EvidenceScope  Value error, evidence date 2026-06-12 is sealed: protecte
```
**Reproduction steps**: `build_mbp1_source_artifact({"2026-06-12": normalized_day(rows, day="2026-06-12")}, contract=synthetic_contract(), authorized_date_set_id="x")`; then `build_mbp1_coverage_diagnostic(env, run_scope="synthetic_fixture", allowlist=("2026-06-12",), authorization_content_hash=None)`.
**Suggested resolution**: validate `trading_day` / `source_partition_utc_date` in `Mbp1PartitionCoverage` and `allowlist` / `trading_day` in the diagnostic payload with `assert_evidence_date_representable` (or `VerificationDataPolicy`'s window rule), and extend `test_protected_and_sealed_dates_are_unrepresentable_in_evidence` to those fields.

## S6 — The real diagnostic has no evidence-input seam: `coverage_evidence=None` is hard-coded, `load_verified_partition_evidence` has no production caller, and `OWNER_REVIEWED` has no producer anywhere — so the shipped real run can only ever report `completeness_unknown`, never an open interval, never a recovery status

**Finding**: `scripts/ifvg_mbp1_coverage_diagnostic.py:153` passes `coverage_evidence=None`; the CLI exposes only `--store-root`, `--verification-run-id`, `--synthetic-source-artifact-id`. Without a scope, `_unknown_partition_coverage` (`mbp1_source_artifact.py:332-369`) never calls `bad_book_uncertainty_intervals`, so `open_uncertainty_to_partition_end` is structurally `False` and `F_MAYBE_BAD_BOOK` appears only as a count. The plan requires the diagnostic to report "open-interval facts … declared manifests / condition records present, completeness status". This is fail-closed (safe) but does not deliver owner Q1 #6 as stated, and the GATE_SUMMARY must not describe the real run as characterizing evidence it cannot load.
**Severity**: MINOR (safety-conservative; a fidelity/scope gap for the owner run).
**Contract citation**: owner Q1 #6; plan §6.I bounded diagnostic bullet; §12 blocker 3.
**Code/test evidence** (`probe_f_boundary.py`): `F5 CLI real path passes coverage_evidence=None: True` / `F5 CLI references load_verified_partition_evidence: False` / `F5 CLI argparse options: ['--store-root', '--verification-run-id', '--synthetic-source-artifact-id']`; `grep -rn "OWNER_REVIEWED\|\"owner_reviewed\"" src scripts` → only the enum definition and the CLI's refusal test.
**Reproduction steps**: read `_real`; run the synthetic path and observe every row `gap_manifest_present=False`, `open_uncertainty_to_partition_end=False` whenever no evidence is supplied.
**Suggested resolution**: add a repeatable `--partition-evidence-manifest-id <64-hex>` option that verified-loads through `load_verified_partition_evidence` (same store root) and maps to `coverage_evidence` by `(trading_day, physical_partition_key)`; or state in the R5B.1 GATE_SUMMARY/D-048 that the real run characterizes flags/diagnostics only until an evidence seam exists.

## S7 — `source_document_sha256` is an unbound field: it accepts an artifact identity (the compilation report's own id) as the "source document" hash

**Finding**: `compile_mbp1_partition_gap_manifest(report, source_document_sha256=...)` (`mbp1_coverage_evidence.py:442-478`) copies the caller's string; nothing distinguishes a document hash from an artifact id or binds it to any inventory. The manifest's OWN id cannot enter its payload (fixed-point) — that part holds.
**Severity**: MINOR.
**Contract citation**: final corrections #7 ("MBP source manifests use `source_document_sha256`; artifact content hashes remain envelope/manifest facts and never self-enter semantic payloads").
**Code/test evidence** (`probe_e_immutability.py`): `E6 manifest.source_document_sha256 != manifest id: True` / `E6 source_document_sha256 accepted the report's own artifact id (unbound field): True`.
**Reproduction steps**: `compile_mbp1_partition_gap_manifest(report, source_document_sha256=report.mbp1_completeness_compilation_report_id, ...)`.
**Suggested resolution**: bind `source_document_sha256` to an entry of `report.payload.evidence_refs` (or a source-document inventory) and refuse values equal to any registered artifact id in the evidence chain.

## S8 — Release hygiene: the worktree's shared docs do not yet carry the R5B.1 transforms, the R5B.1 evidence folder is incomplete, and R6.1 work is landing concurrently in the same worktree

**Finding**: `grep -c "R5B.1 correction (MBP-1 source-coverage policy v2" ARCHITECTURE.md` → `0`; `docs/README.md` → `0`; `docs/pipeline_state.yaml` → `0` (only `docs/DECISIONS.md` D-048 is present); `git diff --cached --stat` → empty (stage script not run). `implementation-progress/R5B.1/` holds only `PRE_R5B_1_BASELINE.md` + `stage_shared_docs.py` (no GATE_SUMMARY / TEST_RESULTS / FILES_TOUCHED / DEVIATIONS / ACCESS_SAFETY_EVIDENCE yet). During this review new untracked R6.1 files appeared (`features/arrow_tables.py`, `features/context_bar_panel_materializer.py`, `fold_schedules.py`, `ml/fold_set_artifact.py`, `ml/regime_oos_assignment.py`, `ml/regime_sample_adequacy.py`) alongside the already-modified `ml/regime_*.py`; the R5B.1 commit must be path-scoped.
**Severity**: MINOR (process; not a safety defect).
**Contract citation**: kickoff §1.4 / §8 ("stage and commit only files changed by this implementation … do not include unrelated changes"); §7 per-release folder minimum; CLAUDE.md documentation rule; plan §4 commit order.
**Code/test evidence**: the grep counts above; `git status --short` at review end.
**Reproduction steps**: run the three greps and `git diff --cached --stat`.
**Suggested resolution**: run `R5B.1/stage_shared_docs.py` (then `--apply-worktree`) at commit time exactly as R6 did; `git add` the explicit R5B.1 file list only; complete the evidence folder before the commit.

---

## What holds (probes that failed to falsify)

- **Fail-before-path ordering in `_real`**: namespace check (`:108`) → 64-hex run-id check (`:109-114`, no id → `SystemExit "…refused before any source path"`) → store load → `VerificationReplayPolicy(days)` (`:119`) → `assert_diagnostic_authorized` (`:120-124`) → only then `build_mbp1_source_artifact_from_paths` (`:147-154`); the per-day path exists only inside `access_policy.resolve_source_path` after `authorize_date` (`mbp1_source_artifact.py:587`; `data_access.py:143-152`). `assert_zero_forbidden_access()` (`:155`) runs after the reads and BEFORE the first store write (`:156`). No listing/glob/walk anywhere in the R5B.1 set (grep over 12 files: only `test_mbp1_schemas_and_source.py` globbing `features/*.py` source files and the one `access_policy.record_file_open` hit). `test_diagnostic_cli_refuses_real_path_and_runs_synthetic_shape` — `cli.main(["--store-root", str(root)])` → `SystemExit` matching "refused before any source path" (78 passed).
- **`VerificationReplayPolicy` window and cap** (`probe_c_dates.py`): `VerificationReplayPolicy(('2026-06-11',))` / `(('2026-06-12',))` / `(('2026-01-03',))` / `(('2026-06-10','2026-06-11'))` → `PermissionError: verification dates must lie inside the permitted development window; refused before path construction`; six sorted in-window days → `PermissionError: the verification fixture admits at most five real trading days`; empty → `ValueError: the verification allowlist cannot be empty`; `(('2026-06-10',))` → accepted `('2026-06-10',)`. (Canonicality across releases is NOT enforced by the policy — see S2.)
- **`_classify` boundaries / `assert_evidence_date_representable`** (`probe_c_dates.py`): `2025-12-31`, `2026-01-03`, `2026-01-10` → `outside_policy` refused; `2026-01-01`, `2026-01-12` → `warmup` REPRESENTABLE; `2026-01-13`, `2026-04-30` → `prior_research`; `2026-05-01`, `2026-06-09`, `2026-06-10` → `exposed_development_only` REPRESENTABLE; `2026-06-11` → `protected_buffer` refused; `2026-06-12`, `2026-07-01`, `2027-01-01` → `sealed` refused; `last permitted development date = 2026-06-10`; `representable set == PERMITTED_DEVELOPMENT_DATES: True` — consistent with `require_fixed_exploration_allowlist` (`"2026-01-01" <= day <= "2026-06-10"`) and `PROPOSED_VERIFICATION_ALLOWLIST`. `test_protected_and_sealed_dates_are_unrepresentable_in_evidence` (`test_mbp1_coverage_evidence.py:459-471`) builds only `Mbp1EvidenceScope`/`Mbp1DatasetConditionRecord` models — no path.
- **Literal grep over the R5B.1 set**: `2026-06-11|2026-06-12|2026-06-1x|2026-07` → only `test_mbp1_coverage_evidence.py:460,471` (model-construction literals, path-free); `sealed|protected` → docstrings/enum members only; `databento` → `PRICE_SCALE_POLICY_ID`, the contract's `source_partitions` text, `source_partition_id` example strings, and the CLI's parent-directory constant `ROOT / "data/databento"` (`:140`, a Path object for the data ROOT, never a date path; consumed only through the policy-gated factory).
- **On-disk audit** (PowerShell): files under `data/` newer than 2026-08-27 → `0`; no `mbp1_gap_manifests|mbp1_completeness_reports|mbp1_coverage_diagnostics|mbp1_source_artifacts|mbp1_feature_artifacts|mbp1_coverage_reports|search_test|verification_runs` directory under `data/`; `Test-Path data\ifvg_datasets\search` → `False`, `search_test` → `False`; no `VERIFICATION_ALLOWLIST_MARKER.json` under `data/`; re-audit after all probes: `find data -type f -mmin -90 | wc -l` → `0`. Every test write is `tmp_path`-rooted (`test_mbp1_coverage_evidence.py:156,415,528,539,645,702,723-725`; `test_mbp1_materializer.py`/`test_mbp1_schemas_and_source.py` idem); no absolute or repo-relative write in the modified tests.
- **Immutability / identity** (`probe_e_immutability.py`): `registered_identity_pairs()` includes `Mbp1CompletenessCompilationReport`, `Mbp1PartitionGapManifest`, `Mbp1CoverageDiagnosticReport`; `<id> == canonical_contract_sha256(payload)` → `True` ×3; wrong id → refused; 11 mutation attempts (envelope field, payload field, nested row, `ImmutableMap.__setitem__`, tuple element, `tuple.append`) → all `refused (ValidationError|AttributeError)`, `payload unchanged after attempts: True`; verified reuse → `True` ×4; tampered gap-manifest `envelope.json` → `artifact envelope.json failed verification`; tampered diagnostic `manifest.json` sha → `manifest hash mismatch`; tampered completeness report → `failed verification`; same-id `events_stored` flip → `store entry … exists with DIFFERENT content` and the stored entry is unchanged (`events_stored: True`). No R5B.1 code writes into an existing store entry (only `save_or_reuse_envelope`); the schema change is recorded as pre-acceptance re-minting (DECISIONS_TAKEN #71, plan §7) and no persisted artifact exists inside the repo (the only R5B-era `mbp1_source_artifacts` entry is the `%TEMP%\ifvg_r5b_smoke` scratch, outside governance).
- **Multi-partition sidecar hashing**: `save_mbp1_source_artifact` (`:667-675`) requires `{row.content_sha256 for rows of day} == {sha256(bytes)}`; `test_multi_utc_partition_denominators_and_channel_wide_bad_book_scope` forges row 1 → `ValueError "coverage content_sha256"` before any write (passed).
- **Synthetic provenance cannot enter a real scope**: `context.synthetic = isinstance(charter.payload.owner_authorization, SyntheticAuthorizationMarker)` (`pipeline.py:822-824`) — a typed charter marker, refused for `full_authorized_development` (`:861-865`) and refused by `validate_verification_run` on the real slice; S00 refuses an MBP-1-bearing plan with no `mbp1_evidence_source` (`:787-793`) and no real entry wires one (grep: only `pipeline.py`); `_ensure_mbp1_evidence` calls `assert_evidence_provenance_permitted(..., synthetic_scope=context.synthetic)` (`:1123-1127`); `build_mbp1_source_artifact_from_paths` refuses `SYNTHETIC_FIXTURE` before any path (`test_synthetic_evidence_is_refused_outside_the_synthetic_scope`: `calls == []`). A synthetic manifest cannot be RELABELED `owner_reviewed` (`_scope_agrees` `:510-515`, test at `:379-380`) — the hole is fabrication from scratch (S3), not relabeling.
- **Order-flow boundary** (`probe_f_boundary.py`): `assert_no_deep_book_identifiers(['publisher_id','flags'])` ok; both schemas carry `publisher_id`/`flags` and no `_01…` column; `_refuse_deeper_book_columns(['flags','bid_px_01'])`, `(['publisher_id','mbp10_flags'])`, `(['flags_mbp2'])` → refused; import-time guard covers all four Arrow schemas (`mbp1_arrow_schemas.py:216-223`) and the registry/feature names (`feature_blocks.py:716-720`, 76 names ok); `_require_no_legacy_provenance("legacy_verified_replay_source")` → refused, and no new code queries that literal; `Mbp1SourceContract` schema `mbp-10` → refused (Literal); `Mbp1CoverageDiagnosticReportPayload.research_boundary='live'`, `completeness_inferred_from_sequence_continuity=True`, `run_scope='full_authorized_development'` → refused (Literal); `Mbp1CoverageReportPayload.research_boundary` default `research_only_offline`, `coverage_policy_id` default `mbp1_source_coverage_declared_evidence_v2`.
- **UI panel**: `st_module.*` calls in `scripts/ifvg_mbp1_panels.py` → `['caption', 'dataframe', 'markdown', 'text_input', 'warning', 'write']` (the `text_input`s are the pre-existing R5B exact-ID inputs); no `button|form(|toggle|selectbox|checkbox|radio|subprocess|Popen|_spawn` (the diff adds only markdown/dataframe/caption text); `test_mbp1_panel_autofills_and_renders_coverage_and_comparison` passes.
- **No real run / import launches nothing**: `python -c "import ifvg_mbp1_coverage_diagnostic"` → `modules imported by the CLI import: none from alpha_lab`; `main` guarded by `__name__`; default `--store-root` = `<repo>\data\ifvg_datasets\search_test\v1` (the designated verification namespace; a bare `main([])` refuses at the run-id check). No code in the R5B.1 set performs a development replay, real materialization, model fit, or operator run; the largest test computation is the synthetic pipeline E2E.
- **Frozen lanes**: `git status --short` shows no M0–M3 module (`context_model.py`, `context_feature_view.py`, `context_folds.py`, `context_experiment_contracts.py`), no `propsim/` file, in the R5B.1 set; `git -C ..\Strategy-Core status --short` → 0 entries; Trade-Lab → 27 pre-existing entries, none with mtime > 2026-08-01 (newest 2026-07-31 14:19); `FINAL-IMPLEMENTATION-PLAN-DOCS` → 15 files, none with mtime > 2026-08-18 (newest 2026-08-17 23:39); `R6.1-CORRECTION-PLAN-DOCS-FINAL/PACKAGE_FILE_SHA256.txt` → all 7 files `OK`.
- **Required test run**: `python -m pytest -q -p no:cacheprovider tests/agents/ifvg_search/test_mbp1_coverage_evidence.py tests/agents/ifvg_search/test_pipeline_run.py tests/agents/test_ifvg_pipeline_tab.py tests/agents/test_ifvg_study_scans.py` → `78 passed in 15.28s` (15 + 19 + 30 + 14). `git diff --check` over the R5B.1 set → exit 0 (CRLF warnings only).
