# R5B.1 — Adversarial Review Resolution

Every finding dispositioned; fixes verified by the re-run suites (final
counts in `TEST_RESULTS.md`). "FIXED" = code/test change in this release;
"ACCEPTED" = recorded without code change (with rationale); "REFUTED" =
disproven with evidence. Reviewer 1 = contract fidelity (F1–F12); Reviewer 2
= safety/access/immutability (S1–S8). Overlaps: S3 ≡ F1 (fabricated
positive completeness), S6 ≡ F4 (no evidence seam), S4 ⊂ F9 (namespace
substring), S2 ⊃ F9 (authorization gate).

Session note: the R5B.1 change set was authored before a context reset; its
first internal review round's report was lost with the context (its fixes
are in the code), so — as R6 did — the adversarial round was re-run in full
by two independent read-only reviewers on the post-fix tree
(`ADVERSARIAL_REVIEW.md`, verbatim).

| # | Sev | Disposition | Resolution |
|---|---|---|---|
| F1 / S3 | MAJOR | **FIXED** | (a) `Mbp1PartitionEvidence` carries `completeness_report` and its validator enforces what only the store loader used to: a manifest asserting completeness REQUIRES the report; report id / scope / provenance / intervals / `positive_completeness_authorized` must agree; a provenance label with no manifest is unrepresentable (`Mbp1PartitionEvidence` and `Mbp1PartitionCoverage` alike — a labeled row must carry a `gap_manifest:` evidence source). (b) `compute_partition_coverage(..., partition_content_refs)` + `_partition_coverage`: a positive claim whose report `verified_partition_refs` do not intersect the partition's content hashes (`content_sha256` of the canonical bytes; the raw file sha256 on the real path — `source_content_refs`) is REFUSED at build. (c) `verified_partition_refs` / `evidence_refs` are SHA-patterned; `owner_review_decision_id` must be a 64-hex decision hash when positive. (d) `load_verified_partition_evidence` populates the report; the real diagnostic consumes evidence only through it (see F4). The fixture computes the certified content hash (`canonical_content_sha256`) before building evidence. Tests: `test_positive_completeness_only_from_the_verified_compiler` (S3/F1 blocks), `test_multi_utc_partition_denominators_and_channel_wide_bad_book_scope` (unbound positive claim refused). Owner-review hash → persisted owner-decision artifact binding is the R6.1 owner-evidence workstream (DEV-R5B.1-2). DECISIONS_TAKEN #73. |
| F2 | MAJOR | **FIXED** | `build_mbp1_source_artifact` refuses any in-session event outside every declared (clipped) partition span ("the coverage evidence contradicts the data"); `day_coverage_views` computes `uncovered_session_intervals` / `uncovered_session_ns` (session span minus the union of declared spans); the materializer types a window touching an uncovered sub-span `coverage_evidence_unavailable` before the declared-gap check. Test: `test_undeclared_hole_between_partitions_types_windows_and_stray_events_refuse` (the reviewer's hole probe: `ofl_win_inversion_entry` / `ofl_snap_entry` typed, `ofl_snap_htf_tap` valid). DECISIONS_TAKEN #70 amended. |
| F3 | MAJOR | **FIXED** | `_partition_slice` is half-open `[start, end)`; `_clipped_spans` refuses overlapping/duplicated spans within a day after clipping to the session span; every event is counted exactly once across contiguous partitions. Test: the multi-UTC test now asserts `sum(row_count) == len(events)` and the overlap refusal. DECISIONS_TAKEN #70 amended. |
| F4 / S6 | MAJOR / MINOR | **FIXED** | `scripts/ifvg_mbp1_coverage_diagnostic.py --evidence-json` → `load_partition_evidence_manifest`: store-VERIFIED manifests (with their compilation reports) plus typed recovery-boundary / condition records per trading day — never in-memory evidence, never a path; the loaded manifest ids ride the report (`partition_evidence_manifest_ids`). `Mbp1PartitionDiagnosticRow.open_uncertainty_to_partition_end` is tri-state (`None` = not computed, enforced together with `partition_scope_evidence_present`). Docs (ARCH_APPEND, D-048, #72) now state exactly what the real run characterizes with and without the seam. Test: `test_partition_evidence_manifest_seam_loads_store_verified_evidence`. |
| S1 | MAJOR | **FIXED (+ documented deviation)** | `read_mbp1_partition_frame` clips the UTC-date file to `[18:00 ET D-1, 17:00 ET D)` AND `ts_event < DEVELOPMENT_CUTOFF_UTC` BEFORE normalization/hashing (the protected 18:00 ET tail of 2026-06-10 — trading day 2026-06-11 — is structurally excluded); the clipped counts and the raw file's sha256 ride `frame.attrs` → the partition row (`rows_outside_session_span`, `rows_after_development_cutoff`, `source_content_refs`). Composition of the previous UTC file's evening portion is NOT done in R5B.1 — recorded as DEV-R5B.1-1 (with declared evidence that span is an uncovered session interval whose windows type `coverage_evidence_unavailable`; without evidence the day is unknown anyway). Test: `test_real_read_clips_to_trading_day_session_and_development_cutoff` (synthetic parquet under tmp; proves the last exposed day's session end IS the cutoff and the 06-11 rows are clipped). DECISIONS_TAKEN #74. |
| S2 / F9 | MAJOR / MINOR | **FIXED** | `assert_diagnostic_authorized(store_root, run_envelope, access_policy, allowlist)` is the R1 gate: `isinstance(access_policy, VerificationReplayPolicy)`; run envelope required and typed; `run.allowlist == days`, `run.allowlist_hash == sha256(days) == ref.approved_allowlist_hash`; policy id on run AND ref; `output_namespace == search_test/v1`; coverage-matrix id equality run↔ref; the `CoverageMatrixEnvelope` verified-loaded from the store with `candidate_allowlist == days`; `register_program_allowlist` (ONE canonical allowlist, V3 P0-7); the policy's `allowed_dates == days`. The CLI routes through it and surfaces `PermissionError`s as `SystemExit`. Tests: the gate matrix in `test_coverage_diagnostic_shape_and_fail_before_path` (absent run / wrong policy / evil namespace / unbacked matrix / different allowlist / canonical-marker mismatch / forged hash) + the CLI refusal of an unbacked persisted envelope. DECISIONS_TAKEN #72/#75. |
| S4 | MEDIUM | **FIXED** | `assert_verification_namespace`: the store root's last two path components must be `("search_test", "v1")`; the CLI's both paths use it. Test: `search`, `search_test_evil`, `xsearch_testx` refused on both paths. |
| S5 | MINOR | **FIXED** | `Mbp1PartitionCoverage.trading_day` / `source_partition_utc_date`, `Mbp1PartitionDiagnosticRow.trading_day` / `source_partition_utc_date`, and `Mbp1CoverageDiagnosticReportPayload.allowlist` are validated with `assert_evidence_date_representable`. Test: sealed allowlist refused in `test_coverage_diagnostic_shape_and_fail_before_path`. |
| S7 | MINOR | **FIXED** | `compile_mbp1_partition_gap_manifest` requires `source_document_sha256 ∈ report.evidence_refs` and `!= report id`. Test: `test_positive_completeness_only_from_the_verified_compiler`. |
| S8 | MINOR | **FIXED (process)** | Shared docs staged via `stage_shared_docs.py` at commit time (`--apply-worktree` after); the commit is path-scoped to the R5B.1 file list (`FILES_TOUCHED.md`); the evidence folder completed before the commit; the concurrent R6.1 files are NOT in the R5B.1 commit. |
| F5 | MINOR | **FIXED** | `end_ts = max(end_ts, flagged_ts + 1, start_ts + 1)` unconditionally — the flagged record is always inside the uncertainty. Test: boundary AT the flag → `[100 s, 200 s + 1 ns)`. |
| F6 | MINOR | **FIXED** | A documented boundary closes only when `flagged_ts <= ts <= partition_expected_end_ts`; otherwise `PARTITION_END_FAIL_CLOSED` with `open_uncertainty_to_partition_end=True`. `Mbp1RecoveryBoundary` is bound to `utc_date` + `physical_partition_key`. Test: boundary at 900 s beyond a 600 s partition. |
| F7 | MINOR | **FIXED** | `_scope_agrees` refuses a dataset-condition record for another dataset/UTC date and a recovery boundary for another partition. Test: both refusals in `test_maybe_bad_book_without_documented_recovery_fails_closed`. |
| F8 | MINOR | **FIXED** | `_validate_window_specs` refuses a resolution whose formula/materializer versions are not the versions this materializer implements; the feature artifact stamps the versions FROM the block. Test: `test_superseded_resolution_is_refused_by_the_materializer` (the exported R5B v1 activation envelope refuses). |
| F10 | MINOR | **FIXED** | `MBP1_PROPOSED_DEFAULTS` (seven R-6-family values, `proposed_protocol_default`, owner decision R-6) surfaced by the MBP-1 panel's new stamp table; `evidence_kinds_accepted` / `recovery_boundary_kinds_accepted` are validated by ORDERED equality (one identity per contract). Test: `test_mbp1_panel_renders_the_stamped_coverage_policy_defaults`. |
| F11 | MINOR | **FIXED** | DECISIONS_TAKEN #48/#49 carry in-place "AMENDED by R5B.1" pointers; `R5B/DEVIATIONS.md` DEV-R5B-6 carries a "SUPERSEDED by R5B.1" note (additive; nothing rewritten); #70/#72, D-048, and `ARCH_APPEND` state the F2/F3/F4/F6 resolutions exactly. |
| F12 | MINOR | **FIXED** | `Mbp1DeclaredGapInterval.contributing_evidence_kinds` — merged intervals carry every contributing kind (test asserts both kinds on the merged interval); `_flag_scope_level` labels `channel_partition` only when the SCOPE is channel-scoped behind a verified map. |

## Post-fix verification

Lane suites after the round: `test_mbp1_coverage_evidence.py` 18 +
`test_mbp1_materializer.py` 26 + `test_mbp1_schemas_and_source.py` 22 +
`test_feature_blocks.py` 11 + `test_identities.py` 19 +
`test_bundle_feature_view.py` 9 + `test_pipeline_run.py` 19 +
`test_controlled_feature_study.py` 7 + `test_ifvg_pipeline_tab.py` 31 +
`test_ifvg_study_scans.py` 14 — all passed; `ruff check src tests scripts`
clean; `git diff --check` clean. Release-final full-suite counts (run on the
committed tree in a detached worktree, once as-is and once with the API keys
cleared): `TEST_RESULTS.md`.
