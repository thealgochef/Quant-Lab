# Plan: Produce the QL-FSM-PROP-SEARCH-DASHBOARD Implementation Plan Document Set

> **PROVENANCE ONLY — SUPERSEDED.** This planning-process record is not implementation authority. The current package incorporates the first revision, Amendment V2, V3, and the V4 final contract-closure patch. `README.md`, `IMPLEMENTATION_PLAN.md`, the supporting authority documents, and `FINAL_CONSISTENCY_AUDIT.md` govern.

## Context

The user has a 5,355-line planning brief (`QUANT_LAB_FSM_PROP_SEARCH_DASHBOARD_PLANNING_BRIEF_V4.md`, in `C:\Users\gonza\Documents\Claude-Quant-Lab\QL-FSM-PROP-SEARCH-DASHBOARD\`) defining a new Quant-Lab research lane: an FSM configuration search + prop-firm realization workspace with an ML/regime/spectral-clustering contract and a guided dashboard. The brief instructs the planning agent (me) to inspect Strategy-Core and Claude-Quant-Lab, then produce a **codebase-specific implementation plan document** (35 required sections per brief §18) plus supporting docs, all saved into the same `QL-FSM-PROP-SEARCH-DASHBOARD` folder. **No production code changes** — this task is documentation-only (the brief itself says "Stop after the plan").

## Deliverable (what will be written after plan approval)

All files go in `C:\Users\gonza\Documents\Claude-Quant-Lab\QL-FSM-PROP-SEARCH-DASHBOARD\`:

1. `IMPLEMENTATION_PLAN.md` — the master plan covering brief §18 items 1–35 (executive recommendation, architecture map, gap analysis, exact backend/frontend file changes, contracts/schemas, identity model, orchestration, prop engine, search/robustness protocol, insights, UI map, reuse-vs-rewrite, migration, access safety, performance, test matrix, phases, acceptance mapping, owner decisions, risks, expected files, study-cell identity, delta taxonomy, feature blocks incl. MBP-1-only, effects design, 5-day verification policy, full-pipeline contract, MBP-1 confirmation, model ladder, regime schemas, spectral restrictions, Nyström design, drift monitoring, no-code-changed confirmation).
2. Supporting docs (split out so the master plan stays navigable):
   - `ARCHITECTURE_MAP.md` — current-state codebase map (QL + SC) with exact paths/symbols.
   - `CONTRACTS_AND_SCHEMAS.md` — full field-level schema specs for all new contracts.
   - `TEST_MATRIX.md` — complete test matrix incl. 5-day verification fixtures + synthetic ML fixtures.
   - `OWNER_DECISIONS.md` — brief §17's 32 decisions classified blocking/deferred with recommended defaults.
   - `DELTA_TAXONOMY.md` — §7A dimension registry, comparison classes, computation-path matrix, metric families.
   - `ML_REGIME_CONTRACT_PLAN.md` — §7B supervised ladder, regime algorithm registry, spectral restrictions, fold-local fit design.
   - `PHASED_DELIVERY.md` — milestone-by-milestone sequencing with per-phase acceptance gates (may fold into master plan if short).
   - `README.md` — index of the folder.

## §18 output-item → document mapping

- IMPLEMENTATION_PLAN.md: items 1 (exec recommendation), 3 (gap analysis), 4–5 (exact backend/frontend changes, summarized w/ pointers), 7 (identity/immutability model), 8 (orchestration), 9 (prop design), 10 (search/robustness), 11 (insights), 13 (reuse vs rewrite), 14 (migration/catalog), 15 (access safety), 16 (perf plan), 18 (phases summary), 19 (acceptance mapping), 21 (risks), 22 (expected files), 27–29 (5-day policy, full-pipeline contract, MBP-1 confirmation), 35 (no-code-changed confirmation).
- ARCHITECTURE_MAP.md: item 2 (current architecture map, QL+SC exploration findings).
- CONTRACTS_AND_SCHEMAS.md: items 6–7 detail (all field-level schemas), 30 (model ladder contracts).
- DELTA_TAXONOMY.md: items 23–26 (study-cell identity, dimension registry, delta taxonomy, computation paths, compatibility, feature blocks/bundles incl. MBP-1-only + MBP-10 exclusion, effects/population-delta design).
- ML_REGIME_CONTRACT_PLAN.md: items 30–34 (ladder, regime schemas/registry, spectral restrictions, Nyström design, stability/alignment/dashboard/drift).
- TEST_MATRIX.md: item 17 (full test matrix + 5-day fixture proof strategy detail).
- OWNER_DECISIONS.md: item 20 (§17's 32 decisions classified w/ recommended defaults).
- PHASED_DELIVERY.md: item 18 detail (milestone-by-milestone file lists + gates). UI IA/component map (item 12) lives in IMPLEMENTATION_PLAN.md §frontend.
- README.md: folder index.

## Hard constraints from the brief (must be honored in the plan content)

- MBP-1 only; MBP-10 excluded everywhere (no schema/UI reservation implying it).
- Implementation verification capped at ≤5 real trading days (one frozen allowlist; warmup+evidence ≤ 5); full pipeline is a UI-launched post-implementation operator action only.
- Separate research lane/contract from M0–M3; no silent adaptive search under the fixed-tier contract.
- Full sequential SC replay per strategy config; never derive execution from candidate-table filtering.
- Prop sims: exact historical + day-block bootstrap + stress; copied accounts share one path.
- Feasibility gates → Pareto frontier → lexicographic tie-breaks; no hidden weighted score.
- Immutability: SHA-256 identities, atomic saves, overwrite refusal, reuse-on-identical-identity.
- Sealed/protected boundary: authorized ≤ 2026-06-10T21:00:00Z; June 11 protected; June 12+ sealed; zero access counters.
- ML: prevalence/logistic/CatBoost ladder; fold-local preprocessing; KMeans/minibatch/GMM/spectral(train-only)/Nyström+KMeans registry; cluster IDs nominal; no auto-selection.
- Plan-only: no code, tests, artifacts, catalogs modified.

## Process status

- Phase 1 (exploration) — DONE: 3 Explore agents covered QL backend, QL dashboard, Strategy-Core; findings below.
- Phase 2 (design) — DONE: 3 Plan agents produced backend, ML/regime/delta, and frontend designs (full text held in context; summaries below).
- Phase 3 (review) — DONE: cross-design reconciliation applied (decision-ID renumbering, shared seams verified consistent); load-bearing claims spot-checked directly.
- Phase 4 (after approval) — Execution steps, in order:
  1. Write `ARCHITECTURE_MAP.md` (current-state map from exploration findings).
  2. Write `CONTRACTS_AND_SCHEMAS.md` (field-level schemas: charter/child/axis registry, prop contract/account/risk/portfolio/stress/simulation, verification + pipeline contracts, stores).
  3. Write `DELTA_TAXONOMY.md` (study-cell, dimension registry, comparison classes, computation paths, delta families, feature blocks/bundles + MBP-1 contract).
  4. Write `ML_REGIME_CONTRACT_PLAN.md` (ladder, regime registry, spectral/Nyström, fold-local fit, calibration/decision/drift).
  5. Write `TEST_MATRIX.md` (synthetic + 5-day fixtures, acceptance-gate mapping, perf budgets).
  6. Write `OWNER_DECISIONS.md` (all 32 §17 items classified with recommended defaults).
  7. Write `PHASED_DELIVERY.md` (M0–M9 with per-milestone file lists and gates).
  8. Write `IMPLEMENTATION_PLAN.md` (master doc: exec recommendation, gap analysis, reuse-vs-new, orchestration/prop/frontier/insights, UI IA + component map, migration, access safety, performance, risks, expected-files list, §18 confirmations incl. MBP-1-max/MBP-10-excluded and no-code-changed).
  9. Write `README.md` (folder index), cross-link all docs, final §18 completeness check (items 1–35).
  All files go to `C:\Users\gonza\Documents\Claude-Quant-Lab\QL-FSM-PROP-SEARCH-DASHBOARD\`. No code, tests, catalogs, or artifacts are touched.

## Design-phase results (Phase 2)

### Backend design (received in full — held in context for write-up)
- Placement: new `src/alpha_lab/agents/data_infra/ifvg/search/` subpackage (axis_registry, charter, child_identity, gates, strategy_metrics, orchestrator, child_replay, frontier, robustness, insights, failure, store, catalog, verification, pipeline) + `src/alpha_lab/propsim/` extension (firm_contracts, account, risk, adapters, portfolio, stress, prop_metrics, simulation). Decisions D-039..041 reserved.
- Axis registry classifies every IfvgSmcSection field: ~19 searchable-today axes (timeouts, invalidation flags, distances, retention, sessions, shorts-unratified, TF sets marked expensive), inert traps blocked (break_even_enabled, legacy_candidate_row_limit, parent_reaction_window_1m_bars_max), reducer-hardcoded blocked (fill/resolver/stop-anchor/retest-family/1m-gaps/ranking/flatten). Charter = frozen pydantic `IfvgStrategySearchSpec` w/ ObjectivePolicy (no weight fields — structurally impossible), DatePolicy, SimulationProtocol; `charter_search_id` via canonical_contract_sha256. Child identity via `resolve_profile_config` + section_config_hash; `enumerate_children` = itertools.product + dedupe.
- Orchestrator: ParentPhase/ChildStatus StrEnums per brief §9.6; `_profile_lock` O_EXCL + `_write_json_atomic` checkpoints + `cancel.requested` sentinel; ProcessPoolExecutor 1-process-per-child max_tasks_per_child=1; artifact prewarm phase grouped by artifacts_tag; reuse-by-identity from stores. Child worker lifts run_ifvg_tf_variant.py flow; promotes `_table_content_hash`→`table_content_hash` for trade-stream hash.
- Prop: `PropFirmContractSpec` (PhaseRules eval+funded, PayoutPolicy incl. waiting/cap/split/threshold-effects, FeeSchedule); dollar-space `AccountWalk` (EvaluationWalk untouched, parity-tested); `PropRiskPolicySpec` + `size_trade`; `PropTradeRecord` adapter from EXECUTED_TRADE tables; portfolio one-common-path (shared day-resample per bootstrap path); STRESS_SCENARIOS_V1 (9 frozen); `PropSimulationSpec/Result`; §9.15 metric blocks + `PayoutReliabilityVector` (§9.16, no scalar collapse).
- Frontier: O(n²) non-dominated sort, dominance edges, tie-break trace persisted, per-firm champions; robustness (neighbor_map ±1 step, plateau width, knife-edge warnings); FailureReason StrEnum (14 values); deterministic insight engine (typed EvidenceRef + INSIGHT_RULES_V1).
- Verification: `VERIFICATION_ALLOWLIST_V1 = 2026-06-04/05/08/09/10` (last 5 trading days ≤ cutoff); new `VerificationReplayPolicy` (policy_id `verification_fixed_allowlist_max5_v1`) as third trusted branch in `require_fixed_exploration_allowlist`; warmup=0 real days via precomputed doc-default seed snapshot; fail-before-path-construction asserts; `search_test` namespace stores. Full-pipeline: `QuantLabPipelineRunSpec` + 16 stages S00–S15 + StageStatus + StageExecutor protocol + estimates; S11 BLOCKED v1 (model-gated family not authorized); full scope constructible only via UI launch token.
- Stores: generic `SearchStore` (manifest protocol clone) × 9 kinds under `data/ifvg_datasets/search/v1/`; mutable display catalog separate. Test matrix: rule-by-rule prop fixtures, §16.3 2×2×2×2 synthetic charter, §16.5 sim acceptance, registry fail-closed tests, 5-day end-to-end (~5 min), negative 6th-date test; perf budgets from measured 15 s/day. **SC changes for v1: NONE (validated).** M1–M8 file lists mapped. §17 items 1–24 classified (blocking-owner: 1,2,3,5,7,8,15,21,24-ish; rest deferred/engineering w/ defaults).
- Flagged assumptions to verify in M1: capture-driver mid-chain start w/ cached day artifacts (fallback = driver param, QL-only); PERMITTED_DEVELOPMENT_DATES = calendar incl. weekends (138 trading); §16.3 "parent-fill policies" mapped to parent_full_fill_invalidation.

### Frontend design (received in full — held in context for write-up)
- Verified: Streamlit pin >=1.38, installed 1.54; `st.fragment(run_every)` (1.37) + `on_select="rerun"` (1.35) + `st.dialog` (1.37) usable; `pinned=` columns need >=1.39 → **recommend pin bump to >=1.41**; no segmented_control/badge.
- IA: Experiments sub-nav = **`st.radio(horizontal=True)`** (not nested st.tabs — only selected panel executes, programmatic nav possible, asserted labels untouched): `New Study | Active Runs | Results | History | Context Research` (last = existing M0–M3 panel verbatim). New files: scripts/{ifvg_ui_common, ifvg_study_tab, ifvg_study_wizard, ifvg_active_runs_tab, ifvg_results_tab, ifvg_results_compare, ifvg_results_charts, ifvg_search_job, ifvg_pipeline_tab, ifvg_pipeline_job}.py + src/.../ifvg/{study_status, study_presentation}.py (pure logic in src for CI lint/mypy coverage). State prefixes `ifvg_study_v1_` / `ifvg_pipeline_v1_`.
- Wizard: 8 steps, session-state step index, validation-gated Next, **disk-persisted drafts** (mutable drafts/ dir), Clone-as-New-Search = deep-copy to new draft; axis cards grouped by market meaning w/ classification badges — locked/blocked have NO widget; §7A.14 interpretation selector on session/direction axes; combination preview separates expensive replays vs cheap prop sims; freeze → `st.dialog` double-confirm (full scope types name) → detached Popen launch (`ifvg_search_job.py`, CREATE_NO_WINDOW pattern).
- Monitor: `st.fragment(run_every="5s")` wrapping plain body fn (AppTest-callable), reads atomic status JSONs; funnel = 5 st.buttons (clickable filter) + go.Funnel; child table `on_select` → detail + jump; safe-cancel writes sentinel via dialog.
- Results: overview 4 cards + exact "No configuration passed." state; frontier go.Scatter (x=payout, y=P(payout/30d), color=breach, size=lifetime) w/ on_select + **always-present selectbox fallback** (accessibility + AppTest); sensitivity heatmap w/ glyph cell classes (◼▲✕·⊘, not color-only); firm matrix; survival curves (step lines, dash-per-firm); payout distributions (P10 shaded/prominent, gross/costed/net selector); explorer w/ column presets + pagination via indexed `load_child_summary_page`; config names from baseline diff; comparison = dimension-diff ribbon + 4 radio panels w/ funnel-delta row→exact-setup drill; account timeline w/ event markers → jump; insight panel renders artifact text verbatim.
- Drill-down: extend `queue_jump` kinds + `setup_id` (update ValueError contract tests); `queue_replay_drilldown` helper writes verifier pair key + toast (top-level tabs can't switch programmatically — existing pattern).
- Pipeline UI: Configure/Preview/Launch/Monitor/Publish radio; scope selector w/ mandatory non-dismissible verification badge; exact §10.12A confirm text; 16-stage list w/ glyph states; resume/retry-same-identity; verify-then-activate publish (two buttons, second disabled until gates pass); Popen only in button handler (AppTest asserts no launch on render).
- Safeguards: `study_status.py` vocabulary + FORBIDDEN_STATUS_TERMS (best/winner/production ready); forbidden-label source-scan extended to new modules; EmptyState enum w/ fixed copy (9 states); dev-only badge; result-scope captions; sanitizer extraction to ifvg_ui_common.
- QA: AppTest matrix (8 new test modules), pure-figure tests, viewport pass at 4 sizes w/ screenshots, CI extended `ruff check src tests scripts` (prep lint-fix commit), pin bump.

### ML/regime/delta design (received in full — held in context for write-up)
- Placement: three new subpackages `src/alpha_lab/agents/data_infra/ifvg/{study,features,ml}/` + test fixtures pkg; M0–M3 lane files NOT modified (tier registry frozen; new lane additive). Verified sklearn 1.7.0/catboost 1.2.10 present — no new deps.
- `study/`: `ExperimentDimensionSpec` registry (17 DimensionTypes, requires_*/can_change_* flags, fail-closed resolve), `StudyCellIdentity` (17 axis sub-identities mapped to EXISTING hashes: artifact_pair_hash, section_config_hash, label derivation id, bundle_schema_hash, resolved model protocol hash, fold protocol Literal 40/5/5/2, none_* baselines for backend axes), `BASELINE_STUDY_CELL`, `study_cell_from_context_run` adapter for legacy runs; `ComparisonSpec/Compatibility/Result` (20 ComparisonClasses; REQUIRED_EQUALITIES table; class/changed-set mismatch refusal; incompatible → config-diff-only); `derive_computation_path` = pure OR-fold + closure rules, unit-tested against §7A.5 row-by-row; 23 DeltaOutputFamily values + DELTA_FAMILY_SELECTION auto-mapping + producer table (existing binary_prediction_report/compute_trade_stats/propsim feed most families); `population_delta` (common/added/removed/Jaccard/first-divergence w/ registered divergence reasons, never-guess `unattributed`); `funnel_delta` over DayFunnelRecord counters; `contrasts.py` (declared-only, UnbalancedDesignError, denominator recorded, causal_language_permitted=False).
- `features/`: `FeatureBlockSpec` (all §7A.11 fields) + registry seeding that PARTITIONS the existing M0–M3 tuples exactly (CORE=M0−session, SESSION, STRUCTURE=M1, STRUCTURE_240 experimental, DISPLACEMENT=M2, LIQUIDITY=M3) + 5 planned blocks (ORDER_FLOW_MBP1, VOLATILITY, REGIME, KEY_LEVEL, EXECUTION_LIQUIDITY); partition-exactness assert; `Mbp1SourceContract` w/ `order_flow_depth_policy: Literal["mbp1_only_v1"]` (only representable value) + `MBP1_MISSING_REASONS` + regex guard test banning mbp10+ identifiers; `FeatureBundleSpec` graph (B0–B6 + TIER_M*_FROZEN_V1 bundles assert-equal to TIER_FEATURE_REGISTRY, order-exact); `resolve_bundle` refuses non-AVAILABLE blocks; activation = status flip + materializer, no redesign.
- `ml/`: MODEL_PROTOCOL_REGISTRY (prevalence=existing, NEW `ifvg_context_logistic_l2_v1` w/ pinned params + fold-local impute/scale/one-hot pipeline + versions-bearing resolved hash, CatBoost=existing, GAM=planned); `run_supervised_ladder` asserts identical oos_row_id sets across rungs; `ProhibitedSelectionError` guardrails; RegimeModelSpec/FitArtifact/Assignment/Coverage/Stability contracts (all §7B.4 fields, seed Literal 7); REGIME_ALGORITHM_REGISTRY (kmeans_v1 + minibatch + gmm diag-only implemented; spectral_train_only implemented-as-diagnostics forced BLOCKED_NO_OOS_ASSIGNMENT, affinity train-fold-only, 20k-row/512MB bounds, no test assignments by construction; nystrom_kmeans_v1 code+synthetic tests, research-availability planned; surrogate planned); fold-local preprocessing dual-persisted (canonical JSON payload hash + joblib) w/ reload-retransform allclose check; `regime_alignment` Hungarian centroid matching (reporting-only, prediction-hash invariance test); promotion gates (occupancy 5% ∧ ≥25 rows, OOS exists, leakage validator, AMI≥0.5 advisory); calibration registry (raw-only V1); DECISION_POLICY_REGISTRY (6 policies, all Literal-True force gated-replay + new trade-stream hash, `ModelGatedReplayRequest` emitted to backend orchestrator); `PropOutcomeAsTargetError`; drift report builders w/ no consumer API (automation structurally impossible); 4 synthetic fixtures (supervised/known-cluster/two-ring-spectral/oos-assignment). §7B.22 20/20 + §7A.19 15/15 acceptance mapped. §17 items 25–32 classified (28/29 blocking-charter-content: candidate-stage@entry grain, fixed_k=3).
- Flags: v2 uuid5 content-derivation to confirm w/ replay-determinism parity test; propsim report schema treated as opaque payload.

## Cross-design reconciliation (to apply during write-up)
1. **Decision-ID collision**: all three designs claimed D-039..041. Renumber in final docs: D-039 search lane+stores, D-040 VerificationReplayPolicy, D-041 child-audit parity exemption, D-042 study-cell+dimension registry, D-043 feature block/bundle graph (M0–M3 frozen as bundles), D-044 supervised ladder+regime registry+spectral restrictions, D-045 UI sub-nav+status vocabulary+pin bump (frontend).
2. Verification allowlist 2026-06-04/05/08/09/10 confirmed as Thu/Fri/Mon/Tue/Wed trading days (06-06/07 weekend); within EXPOSED_DEVELOPMENT_DATES.
3. Consistent shared seams (no conflicts): promoted `table_content_hash`, `VerificationReplayPolicy` (backend owns, ML+pipeline consume), `queue_jump`+setup_id (frontend owns, backend EvidenceRef feeds), status-file contracts (backend writes, frontend fragment reads), `ModelGatedReplayRequest` (ML emits, backend S11 executes — BLOCKED in v1).
4. Spot-checked personally: profiles.py:85 `resolve_profile_config`/`section_overrides`; development_access.py cutoff constants.

## Headline conclusions the documents will present

1. **Feasible with zero Strategy-Core changes in v1.** Every first-wave search axis is reachable through the existing `resolve_profile_config` + `section_overrides` plug point (proven by the shipped retest-480 variant script); reducer-hardcoded axes (fill policy, stop-first branching, retest family, non-1m gaps…) are registered as `blocked_reducer_hardcoded` and deferred to a separately approved SC pin bump.
2. **Heavy reuse**: manifest immutable-store protocol, M0–M3 contract idioms, `run_ifvg_tf_variant.py` child-replay flow, `preparation.py` job/lock/cancel machinery, `alpha_lab.propsim` engine/bootstrap/presets, existing verifier drill-down bus, AppTest harness. Net-new: axis registry + charter/child identity, parent orchestrator, funded-phase/payout/fees account walk + risk policies + portfolio/stress, Pareto/robustness/insights, dimension registry + delta taxonomy, feature block/bundle graph, logistic rung + regime lane, wizard/monitor/results/pipeline UI.
3. **Three structural blockers have designed solutions**: (a) fixed-allowlist access gate vs the ≤5-day cap → new `VerificationReplayPolicy` third trusted class + precomputed seed snapshot (warmup 0 real days); (b) fsm-audit parity gate pinned to doc-default → parity-exempt child-audit variant with explicit non-baseline marker; (c) prior search runner filters candidates post-hoc (violates §9.5.9) → not reused for FSM deltas; only its ledger/budget idioms are imitated.
4. **Verification allowlist recommendation**: 2026-06-04/05/08/09/10 (last five trading days ≤ cutoff), warmup+evidence = 5.
5. **Prop engine**: evaluation-phase `EvaluationWalk` untouched; new dollar-space `AccountWalk` adds funded phase, payouts (waiting/cap/split/threshold effects), fees, breach/replacement, risk-policy sizing, one-common-path portfolios, 9 frozen stress scenarios, and the §9.16 payout-reliability vector.
6. **MBP-1 is the maximum order-flow depth everywhere**: `order_flow_depth_policy: Literal["mbp1_only_v1"]` is the only representable value; a regex guard test bans any mbp10+ identifier in registries/UI.
7. Owner decisions: ~10 of the 32 §17 items are genuinely blocking (first axes/values, firms, gate thresholds, 5-day allowlist sign-off, nested-WF mandate, publication UX, regime grain + fixed_k); all get recommended defaults; the rest are resolved as engineering decisions in the docs.

## Findings (from exploration)

### QL backend (explored — full report received)
- Package `alpha_lab` (src-layout, py3.13, pydantic v2, catboost, sklearn, duckdb; `strategy-core @ git+...@a4e3303` pin). No `[project.scripts]`; invoked as `python scripts/X.py` or `python -m alpha_lab.propsim`. CI: `ruff check src tests` + pytest (scripts/ NOT linted).
- **Immutable store discipline** = `ifvg/manifest.py`: `canonical_sha256`/`file_sha256`/`source_tree_hash`/`RepositoryState`; save protocol = gate-reports-pass → 64-hex `dataset_id` → refuse-if-exists → tmp dir + validate PK/FK/recon → manifest w/ `manifest_payload_sha256` → `os.replace`. Three savers: v2 (`data/ifvg_datasets/v2/`), v3, fsm_audit (`ifvg_fsm_audit_v1`, 13 AuditTables, on-disk id 7e55ee89…). Verified reloaders in `ifvg/artifact_io.py` (`resolve_artifact_directory` exact-ID no-listing). Lightweight immutable store to clone: `ifvg/context_run_store.py` (`_atomic_directory`/`_publish`) + `_persist_and_verify_*` save→reload→assert.
- **M0–M3 experiment lane = contract template**: `context_experiment_contracts.py` (frozen pydantic `extra="forbid"`, `canonical_contract_sha256`, `context_run_identity` folds in exact OOS prediction stream; `PROFILE_CAPABILITY_REGISTRY`, `ArtifactPreparationStatus`, `ContextFeatureTier`); folds `context_folds.build_context_folds` (expanding walk-forward 40/5/5, setup-grouped boundary exclusion + interval purge + 2-day embargo + dup-OOS assert + per-fold prevalence); model `context_model.resolve_context_model_protocol` (pinned CatBoost params, protocol hash incl. lib versions) + `run_context_fold_models`; metrics `context_statistics.binary_prediction_report` (Brier/ref-Brier-from-prevalence/BSS/logloss/AUC/calibration/reliability/threshold grid) + `block_bootstrap_interval` (10k, seed 7) + `paired_tier_delta_report`; orchestration `context_reporting.execute_context_experiment` + `context_experiment_service.run_and_catalog_context_experiment`.
- **Search infra**: `scratch_ifvg_search.py` (962 LOC untracked, repo root) = reference parent orchestrator (budget cap 100, phase/day-window policy, mkdir-lock ledger, `_sealed_precheck/postcheck`, atomic ledger writes, subcommands recon|run|status|declare-shortlist|validate|export-*). `IFVG_SEARCH_LEDGER.json` (meta + 45 entries; every entry carries sealed_ledger_count:0). `ifvg_search_trades/*.csv` = propsim-ready trade stream (setup_id, trading_day, direction, session, risk_points, net_r, mfe_r/mae_r...). CAVEAT: legacy `run_ifvg_experiment`/`run_sealed_validation` hard-refuse repaired v2 lane — new search must go via `run_ifvg_v2_evaluation`/context lane.
- **FSM config axis exists**: `ifvg/profiles.py::resolve_profile_config` w/ `PROFILE_BUILDERS` (5 named profiles) + free-form `section_overrides` (escalation guards), `section_config_hash`/`evaluation_config_hash`, `IfvgCaptureConfig.capture_tag()/artifacts_tag()`; replay driver `dataset.build_ifvg_v2_capture(dates, cfg, resolved_profile, access_policy, cached_artifacts_only, progress_fn)`. **No enumerator/grid/axis registry, no per-config batch driver.**
- **Jobs**: `ifvg/preparation.py::run_resumable_preparation_job` (date-boundary checkpoints, cancel.requested sentinel, `_profile_lock` O_EXCL, `_write_json_atomic`, `PreparationJobState`); `w3_cache_warmer.py` process-pool template (max_tasks_per_child, spawn-safe, skip-if-loads, self-heal, peak-RSS). Replays are chained (prior-day seeds) → sequential per config; parallelism at day-cache layer only.
- **Safety**: `ifvg/development_access.py` — `DEVELOPMENT_CUTOFF_UTC=2026-06-10T21:00:00Z`, `FROZEN_WARMUP_DATES` (10), `PERMITTED_DEVELOPMENT_DATES`, `SourceDateClass` (PROTECTED_BUFFER=06-11, SEALED>=06-12), `DevelopmentDataAccess` (authorize-before-path, no parent listing, event-chain sha), `assert_zero_protected()`, policy id `development_explicit_dates_before_path_v2`. Sealed look counter: `experiment.sealed_ledger_count()` reading `data/ifvg_experiments/sealed_ledger.jsonl`.
- **Cost/labels**: `NQ_COST_POINTS_ROUND_TURN=0.514`, `DOLLARS_PER_POINT=20.0`, `trade_stats.compute_trade_stats` (executions-only, Wilson/mean CI, cluster bootstrap); label kernel in SC `resolve_ifvg_outcome` (`next_1m_bar_stop_first_v1`), family-qualified label rows (`static_r_{r}_next_bar_stop_first_v1`), `context_labels.derive_context_candidate_labels` + bar-evidence hash.
- **PROPSIM EXISTS**: `src/alpha_lab/propsim/` — `TradePath`/`Ruleset`/`WalkResult` (18:00 ET day roll), `EvaluationWalk` (BREACH_MODES realized_only|unrealized_adverse_first; trail styles eod_floor_realtime_breach|intraday_peak_trail|static_floor; soft-DLL halt; consistency; min_days; max_eval_days→expired), `group_by_day`, `walk_days`, `run_bootstrap` (day-block MC 10k seed 42, Wilson CI), `PRESETS` (topstep_50k, apex_50k_eod, apex_50k_intraday, tpt_50k_test — presets are data), `build_report` (fill-col × breach-mode matrix), `format_human`, CLI. tests/propsim/ 6 files. Missing: IFVG executed-trade/`ifvg_search_trades` loader, multi-config×preset sweep, funded-phase/payout/fees model (engine is evaluation-phase pass/bust only!), immutable persistence.
- **Gaps (must create)**: Pareto/frontier (none), sensitivity generator (none), SearchCharter contract (none), tracked search runner in src/, immutable search-run store (legacy 8-hex store is mutable), v2-native config-sweep evaluator, FSM axis registry/enumerator, TradePath adapter from EXECUTED_TRADE tables, batch prop-sim + persistence, payout/funded-account state machine (fees/payouts/replacement NOT in propsim), trade-stream fingerprint helper (promote `dataset._table_content_hash`), parent-level checkpoint over configs, docs (next decision id **D-039**; ARCHITECTURE.md §2e; docs/README.md; docs/pipeline_state.yaml).
- Conventions: frozen pydantic for boundary specs (D-001), dataclass(frozen) for internals, StrEnum, MappingProxyType registries, JSON+MD paired reports, `docs/DECISIONS.md` D-0NN format (latest D-038 = propsim OOS columns), governing docs AGENTS.md/CLAUDE.md/ARCHITECTURE.md/docs/pipeline_state.yaml (update-docs-in-same-change rule). Test fixture pattern: `tests/agents/ifvg_v3_fixtures.py::context_fixture` (synthetic verified pair).

### QL dashboard (explored — full report received)
- **`dashboard-ui/` is DEAD** (orphaned node_modules only; old prototype deleted in 2026-07 cleanup). QL UI = pure **Streamlit + Plotly**, entry `scripts/dashboard.py` → `main()`; 8 top-level `st.tabs`, incl. `🧪 IFVG Lab` → `render_ifvg_lab_tab()` with sub-tabs `Experiments / Replay / Verifier / Data & Audit` (matches brief §10.1).
- UI convention: `*_tab.py` (widgets/caching/layout, `st_module=st` injection seam) + `*_charts.py` (pure figure builders) in `scripts/`; pure providers in `src/alpha_lab/agents/data_infra/ifvg/`.
- Verifier drill-down target: `scripts/ifvg_verifier_tab.render_verifier_section(st_module, pair, entry)`; cross-tab jump bus `queue_jump(kind, value)` (kinds: candidate_id|decision_id|trade_id) + `_apply_pending_jump`; selectable-dataframe→jump recipe at `_render_execution_report:531`.
- 3-pane chart: `scripts/ifvg_verifier_charts.build_verifier_figure` (1m/parent/HTF), `LAYER_BUDGETS`+`OmissionReport` honest truncation, `to_display_timezone` (America/New_York).
- PIT provider layer: `src/alpha_lab/agents/data_infra/ifvg/replay_chart_provider.py` (`open_replay_context`, `candidate_evidence(mode, stage)`, `bars_for_pane`), `replay_chart_store.py` (`REPLAY_CHART_STORE=data/ifvg_datasets/replay_chart/v1`, `AUTHORIZED_CUTOFF_UTC="2026-06-10T21:00:00Z"`), `setup_verifier_provider.py`, `visual_review_store.py` (append-only JSONL ledger + CSV export).
- Caching doctrine: `@st.cache_resource`/`@st.cache_data` keyed by full artifact IDs + manifest hashes; hash re-verified after load; short IDs display-only.
- **Existing prop-sim package: `src/alpha_lab/propsim/` (engine.py, loaders.py, presets.py, report.py, models.py, bootstrap.py) — CLI-only (`python -m alpha_lab.propsim`), ZERO UI wiring.** Major reuse candidate for §9.9–9.16.
- Job machinery exists but UI-blind: `scripts/ifvg_recapture_job.py` (detached process + JSON status file, `write_job_status`/`read_job_status`), `preparation.py` (`PreparationJobState`, `read_preparation_state`, `_profile_lock`, `run_resumable_preparation_job`). No `st.fragment`/auto-refresh/polling anywhere.
- Search-run data (IFVG_SEARCH_LEDGER.json, ifvg_search_runs/, ifvg_search_trades/) produced by root `scratch_ifvg_search.py` — **no contract module, no store class, no UI reader, no tests**.
- Testing: pytest + Streamlit AppTest (pattern in `tests/agents/test_ifvg_lab_tab.py:406-427`), source-introspection contract tests (forbidden button labels `delete|sealed|recapture|promote|unlock`; `allow_sealed` banned). **CI lints/type-checks `src tests` only — `scripts/` (all UI) is outside CI lint/mypy.**
- Orphans to decide: `scripts/ifvg_lab_charts.py` (1150-line semi-orphan with tested-but-unwired `build_equity_figure`/`build_r_histogram_figure`/`build_calibration_figure`/`build_coverage_figure`).
- Gaps: no wizard/stepper, no live job monitor, no in-UI long-job launcher, no frontier/heatmap-of-configs views, no md-report viewer, no pagination, no deep links, no theming, duplicated `_sanitize_error`.
- Canonical UI contract doc: `docs/ifvg/IFVG_LAB_UI_RESTORATION_MAP.md` (reuse/rewrite + acceptance map); `AGENTS.md` governs.

### Strategy-Core (explored — full report received)
- SC = library only (`strategy-core`, no entry points), `PLATFORM_VERSION="strategy_core_platform_v1"` (engine_version symbol renamed at E1; v1→v3 lineage in `__init__.py` docstring), `CONTRACT_VERSION="trade_lab_contract_v3"`. IFVG FSM in `strategies/ifvg_smc/` (reducer.py 2798L = FSM S0→S5, single `_setup` slot, one-active-setup/trade structurally enforced; counterfactual streams while occupied).
- **Profile = `IfvgSmcSection`** (pydantic, extra=forbid) — full field list captured (sessions/doc_sessions/enabled_entry_sessions/outside_session_policy, enable_longs/shorts, htf/parent timeframes, parent_reaction_window, parent_retest_timeout_1m_bars, opposing/inversion timeouts, post_inversion_expiry, distance/locality, retention, swings, sl_buffer/tp_r, entry families, causality triple, resolver_policy, invalidation flags, max_executed_trades_per_day). `ifvg_profile_hash(section)` = identity; 5 named builders (only doc-default runnable). INERT-BUT-HASHED trap: `break_even_enabled`, `legacy_candidate_row_limit` (change hash, no behavior).
- **Child plug point (strongest finding)**: `QL profiles.resolve_profile_config({"profile_name": base, "section_overrides": {...}})` → `dataclasses.replace(IfvgCaptureConfig(), section=...)` → `build_ifvg_v2_capture` → `run_ifvg_v2_evaluation`. Working single-child template: `QL/scripts/run_ifvg_tf_variant.py`; retest480 variant script already flips `parent_retest_timeout_1m_bars=480` (D-6 ratified 2026-08-11).
- Replay: `run_day(bars_by_tf, section, seed, trading_day, levels_for, audit_capture_mode, context_*)`; multi-day = caller loop carrying `end_seed`; seed profile-hash-checked; cache-trust theorem (`test_ifvg_replay_parity.py`) licenses per-day chunking. No RNG anywhere; total-order keys everywhere.
- Audit channel: `audit_capture_mode="fsm_audit_v1"` opt-in, not in profile hash, core emissions byte-identical on/off; AuditStamp ordering authority; QL builds 13 AuditTables.
- Costs: SC frictionless (entry=confirmation close, stop-first same-bar, gross ticks); costs live in QL (`NQ_COST_POINTS_ROUND_TURN=0.514`).
- Perf: reader ~98% of day cost (vectorized ×13, 2.87-3.36 µs/event); **v2 replay on CACHED artifacts ≈15 s/day (138-day chain <~35 min incl. audit+parity+save)**; new TF set forces sequential seed-chained artifact rebuild (~40 s/day; warmer refuses workers>1); parallel axis = children (processes), not days (w3_cache_warmer pattern).
- **Hardcoded SC blockers for search axes** (need SC change + pin bump): fill policy (close-only), resolver stop-first (field exists, never branched), stop anchor fixed, opposing/entry gaps 1m-only, ifvg_retest permanently blocked (`retest_trigger_unratified` unconditional), inversion body-close-only, tap-conflict hard drop, fixed parent/HTF ranking, no intraday flatten for IFVG, session gating uses `doc_sessions` not session_scheme (varying scheme changes artifacts_tag → decode rebuild without gating change).
- **QL-side policy gates blocking N-child orchestration as-is**: (13) `require_fixed_exploration_allowlist` refuses any allowlist except 26-day repair set or full DevelopmentReplayPolicy w/ frozen 10-day warmup prefix → **5-day verification policy needs a third trusted policy class**; (14) `build_ifvg_fsm_audit_v1` parity-gated to accepted doc-default v2 dataset → child audits need a non-parity-gated variant; (15) capability registry requires RUNNABLE base (children derive from doc-default only); (16) `_AUTHORITATIVE_SOURCE_BLOB` pin + dirty_status in identity → dirty trees churn child IDs; (17) no v2 per-day capture cache → every child re-drives full chain; (20) SC = pinned non-editable VCS wheel @a4e3303, `verification` raises if pin ≠ sibling HEAD; (21) prior search = downstream filter search (violates §9.5.9 if reused for FSM deltas); (22) no parent/child orchestrator exists — net-new.
- Reuse: `run_resumable_preparation_job`+`_profile_lock`+`PreparationJobState` (maps to §9.5/9.6 states), `manifest.dataset_id_for`+`_publish_or_reuse_verified` (reuse-by-identity solved), `DevelopmentDataAccess`/`DevelopmentReplayPolicy`, audit channel end-to-end, `capture_tag()/artifacts_tag()` as cache keys (artifacts_tag = TF-set-dependent → group children by TF set).
